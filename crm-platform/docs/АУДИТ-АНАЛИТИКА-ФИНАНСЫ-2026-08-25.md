# 🔎 АУДИТ АНАЛИТИКИ И ФИНАНСОВ Wallcov CRM — 2026-08-25 (read-only)

> 4 независимых агента (Meta реклама / органика+атрибуция+CAPI / финансы+сверка / пользовательский путь+доверие). Только чтение: SELECT, GET, логи. Код/прод/Meta/токены НЕ менялись. Прод: Netcup 159.195.136.213, ветка `codex/webchat-landing-crm`, HEAD 9db2708.
> Координация с Codex: это карта проблем. Кто берёт строку в работу — вписывает имя+дату. Исправления НЕ выполнены — ждём отдельного разрешения Олега.

## 1. КРАТКИЙ ВЫВОД
- **Финансам (касса) — можно доверять.** Журнал транзакций сходится сам с собой на всех уровнях; карточки сделок = БД точь-в-точь (8/8 проверенных сделок). «Противоположный знак» из аудита 18.08 исправлен.
- **Meta счётным цифрам (показы, клики, диалоги, расход в $, CTR, CPM) — можно доверять**: БД=API=экран байт-в-байт, дублей нет, синк идемпотентен.
- **Meta деньгам в гривне, всем ROAS/ROMI и связи «реклама→продажа» — доверять НЕЛЬЗЯ.** Курс подтянут только с 15.08 (60 из 71 дня «—»); атрибуция ~0 (0 из 33 667 сделок); ROAS показывает «0» там, где на самом деле «нет данных».
- **P&L «прибыль» — это норматив-модель, а не факт**: завышает в 2.4–5.2 раза. Реальная прибыль = «Направления»/журнал.

## 2. ЧТО ПОДТВЕРЖДЕНО ФАКТИЧЕСКОЙ СВЕРКОЙ
- Касса: журнал Σдоход = P&L revenue = Направления = Breakdown (7д 229 348 / мес 663 670 / год 6 395 046) — разница ≤0.7 грн (округление). Строки = итог. Утечка в «Без напрямку» = 0.
- Карточки сделок: оплата/маржа/бонус = прямой расчёт из БД (diff 0.00) на 8 сделках всех типов (оплата/аванс/скидка/переплата/2 платежа/отказ/склад).
- Meta счётное: расход $3 219.42, показы 1 017 498, охват 855 605, клики 36 094, диалоги 3 358, лиды 66, покупки 57, видео 972 650 — БД=API=экран. Уровни согласованы (account = Σ ad-строк за день). Паузы объявлений учтены.
- Аванс/переплата: #66202 аванс=1473.59 (сверка совпала). Фантом 2.39млн из прошлого не вернулся.
- CAPI идемпотентен (детерминированный event_id), приватность (только SHA-256 хеши).
- NULL vs 0 в органике — реализовано правильно («—» + пояснение про Reels).

## 3. КАКИЕ ЦИФРЫ НЕВЕРНЫ ИЛИ МОГУТ БЫТЬ НЕВЕРНЫ
| Цифра | Экран | Проблема |
|---|---|---|
| Реклама по курсу НБУ / Прибыль после рекламы / Общий ROAS / ROMI | Маркетинг Meta | «—» за 60 из 71 дня (курс не подтянут до 15.08), БЕЗ предупреждения |
| Выручка 0 ₴ / ROAS 0 (конус воронки) | Воронка Meta | рисует «0» вместо «нет данных»; за период закрыто 302 сделки/1.1млн, но 0 атрибутировано |
| Instagram: сделки 0 / выручка 0 ₴ | Маркетинг Meta | «атрибутированные», а атрибуции нет (0/508) → выглядит как «IG не принёс денег» |
| cost_per_lead / ROAS в воронке | Воронка Meta | спенд считается на 11 «валютных» днях против лидов за весь период |
| «Купили основной 301 (11%)» | Путь клиента | это наличие карточки сделки, НЕ оплата; реальных оплат 62; рядом lead_to_sale 2% |
| «Конверсия 93.9%» | Сводка продаж | это win-rate won/(won+lost), не лид→продажа; раздут (не ставят «проиграно») |
| P&L «чистая прибыль» | Финансы P&L | норматив-модель, завышает ×2.4–5.2 |
| «Каналы» выручка/ROAS | Финансы Каналы | метод начисления + ручной spend пустой → ROAS=None |
| Маркетинг ROAS | Воронка Meta | =0 (атрибуция сломана) |

## 4. КРИТИЧЕСКИЕ ОШИБКИ (🔴)
1. **Атрибуция реклама→продажа почти нулевая.** Лиды 136/4465 (3.05%), **сделки 0/33 667 (0%)**, campaign_id не сохраняется (0). Корень: метку ставит только прямой вебхук Meta Direct, а трафик идёт через ChatPlace мимо него; плюс `ai_convert_leads` хронически `converted=0` (лиды не превращаются в сделки). Влияние: exact-ROAS всегда 0, весь низ рекламной воронки = 0, CAPI не шлёт Purchase в Meta → оптимизация рекламы «голодает».
2. **Гривна и ROAS/ROMI пусты за 60 из 71 дня.** Курс НБУ подтянут только с 15.08; watch-синк переписывает лишь последние 7 дней, историю никто не бэкфиллит. На дефолтном экране — сплошные «—» без предупреждения (флаг `unavailable_until_insights_sync` пуст).
3. **Ловушка доверия: «0» вместо «нет данных».** Конус воронки рисует «Выручка 0 ₴, ROAS 0» (красным) на расход 21 148 ₴ — читается как «реклама вернула ноль», хотя это «атрибуции нет». (Соседний blended_roas честно = «—».)
4. **Воронка Meta занижает расход:** cost_per_lead/ROAS считаются на 11-дневном спенде против полного периода лидов; спенд не меняется при смене даты «от».

## 5. РИСКИ
**🟡 Высокие/средние:**
- P&L «прибыль» = норматив (завышает ×2.4–5.2) — легко принять за реальный заработок.
- Три разных «маржа %» (карточка-факт / P&L 46.1% / Каналы 48.89%) — несравнимы.
- «Каналы» = начисление (Deal.amount won), за 7д <10% кассы.
- Дубль ручной оплаты #65767 (вторая reqs-оплата 6010 без external_id не создала доход → «оплачено 12020», фантомный аванс 6010). Канал «reqs» не идемпотентен при пустом ref.
- Таймзона: большинство аналитических вьюх считают диапазон по UTC (`date.today()`), а бакеты по Киеву → в 00:00–02:59 Киев «сегодня»=вчера. MetaMarketingView сделан правильно.
- Поздние конверсии (>7 дней) и дни 16–25.06 через кнопку не бэкфиллятся.
- CAPI match-quality слабый (нет fbc/fbp/ctwa_clid/IP/UA).
- Воронки продаж реконструируются парсингом текста логов (`ActivityLog.detail`) — хрупко, при отсутствии записи тихо недосчитывает.

**🟢 Низкие/латентные:**
- Мультивалюта: CPM/«Расход»/cashflow-мини суммируют сырой $ без конверсии. Сейчас 1 кабинет USD, 1 USD-транзакция из 21 402 — эффект ~0, но при росте USD разъедется.
- reach суммируется по дням/постам (завышает уникальный охват); на экране показывается корректный account-level.
- like/comments не nullable («нет данных»=0).
- CPC, frequency, Meta action_values при синке отбрасываются — метрик нет.
- Stories = 0 записей (не сохраняются, ошибки edge глушатся).

## 6. КАРТА ИСТОЧНИКОВ ПРАВДЫ
| Показатель | Канонический источник | Метод |
|---|---|---|
| Расход/показы/клики/охват/диалоги/лиды-Meta/покупки-Meta/видео | `MetaAdDailyStat` (Meta insights) | касса Meta по дню |
| CTR/CPM/cost_per_* | считаются в CRM из MetaAdDailyStat | вычисляемые |
| Органика (reach/views/saves/follows…) | `MetaContentStat` (IG media insights) | lifetime-снимок |
| Подписчики | `MetaAccountDailyStat` | баланс+дневной прирост |
| Денежный приход/доход/выручка-факт | `Transaction`(журнал) | касса по дате |
| Оплата по сделке | `Payment`(is_paid) | подтверждённая оплата |
| Маржа сделки | карточка: Σ строк − факт-себест склада | по сделке |
| Себестоимость склада | `StockMovement`/`DealItem.cost` | снимок на момент продажи |
| P&L прибыль/маржа/безубыток | Финмодель (норматив %) | МОДЕЛЬ, не факт |
| Атрибуция рекламы | `Lead/Deal.meta_attribution` (вебхук Meta Direct) | ~3% лидов, 0% сделок |
| Выручка для ROAS | `Payment`+`Transaction` (CRM), НЕ Meta purchase value | касса |

## 7-8. КОНТРОЛЬНЫЕ ТАБЛИЦЫ (Meta + финансы) — см. §2 (все сверки пройдены) и детали в отчётах агентов.
Meta БД↔API↔экран: ✅ все счётные; 🔴 spend_uah/ROAS = None/«—». Финансы: ✅ 8/8 сделок, итоги день/7д/мес/год сходятся ≤0.7грн.

## 9. РАСХОЖДЕНИЯ источник↔база↔API↔экран
- spend_uah: `/api/meta-marketing/` = None («—»), а `/funnel/` = 21 148 (Sum отбрасывает NULL-дни) — два эндпоинта дают разное.
- Два ROAS: `/funnel/` = 0.0 («0»), `/api/meta-marketing/` blended = None («—»).
- Две «Выручки» на странице Meta: «успешных 0₴» (атрибут.) и «563 846₴» (ядровые воронки).
- Сводка `/api/analytics/`: leads_total 4465 (все) рядом с deals/revenue по ОДНОЙ авто-воронке — разный масштаб.
- 4-й уровень (сырьё Meta Ads Manager) НЕ сверялся — нет доступа к кабинету.

## 10. ОГРАНИЧЕНИЯ ВНЕШНИХ СИСТЕМ
- Meta не отдаёт «подписки с публикации» для Reels (#100) — честно «—».
- Follower_count insights — только 30 прошлых дней (не сегодня).
- Stories edge — окно 24ч, легко пропустить между синками.
- Курс НБУ (bank.gov.ua) — если ответа нет, spend_uah остаётся NULL без ретрая.

## 11. ЧТО НЕ УДАЛОСЬ ПРОВЕРИТЬ (нужен доступ)
- Сверка наших spend/impr/CTR с самим Ads Manager (кабинет `act_290942750588781`) — Meta MCP отключён. Рекомендация: выгрузить 1 день (16.06: $48.17 / 15 497 показов / 506 кликов) и сверить.
- Публикуются ли Stories вообще; подписан ли webhook-field `leadgen`; качество матчинга CAPI в Events Manager; почему referral не приходит (настройка click-to-message vs ChatPlace).

## 12. ПЛАН ИСПРАВЛЕНИЙ ПО ПРИОРИТЕТУ (НЕ ВЫПОЛНЕНО — ждём разрешения)
| # | Что | Приоритет | Кто |
|---|---|---|---|
| 1 | «0» → «—» когда нет атрибутированных продаж (конус/Instagram-выручка). Убрать ложный «ROAS 0» | 🔴 быстро | _(свободно)_ |
| 2 | Починить атрибуцию лид→сделка: чинить `ai_convert_leads` (converted=0) + захват campaign_id/ctwa_clid (ChatPlace-трафик) | 🔴 | _(свободно)_ |
| 3 | Бэкфилл курса НБУ на историю (spend_uah 16.06–14.08) + ретрай курса | 🔴 | _(свободно)_ |
| 4 | Воронка Meta: считать spend за тот же период что показы/лиды (не отбрасывать NULL-дни) | 🟡 | _(свободно)_ |
| 5 | Подписать P&L «модель» vs факт; развести 3 «маржа %»; «Купили» и «Конверсия 93.9%» переименовать честно | 🟡 | _(свободно)_ |
| 6 | Таймзона: диапазоны считать `timezone.localdate()` во всех вьюхах (как в MetaMarketingView) | 🟡 | _(свободно)_ |
| 7 | Идемпотентность reqs при пустом ref; разобрать дубль #65767 | 🟡 | _(свободно)_ |
| 8 | Показать «обновлено N назад» на всех аналитических экранах (сейчас только Meta) | 🟢 | _(свободно)_ |

## 13. ПЛАН АВТОКОНТРОЛЯ (чтобы не возвращалось)
- Ночной чек-инвариант: Σжурнал(доход) == P&L.revenue == Directions.доход (алерт при расхождении >1грн).
- Чек «нет FX-дней»: если spend>0 & spend_uah=NULL за >0 дней в запрошенном периоде — баннер «данные неполные».
- Чек атрибуции: % сделок с meta_attribution; алерт если exact-ROAS считается при 0 атрибуции.
- Чек `ai_convert_leads`: алерт если converted=0 N прогонов подряд.
- Правило: любой «ROAS/маржа/выручка» на экране обязан показывать источник (факт/модель) + период + время обновления.
