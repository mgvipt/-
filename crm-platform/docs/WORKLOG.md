# Worklog — Wallcov-CRM (gmideas/crm-platform)

> Лог всех изменений по проекту. Новые записи — сверху. Формат: дата · ветка/коммит · что сделано · файлы.

## 2026-06-20 · ветка `claude/wallcov-crm-dealcard-merge`

### Коммит `7db1879` — merge переработанной карточки сделки (по анализу конкурентов)
**Backend:**
- `backend/apps/crm/models.py` — `Deal`: +`discount_pct`, +`pay_type`, +`ttn`, +`checkbox_status`; `Contact`: +`loyalty_tag`, +`birthday`.
- `backend/apps/crm/migrations/0003_dealcard_fields.py` — миграция (только AddField с дефолтами). Применена на проде ✓.
- `backend/apps/crm/serializers.py` — `ContactSerializer`/`DealSerializer` отдают новые поля; `DealDetailSerializer` +`margin`, +`bonus`, +`days_in_stage`, +`contact_loyalty`, +`contact_id`.

**Frontend:**
- `frontend/src/pages/DealCard.tsx` — лента реальных событий вместо заглушки, инлайн-edit суммы/скидки, дни на стадии, блоки лояльность / скидка(+VIP авто-реко) / доставка-документы(ТТН/чек+бейджи) / маржа+бонус, быстрые действия (ссылка на оплату / ТТН / чек).

**Деплой:** `docker compose -f docker-compose.prod.yml build web caddy && up -d` → миграция OK, сайт 200, API отдаёт новые поля (проверено на сделке #9). Бэкапы `*.bak.*_dealcardmerge` на сервере.

**Демо-данные:** 6 контактов с тегами лояльности (VIP/Активный/Новый/Спящий) через `manage.py shell`.

### Документация
- `docs/CODEMAP.md` — код-карта всей CRM (модули backend, экраны frontend, «где менять типовые вещи», поток данных, точки интеграции). Зеркало в волте: `Projects/Wallcov-CRM/Код-карта.md`.
- `docs/WORKLOG.md` — этот файл.

### TODO (из Merge-плана)
- [ ] DealCard.tsx — реструктурировать в чёткие пронумерованные блоки (требование Олега).
- [ ] Канбан (Board.tsx): чипы Чек✓/ТТН✓ + Rotting.
- [ ] Финмодуль (Finance.tsx): P&L по ATM + дебиторка.
- [ ] Реальный чат в ленту из app `inbox`.
- [ ] Боевые кнопки ТТН/Checkbox/ссылка → `@action` в `DealViewSet` поверх готовых `integrations/adapters.py`.
- [ ] push ветки в origin (по запросу).

## 2026-06-20 (продолжение) · обход Bitrix + ТЗ переноса

### Что сделано
- Через браузер пройдены все сущности Bitrix24 (b24-gmideas.com.ua): лиды, сделки, карточки, товары в сделке, каталог/склад, клиенты, Список діалогів. Структура → волт Bitrix-структура-обход.md.
- Мультиагентный дизайн (4 домена) → docs/TZ-bitrix-merge.md (ТЗ «Перенос Bitrix + улучшения»): по сущностям, чаты-RBAC, пайплайн 15 стадий, план Этап 0-4.
- Сверка ТЗ с живым кодом: Conversation.assigned_to ✓, filterset assigned_to ✓, accounts can_see_all_deals/ScopedByRoleMixin ✓, ingest() не ставит assigned_to и плодит дубли (подтверждённый разрыв = боль Олега).

### TODO (план переноса)
- [ ] Этап 0: Contact.owner+дедуп-поля+миграция; права conversation.view.own/all; пайплайн 15 стадий.
- [ ] Этап 1 ⭐: чаты-RBAC (ingest dedup+assigned_to; ConversationViewSet scope; Inbox.tsx колонка Відповідальний).
- [ ] Этап 2: карточка сделки таймлайн+чат+боевые ТТН/чек/ссылка. Этап 3: карточки лида/клиента+дедуп. Этап 4: каталог-дерево+честный остаток.

## 2026-06-20 (продолжение) · перенос товаров из Bitrix + старт плана

### Каталог товаров (коммит 9ba87e3)
- warehouse/models.py: ProductCategory (дерево, bitrix_id) + Product.category/currency/bitrix_id; migration 0002.
- Команда import_bitrix_catalog (идемпотентно по bitrix_id, DRY-RUN→--commit).
- Выгрузка из Bitrix REST (iblock 24, /tmp/b24_export_products.py на маке): 1678 товаров → 982 перенесено, 696 в папке !!УДАЛИТЬ (id 3394) исключены. 33 категории.
- Импорт LIVE: 982 товара + 33 категории. Проверено: Product.count=987 (982+5 демо).

### Этап 0+1: чаты с RBAC (коммиты 8bd0c31 + ca12d6a)
- Contact.owner + last_touch_at (migration 0004); права conversation.view.own/all + can_see_all_conversations().
- ingest() авто-привязывает чат к ответственному (owner контакта/последней сделки), None=«Не призначені» без fallback.
- ConversationViewSet: RBAC-скоуп (менеджер видит только свои чаты) + ?scope=mine/unassigned + @action assign.
- Inbox.tsx: чипы Мої/Всі/Не призначені + ответственный в строке. seed_demo: право руководителям.
- Бэкфилл из сделок. Проверено: kirill(менеджер)→свои, head/director→все. Деплой OK, сайт 200.

### Статус плана
- [x] Перенос товаров (982) · [x] Этап 0 фундамент · [x] Этап 1 чаты-RBAC
- [ ] Этап 2: карточка сделки таймлайн+чат+боевые ТТН/чек/ссылка
- [ ] Этап 3: карточки лида/клиента + дедуп · [ ] Этап 4: каталог-дерево UI (Warehouse.tsx) + честный остаток

### Склад: каталог UI (коммит warehouse-catalog)
- Пагинация page_size 5/20/50/100/500 (DefaultPagination, max 500); фикс: раньше показывалось только 50.
- Дерево категорий (ProductCategory API) + фильтр по категории + поиск.
- Warehouse.tsx переписан (блоки [1..7]): дерево слева + таблица + пагинация + размер страницы.
- Единицы нормализованы (746 шт: коды 796/166/6 → шт/кг/м). Проверено в браузере под director.

### Склад: остатки + закупка (коммит 4b4f4ed)
- import_bitrix_stock: остатки из catalog.storeproduct → приход BITRIX-STOCK (идемпотентно). 175 товаров с остатком (65080 ед.). 113 не сматчены (папка !!УДАЛИТЬ).
- Warehouse.tsx: колонка «Закупка» (Product.cost) с инлайн-вводом (PATCH).
- ВАЖНО: закупочной цены в Bitrix НЕТ (только тип цены BASE/розница, PURCHASING_PRICE пуст, PROPERTY_124='Остатки' не закупка). Закупка вписывается вручную.

### Склад: закупочные цены (коммит import-cost)
- Закупка в Bitrix хранится в ПРИХОДНЫХ накладных (catalog.document.element.purchasingPrice), не в прайс-типе.
- import_bitrix_cost: последняя закупка по товару → Product.cost. 233 товара с закупкой. Пример OIKOS OTTOCENTO 2465/4365.

### Закупки v2 — полный источник (обновление)
- Источник закупки в Bitrix двойной: поле товара catalog.product.purchasingPrice (колонка «Закупівельна ціна», 614 товаров) + приходные накладные (fallback).
- b24_export_cost2.py объединяет оба → 919 закупок. Импорт: 330 товаров с закупкой (было 233). Остальные — нет данных в Bitrix (вписываются вручную).

### Склад: карточка + инвентаризация + аналитика (коммит inv-analytics)
- Карточка товара (клик по названию): розница/закупка/маржа/остаток + история движений (ProductViewSet.movements).
- Инвентаризация (kind=inv): факт→остаток выравнивается, движение=факт−учёт. Проверено end-to-end (4.1→10→4.1).
- Аналитика: вкладки Продажі/Склад. Склад: запас по закупке 1.14М/рознице 3.48М, потенц.маржа 2.34М, в наличии 180/987, по категориям (InventoryAnalyticsView).
- Единица учёта: по каждому товару своя (шт 747/кг 89/см 80/м 23/мл 20/л 8...).

### Инвентаризация: бухгалтерская ведомость (коммит inv-sheet)
- InventorySheetView: Початковий(до from) + Надходження − Продано(за період) = Кінцевий обліковий. Розбіжність = Факт − обліковий.
- Период (from/to, по умолч. текущий месяц). Проверено: OIKOS поч 0/надх +10/прод −5.9/кінц 4.1.

### Финмодуль: план + sticky-шапка инвентаризации (коммит inv-sticky)
- Sticky-шапка таблицы инвентаризации (не прячется при скролле). Проверено.
- Мультиагентный анализ Cashflow (/opt/finmap-bridge/src/services/kpi/) → ТЗ docs/TZ-finmodule.md: точка безубыточности (формулы A реализ.+B ATM), P&L ATM 5 уровней, маржа сделки, ЗП, FIFO склад, проводки инвентаризации. План Этапы 0-3.

### Финмодуль Этап 0+2 (коммит finmodule)
- FinModelArticle справочник (14 статей сид) + права finance.manage/analytics.view.
- P&L ATM (compute_pnl, 5 уровней) + Точка безубыточности (compute_breakeven, формулы из Cashflow): ТБ=176173₴, маржа 38.22%.
- Finance.tsx вкладки: Дашборд/P&L/Точка беззбитковості/Фінмодель (CRUD статей).
- TODO Этап 1 (FIFO склад+проводки inv), Этап 3 (аналитика менеджеры/ЗП/планы).

### Финмодель: реальные данные + точная ТБ (коммит finmodel-real)
- Заменил placeholder на 27 РЕАЛЬНЫХ статей из Cashflow (бэкап /opt/backups/.../finmap.db fin_model_articles).
- compute_breakeven 1:1 как admin_router.breakeven: ТБ=276635₴/міс costs /(38.22%/100)=723808₴/міс. P&L пересчитан.
- Источник: реальная финмодель Wallcov, не выдуманные значения.

### Финмодель: напрямки бізнесу (проекти Finmap) — коммит directions
- FinDirection: 6 напрямків з Finmap Проекти (план дохід/витрати/прибуток = реальні річні Finmap).
- Transaction.fin_direction FK → факт по напрямку з CRM. DirectionsReportView (план/факт/рентабельність).
- Finance.tsx вкладка Напрямки. Рентабельність співпадає з Finmap (ДЕКОР 13%/Обєкти 34%/Hardwork 22%/Маркетинг 97%/Особисті -135%).
- Cashflow UI-match: 4 карти ТБ (Сума фондів 61.78%) + прогрес-прогноз.

### Аналітика: канали доходу (коммит channels)
- Превью СРМ (preview СРМ.html) звірено: структура CRM вже збігається (тема/телефонія/ролі-матриця/інтеграції). Наша CRM потужніша (P&L/ТБ/Напрямки).
- Додано канали доходу: розширено джерела (Сайт/Опт/Дизайнери/TikTok), ChannelSpend для ROAS, compute_channels, вкладка Аналітика→Канали (виручка/частка/ROAS/чистий внесок).
- Мультиагентний фінплан: повне ТЗ docs (канали+ROAS+планування FinPlan+план-факт). Наступне: FinPlan (план доходів/витрат), Meta-spend авто.

### Финмодель: повна структура фондів з Finmap (коммит funds-full)
- Зайшов у Finmap (app.finmap.online → Аналітика → Проекти → розгорнув ДЕКОР_Товари): зняв повну структуру фондів ФВ(виручки)/ФМ(операційні)/ФСКД(розвитку).
- Додав недостаючі: ФВ ФОТ%продажу, ФВ Дивіденди майстрам, ФМ Маркетинг СММ + нова категорія skd (ФСКД: обладнання/навчання/госп/офіс). seed 35 статей.
- Finmap accounts-фонди (ФОНД ОПЕР/НАЛ/УПРАВЛІННЯ, КАРТА Кредитка/Сервіси/Маркетинг/Особисті) — TODO як finance.Account.

### Журнал + фікси багів (коммит journal-fixes)
- Журнал транзакцій (Finance→Журнал): список + створення з фондом(fin_article)+напрямком(fin_direction)+каналом+рахунком. Як Finmap.
- ФІКС: + Сделка / + Создать (модалки створення); картки лідів кліки (LeadCard + /leads/:id роут); Lead.convert→сделка.
- Фін-аналітик: загальний пул фондів+аллокація, 2 рівні Напрямок→Канал, конверт-логіка ФВ→ФМ→ФСКД, ТБ по напрямку+зведена, 3-рівневі підфонди. ТЗ збережено.
- TODO (з великого списку Олега): редаговані стадії воронки+кольори+додати/видалити; чат+AI у картці сделки; підфонди(ієрархія); планування по фондах-конвертах (остаток фонду); проваль у журнал напрямку; звʼязки сутностей; візуал шапки воронки.


---

## 2026-06-27 — 07-01 · Склад, НП-трекінг, AI-витрати, Ролі (Claude Opus 4.8)

### Склад · Робота (WarehouseWork.tsx)
- Канбан «Мої задачі» по NP-стадіях: ТТН створена → НП·Відправлено → В дорозі → Прибув → Отримано.
- Зведення задачі: товари + виявлення потреби + фото клієнта (перед тонуванням).
- Превʼю фото складу — постійний том `/root/crm-media/warehouse_photos` (раніше губилися при build).
- Кнопки у шапці задачі (іконки): 📄 накладна (KpDoc — та сама що в сделці) + 🎨 бланк викраски (модалка VykraskaDoc + Друк).
- Доступ складу до сделки (get_object broaden) для НП-форми.

### НП-трекінг (np_status_sync.py, крон */20)
- StatusCode НП → авто-рух стадії + авто-повідомлення клієнту (шаблони з Bitrix: Відправлено / Прибуло+перевірка цілісності / 3-дні). Флаги msg_* у np_data (1 раз). Cap 40/прогон.
- create_ttn ставить «НП_ТТН створена» + зберігає ttn_ref. recreate/cancel шукають Ref за номером (getDocumentList). Друк ТТН 4 формати (np_print, проксі).

### AI-витрати (контроль)
- priority_sweep → Haiku 4.5, sales_analyst → Sonnet + переключатель «авто/тільки кнопка» (analyst_auto default OFF).
- Prompt caching: claude_json(system,cache) + агент _call (system+tools cache_control). AgentConfig: priority_model/analyst_model/suggest_model/cache_enabled/analyst_auto. UI: Налаштування→Агент→«Моделі ШІ та витрати».

### Ролі / співробітники
- Звільнення: деактивація + round-robin сутностей (продажник→всі продажники; інший→керівник відділу/всі). cost.view прибрано з ролі Комірник. Розділ Розвиток/Аналітика по правах (development.view/analytics.view). Обʼєднано дублі Олега (1 акаунт director).

### Lead-lifecycle / інше
- Закриття чату→знімок стадії; повернення клієнта→новий лід з перенесеним контекстом. Reset клієнта (тест).
- Відправка файлів IG ПОСИЛАННЯМ (SharedLink + /api/f/<token>/) — обхід обмеження IG.
- CallBar — плаваюча кнопка «покласти слухавку». Фікс фокусу полів (поллінг DealCard не чіпає qualification/items). Бланк викраски — колонки 50/2/13/35.
