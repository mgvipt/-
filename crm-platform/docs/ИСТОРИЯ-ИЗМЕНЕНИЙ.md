## 09.08.2026 — Приход-накладная поставщика: создать карточку товара прямо при заполнении

**Что попросили:** при обработке накладной поставщика (кредиторка), если нужного товара нет в номенклатуре, уметь создать карточку товара прямо там, не выходя из накладной.

**Что сделали:**
- В поле «Наш товар (склад)» в выпадающем списке появилась кнопка **«＋ Створити товар «название»»**. Клик — создаёт карточку товара с этим названием (со складским учётом) и сразу подставляет её в строку накладной.
- Дальше можно указать кол-во/коэффициент/сумму и провести приход как обычно. Розничную цену и детали можно дописать позже в карточке товара.

**Где в системе:** CRM → Финансы → входящая накладная поставщика → строка товара → поиск «Наш товар».

## 10.08.2026 — Письмо с двумя накладными → две записи во «Входящих» (два прихода)

**Что было не так:** если поставщик присылает в одном письме ДВЕ накладные (два Excel), во «Входящие» подтягивалась только одна — вторая терялась. Нельзя было сделать два прихода.

**Что сделали:** теперь каждая накладная (Excel) из письма создаёт ОТДЕЛЬНУЮ запись во «Входящих» → отдельный приход. Если у обеих один номер заказа (как у ФОП Кріжевські — «Шпаклівка» и «Кварц» под № ОПТ-009896), они всё равно идут как две отдельные записи с пометкой (1/2) и (2/2). Повторную присылку того же письма система по-прежнему не задваивает.

**Конкретно по твоему письму:** вторую накладную («Кварц») добрал из уже сохранённого вложения — теперь во «Входящих» две записи, обе черновики, делай два прихода.

**Где в системе:** Входящие документы (накладные с почты).

## 10.08.2026 — Список чатов больше не «прыгает наверх» во время работы

**Что было не так:** когда менеджер работал в выбранном чате, список слева каждые несколько секунд пересортировывался, и чат (или вся прокрутка) прыгал наверх — легко было потерять место.

**Что сделали:** убрали лишнюю пересортировку списка на каждое обновление. Порядок приходит с сервера (новые/сегодняшние сверху) и держится «на месте» — выбранный чат и позиция прокрутки остаются там, где были. Новые чаты по-прежнему добавляются сверху, но список под руками не дёргается.

**Где в системе:** Чаты / Открытые линии — список слева.

## 07.08.2026 - Аналитика склада: категории раскрываются в товары (дерево)

- В складской Аналітике «Запаси по категоріях» каждая категория теперь **раскрывается** (клик по строке) и показывает **товары внутри** с количеством, закупкой и розницей.
- Удобно проверить, почему запас по рознице большой — видно, какие именно товары и в каком количестве держат стоимость (напр. дорогие краски с большим остатком).

## 10.08.2026 — Понятные и компактные значки каналов в карточке клиента

**Что было не так:** когда у клиента много диалогов, значки каналов (Telegram, Instagram…) шли по одному на диалог — 8 одинаковых «Telegram» занимали пол-экрана и не различались.

**Что сделали:** значки теперь группируются по каналу — один значок на канал (8 диалогов Telegram → один значок с цифрой «8»). Названия понятные: «Telegram-бот», «Telegram · номер» (для e-chat), «Viber · номер», «WhatsApp · номер», «Веб-чат» и т.д. Слева от названия — цветная точка: зелёная = есть активный диалог, серая = все закрыты. Клик открывает активный диалог канала, кнопка «⧉» — вторым окном. Компактно, но видно что есть что.

**Где в системе:** Карточка клиента → блок «Чат з клієнтом» → значки каналов сверху.

## 10.08.2026 — Telegram: убрали дублирование сообщений (только e-chat Telegram)

**Что было не так:** телефон 380973282283 был подключён к Telegram ДВАЖДЫ — как бот с Business-подключением и как e-chat Telegram. Из-за этого исходящие сообщения бизнес-аккаунта (через e-chat) бот тоже «ловил» и записывал к себе — с неправильным направлением и каждый раз в НОВЫЙ диалог. Один клиент превращался в кучу диалогов «Telegram бот», а ответ менеджера выглядел так, будто ушёл «в общий бот», а не клиенту.

**Что сделали:** по решению — Telegram работает ТОЛЬКО через e-chat (на номер). Бот больше не записывает к себе бизнес-сообщения личного номера (перестал дублировать и путать направление). Обычные сообщения, которые пишут напрямую боту, работают как раньше. (Обратимо: config-флаг ingest_business.)

**Важно:** раз Telegram теперь только через e-chat — переписка клиента в Telegram появляется в CRM, только если жива сессия e-chat. Если e-chat падает — сообщения не придут (за этим надо следить у e-chat).

**Где в системе:** входящие Telegram — канал «Telegram (e-chat)».

## 07.08.2026 - Планирование фондов: не прыгает при смене месяца + быстрее

**Проблема:** при переключении месяца «Залишок у фондах» показывал старые цифры прошлого месяца и «прыгал» на правильные через паузу.
**Что сделали:**
- При смене месяца сразу показывается «Завантаження фондів…», а не старые цифры (данные очищаются до загрузки) — больше не прыгает.
- Баланс счетов считается одним махом (2 запроса вместо ~64) — страница легче и устойчивее под нагрузкой. Цифры не изменились (сверка 0 расхождений).

## 07.08.2026 - Аналитика денег: комиссия НоваПей, привязка наложки, легаси-минус, воронки в ЗП

- **Комиссия НоваПей теперь в аналитике** (влияет на маржу): при выплате наложки система пишет доход по КАЖДОЙ сделке (номинал) + комиссию НоваПей отдельным расходом на сделку. Раньше комиссия нигде не проводилась — маржа по наложке была завышена.
- **Привязка наложки-пачки исправлена**: когда НоваПей платит за несколько посылок одним переводом — доход раскладывается по своим сделкам (номинал каждой), а не вешается весь на одну. Аналитика по сделке/менеджеру теперь честная.
- **Легаси-клиенты (Битрикс) больше не показывают ложный минус**: себестоимость считается только по сделкам, у которых есть доход в журнале CRM (раньше по старым сделкам себестоимость считалась, а выручки не было → ложный убыток).
- **Сделки менеджера — разбивка по воронкам**: в окне «N сделок» (ЗП/KPI) сверху чипы «сколько денег по каждой воронке» + колонка «Воронка» в списке. Видно, сколько менеджер привлёк по каждому направлению.

## 07.08.2026 - Безопасность: рядовой не видит чужие зарплаты (аудит)

**Дыра (нашёл аудит):** списочный эндпоинт ЗП/KPI (`/api/finance/salary/` без выбора юзера) не проверял права — любой с правом «finance.view» видел зарплаты ВСЕЙ команды + общий ФОТ. Затрагивало Илону Ковальчук (рядовой менеджер).
**Фикс:** рядовой сотрудник (без roles.manage/superuser) в списке ЗП видит теперь ТОЛЬКО свою зарплату. Владелец/руководитель — всю команду как раньше.

## 07.08.2026 - ЗП/KPI: видно скидки, которые дал менеджер сам

- В карточке ЗП/KPI каждого менеджера появился бейдж **«🏷️ скидки −X ₴ (N)»** — сумма скидок по его сделкам за месяц (несогласованные скидки на контроль).
- **Клик по бейджу → окно со списком сделок**, где была скидка (сумма скидки по каждой, клик открывает сделку).
- По решению: скидки **только показываются**, автоматически ЗП не уменьшают. Ты видишь и решаешь сам.
- Пример (август): Илона Ковальчук — 8 195 ₴ по 6 сделкам; Кирилл Оксаненко — 1 668 ₴ по 12.
**Где:** Фінанси → Плани → ЗП/KPI.

## 07.08.2026 - Карточка клиента: честная маржа, скидки, права, «Купив у магазині»

**Было не так:**
- Маржа показывала 99%, потому что себестоимость товара считалась только у ВЫИГРАННЫХ сделок (отгружено). Сделки на «Оплату отримано/Отримано» не учитывались.
- «Мастерам выплачено» показывалось для Instagram-продажи товара — система принимала банковские комиссии за выплаты мастерам.
- «Купив у нашому магазині: 0» — считались только выигранные сделки, а «Отримано» не помечены как выигранные.

**Стало:**
- **Честная маржа/себестоимость.** Себестоимость считается с момента ОПЛАТЫ сделки (не только отгрузки). На примере: маржа стала 74% вместо 99%.
- **Комиссии банка — отдельно**, больше не считаются «выплатой мастерам». Блок мастеров показывается только если в сделке реально есть услуги.
- **«Купив у магазині»** теперь = сумма купленных (оплаченных ИЛИ выигранных) сделок.
- **Скидки по сделкам** — новый блок: сумма скидок, клик → окно со списком сделок со скидками. Скидка уменьшает маржу.
- **Маржа и закупка по каждой сделке** видны в списке сделок карточки.
- **Права.** Менеджеры в карточке клиента видят только доход + комиссии (без маржи/себестоимости/прибыли/скидок). Полные финансы — по праву «Картка клієнта: повні фінанси» (`client.finance.full`). В самой сделке себестоимость у менеджеров осталась.

## 09.08.2026 — Виджет телефона грузится сразу и не мигает

**Что было не так:** виджет телефона появлялся с задержкой (ждал загрузки конфига телефонии), потом «дорастал» панелью набора после подключения — визуально мигал и прыгал.

**Что сделали:** теперь CRM запоминает, что у вас есть телефония, и показывает виджет СРАЗУ при загрузке страницы (не ждёт ответа сервера). Панель набора теперь стабильна — не появляется рывком после подключения, а сразу на месте; кнопка звонка просто неактивна, пока идёт «подключение…», и включается, когда линия готова. Мигание и прыжки убраны.

**Где в системе:** левая панель — виджет «Телефон».

## 09.08.2026 — Чаты грузятся быстро (убрал тормоза при перезагрузке)

**Что было не так:** при перезагрузке страницы список чатов грузился медленно — сервер делал ~500 запросов к базе на одну страницу (по ~10 на каждый чат: последнее сообщение, счётчики, сделка и т.д.).

**Что сделали:** переписали расчёт так, что все данные для страницы берутся за 8 запросов вместо 500 (одним махом на всю страницу, а не по каждому чату отдельно). Плюс убрали три лишних поля, которые больше нигде не использовались. Загрузка списка ускорилась примерно в 2,5 раза (и нагрузка на базу упала в 60 раз).

**Где в системе:** Чаты / Открытые линии — список слева (быстрее открывается и обновляется).

## 09.08.2026 — Второе окно чата прямо в диалоге с клиентом (карточка)

**Зачем:** видеть переписку клиента из другого канала прямо в блоке диалога, не переключаясь — и сворачивать её, когда мешает.

**Что сделали:** в блоке «Диалог с клиентом» (карточка клиента/лида/сделки) у каждого другого канала клиента появилась кнопка «⧉». Нажимаешь — под основной перепиской открывается ВТОРОЕ окно с чатом из того канала. Заголовок второго окна: «Свернуть ▲ / Развернуть ▼» и «✕ закрыть». Свернул — осталась только полоска-заголовок, развернул — снова видно переписку. Второе окно можно растянуть за нижний угол.

**Где в системе:** Карточка клиента → блок «Диалог с клиентом» → чипы каналов сверху, кнопка «⧉» рядом с другим каналом.

## 09.08.2026 — В окне диалога видно, что клиент писал в других каналах

**Зачем:** клиент писал в веб-чат, потом перешёл в Telegram — а менеджер не помнит, что было в веб-чате (или в другом мессенджере).

**Что сделали:** над перепиской появилась полоса «Клиент писал ещё:» с кнопками всех остальных каналов этого клиента (Instagram, Telegram, Viber, WhatsApp, веб-чат). На кнопке — иконка канала, название и красный счётчик неотвеченных. Один клик — и открывается переписка из того канала, не нужно искать вручную.

**Где в системе:** Чаты / Открытые линии → открыть диалог → полоса каналов под шапкой (появляется, если у клиента есть переписка в других каналах).

## 09.08.2026 — Инструмент «Дубли товаров» на складе

**Зачем:** чтобы чистить каталог от карточек-двойников (как было с праймером), безопасно и своими руками.

**Что сделали:** новая вкладка на складе «Дубли товаров» (видна тем, у кого право «Редактировать склад»):
1. Система сама находит ТОЧНЫЕ дубли (одинаковые названия) и показывает по каждой карточке остаток/продано/сделки. Выбираешь главную — жмёшь «Склеить»: продажи, приходы и остатки собираются на неё, дубли скрываются.
2. Ручная склейка — для дублей с разным артикулом (как праймер «1001» и «1001_100»): находишь карточки поиском, кладёшь в корзину, выбираешь главную, склеиваешь.

⚠️ Важно: автопоиск ищет ТОЛЬКО одинаковые названия — он специально не «угадывает» похожие, потому что размерные варианты (шпатель 300/400 мм, шлифлента №40/№240) отличаются одним символом, как настоящий дубль, и их легко перепутать. Похожие с разными артикулами склеиваешь вручную, проверив глазами. Каждая склейка пишет лог для отката.

**Где в системе:** Склад → вкладка «Дубли товаров».

## 09.08.2026 — Праймер: склеили 5 карточек-дублей в одну + выбор товара в сделке только активные

**Что было не так:** праймер «Primer Deep 1» существовал как 5 отдельных карточек (наследие импорта из Битрикса). Продажи и приходы «размазались»: продажи с 31-го попали на одну карточку, приход — на другую, инвентаризация — на третью. Остаток был неверным (одна карточка даже в минусе −100). Плюс в сделке при выборе товара всплывали старые/неактивные позиции со старыми ценами.

**Что сделали:**
1. Склеили все 5 карточек праймера в одну (id 1927, артикул B24-38725): 1139 позиций в сделках, 54 движения склада и мэппинг поставщика перенесены на неё, 4 дубля деактивированы. Цены и количества в старых сделках не менялись. Итоговый остаток стал корректным (100 приход − 122 продано + 24 инвентаризация = 2 шт). Сделан лог для отката.
2. Выбор товара в карточке сделки теперь показывает ТОЛЬКО активные товары — старые/скрытые/дубли больше не появляются в списке.

**Где в системе:** Склад → номенклатура (праймер теперь одна карточка); Карточка сделки → добавление товара (только активные).

## 09.08.2026 — Красный кружок в чатах всегда с числом (нет пустых)

**Что не нравилось:** на диалогах горели пустые красные кружки без числа (там, где ответил только ИИ, а живой менеджер ещё не заходил).

**Что сделали:** теперь красный кружок ВСЕГДА показывает число — сколько сообщений клиента ещё не обработал живой менеджер (считается с момента последнего ответа человека; ответы ИИ/бота за ответ не считаются). Если обрабатывать нечего (клиент ничего не писал или человек уже ответил и клиент молчит) — кружка нет вовсе. Пустых кружков больше не будет.

**Где в системе:** Чаты / Открытые линии — список диалогов слева.

## 09.08.2026 — Кладовщик может проводить инвентаризацию (без полного доступа к складу)

**Что попросили:** Илона внесла фактические остатки, но не могла провести инвентаризацию — стоял блок. Дать ей доступ именно к проведению инвентаризации, не открывая редактирование приходов/реализаций.

**Что сделали:**
- Проведение инвентаризации теперь доступно по праву «Склад → вкладка Инвентаризация» (warehouse.tab.inventory). Раньше требовался полный доступ к редактированию склада.
- Редактирование приходов и реализаций по-прежнему требует полного складского доступа — кладовщик их менять не может.

**Также:** проведена инвентаризация за период (данные Илоны от 08.08) — 30 позиций, датой 08.08; остатки приведены к фактическим.

**Где в системе:** CRM → Склад → Инвентаризация → Провести.

## 07.08.2026 - Кнопка «Обновить цены» — под правом (только ответственный)

- Кнопка **«🏷️ Обновить цены»** теперь под правом **«Оновлення роздрібних цін з номенклатури»** (`deal.price.refresh`).
- Сейчас её видит **только владелец** — у всех остальных кнопки нет, и действие на бэкенде тоже закрыто (403 без права).
- Чтобы дать доступ ответственному: Ролі/Права → выдать право «Оновлення роздрібних цін з номенклатури» этому человеку.

## 08.08.2026 — Диалог нельзя перехватить, если его уже взял другой менеджер

**Что было не так:** Илона взяла диалог в работу, а у Кирилла страница не обновилась — он видел диалог как «свободный» в общем списке, нажимал «Взять в работу», и диалог переназначался с Илоны на Кирилла. Одного клиента вели двое.

**Что сделали:** поставили «замок очереди» на сервере. Если диалог уже за другим менеджером — перехватить его нельзя, даже если страница не обновлена: система отвечает «Діалог уже взяв у роботу {имя}. Перехопити не можна — зверніться до керівника». Перекинуть диалог может только руководитель (через «Переадресувати»). Плюс: при открытии диалога его статус подтягивается заново, а в меню вместо «Закрепить за мной» показывается «Взято: {имя}», если диалог уже чужой.

**Где в системе:** Чаты / Открытые линии → открыть диалог → меню «⋯» → «Закрепить за мной».

## 08.08.2026 — Чаты: имя менеджера в переписке + правильная сортировка блоков (сегодня сверху)

**Что просили:** 1) в переписке над сообщением писало просто «Менеджер» — непонятно, кто именно отвечал. 2) Чаты, где клиент написал сегодня, оказывались где-то в июне; хотелось: сверху новые, потом «Потрібна відповідь», потом «Чекаємо клієнта» — и внутри каждого блока сегодня всегда наверху.

**Что сделали:**
1. **Имя автора сообщения.** Теперь показываем реального человека: если ответили из нашей CRM — имя и фамилия (Кирилл Оксаненко, Илона…). Ответы, набранные прямо в приложении ChatPlace, помечаются «Менеджер · ChatPlace» — ChatPlace, к сожалению, не передаёт нам имя конкретного оператора, поэтому показать его невозможно (это ограничение ChatPlace, не наше). Ответы Юли — «Юля (AI)».
2. **Сортировка и фильтры.** Кнопки «Потрібна відповідь» и «Чекаємо клієнта» теперь настоящие фильтры (считаются на сервере), поэтому сегодняшние чаты всегда наверху, а счётчик показывает реальное количество, а не только подгруженное. Внутри каждого блока (Новые → Потрібна відповідь → Чекаємо клієнта) чаты идут строго по дате: сегодня наверху, дальше вниз.

**Где в системе:** Чаты / Открытые линии — список слева (фильтры сверху) и окно переписки (подпись над каждым сообщением).

## 08.08.2026 — Убрали ложный красный кружок на пустых чатах

**Что было не так:** в списке «В работе» два чата (Виталий Мороз, Елена Гриценко) горели красным кружком «не отвечено», но при открытии были совсем пустые — ни одного сообщения. Красный ставился по правилу «менеджер не ответил», а отвечать там не на что: клиент вообще ничего не писал.

**Что сделали:** красный кружок теперь загорается только если клиент реально написал (есть хотя бы одно входящее сообщение). Пустые чаты и чаты, где мы написали первыми без ответа клиента, красным больше не отмечаются. Чаты с реальными сообщениями клиента без ответа — как и раньше, красный + число.

**Где в системе:** Чаты / Открытые линии → блок «В работе».

## 08.08.2026 — Ведомость инвентаризации: инвентаризационные коррекции больше не считаются «Продано/Надходження»

**Что было не так:** в ведомости у товара показывалось «Продано −1 ↗ сделки», хотя товар не продавали и сделки нет. Причина — колонки «Надходження/Продано» считались по знаку любого движения, включая корректировки прошлых инвентаризаций.

**Что сделали:**
- «Надходження» теперь = только приходные накладные (kind=in).
- «Продано» = только реализации/отгрузки по сделкам (kind=out).
- Корректировки инвентаризаций больше не попадают в эти колонки, но по-прежнему учитываются в «Кінцевому обліковому» остатке (он остаётся верным).
- Недельная проверка «двигавшихся» товаров тоже считает только реальные продажи.

**Где в системе:** CRM → Склад → Инвентаризация → Ведомость.

## 07.08.2026 — Кнопки подстраиваются под размер окна (карточка сделки и вся CRM)

**Что попросили:** при уменьшении окна кнопки вылезали за пределы блока, а на маленьком экране были слишком большими и налазили друг на друга — неудобно работать.

**Что сделали:**
- **Панель товаров в сделке** (Додати / Список / Своя / Прихід / Оновити закупівлю…) — кнопки больше НЕ вылезают за край блока: при нехватке места переносятся на следующую строку, поле поиска сжимается.
- **Все кнопки CRM** теперь плавно меняют размер и шрифт под ширину окна: на широком экране — как раньше, на узком — компактнее, чтобы не занимать лишнее место и не налазить друг на друга.

**Где в системе:** заметно везде, ярче всего — в карточке сделки при сужении окна.

## 07.08.2026 — Права на вкладки клиентов (скрыть Поставщиков/Мастеров и т.д.)

**Что попросили:** в правах уметь ограничивать сотрудникам доступ к вкладкам контрагентов — чтобы они не видели вкладки «Поставщики», «Мастера» и т.д., а по необходимости выдавать ответственным доступ к нужной вкладке.

**Что сделали:**
- В разделе «Клиенты» вверху есть вкладки-сегменты: **Клиенты / Поставщики / Мастера / Сотрудники / Партнёры**. Раньше их видели ВСЕ сотрудники (включая закупочные цены и реквизиты поставщиков).
- Теперь по умолчанию сотрудник видит только вкладку **«Клиенты»**. Вкладки **Поставщики / Мастера / Сотрудники / Партнёры скрыты**, пока в правах роли/отдела/сотрудника не выдано право на конкретную вкладку.
- Право на каждую вкладку — отдельная галка в **Настройки → Права** (группа «Клієнти»): «Вкладка Постачальники», «Вкладка Майстри», «Вкладка Співробітники», «Вкладка Партнери». Выдал — человек видит эту вкладку и контакты в ней.
- Вкладку **«Клиенты»** видят все по умолчанию. Чтобы скрыть её у конкретного сотрудника — добавить ему код `contact.kind.client` в «Запрещённые».
- Кнопка **«+ Поставщик»** видна только тем, у кого открыта вкладка «Поставщики».
- Сейчас доступ ко всем 4 закрытым вкладкам оставлен роли **«Власник»** и супер-администратору. Остальным — скрыто. Кому ещё открыть — отметить галкой в правах.

**Где в системе:** раздел «Клиенты» (вкладки вверху) + Настройки → Права (группа «Клієнти», галки «Вкладка …»).

## 07.08.2026 — Кнопка «Провести реализацию» (повторно, по текущим позициям)

**Что попросили:** после отмены реализации нужно провести её обратно. В Битриксе это делалось сменой стадии туда-обратно; попросили свой вариант.

**Что сделали:**
- **Кнопка «Провести»** в строке «Реализация 📤» карточки сделки (когда реализация в статусе черновик/отменена). Списывает товар со склада по ТЕКУЩИМ (уже исправленным) позициям — количеству и себестоимости. Не зависит от воронки/стадии (надёжнее, чем смена стадий).
- Логика строки: проведено → «Отменить»; черновик → «Провести». Право — склад/бухгалтер.

**Важно:** если товар помечен «без складского учёта» (track_stock=false) — реализация его НЕ списывает. Такой товар нужно включить в складской учёт в карточке товара.

**Где в системе:** карточка сделки → блок сумм → строка «Реализация» → кнопка.

## 07.08.2026 — Ответственный (админ/бухгалтер) может исправить оплаченную сделку

**Что попросили:** защита от изменения оплаченных сделок нужна, НО у ответственного (админ или бухгалтер) должна быть возможность всё поправить. И чтобы при перевыборе товара в сделке подставлялись актуальные цена и закупка.

**Что сделали:**
- **Обход замка для ответственного.** Раньше по любой оплаченной сделке нельзя было менять кол-во/цену/товар (защита фискального чека). Теперь **админ и бухгалтер** (право «обработка платежей») могут исправлять оплаченные сделки. Обычным менеджерам — по-прежнему нельзя, и теперь они видят понятное сообщение почему.
- **Актуальные суммы при перевыборе.** Когда ответственный перевыбирает товар в позиции — автоматически подставляются свежая розничная цена и закупка из номенклатуры (работало и раньше, но мешал замок).

**Порядок исправления оплаченной сделки (для админа/бухгалтера):** отменить реализацию → поправить приход → в сделке перевыбрать товар/обновить цены → суммы подтянутся.

## 07.08.2026 — Отмена реализации из сделки + понятные уведомления, почему нельзя изменить

**Что попросили:** уметь исправить неправильно проведённую сделку: отменить реализацию (товар вернётся на склад), потом отменить приход и поправить; и чтобы при блокировке было видно ПОЧЕМУ нельзя что-то изменить (а не молча).

**Что сделали:**
- **Отмена реализации в сделке.** В карточке сделки у строки «Реализация 📤» появилась кнопка **«Отменить»** — товар возвращается на склад (в количестве позиций сделки). Дальше можно отменить/поправить приход и, если сделка не оплачена, изменить позиции. Право — склад/бухгалтер.
- **Понятные уведомления.** Раньше при изменении товара в оплаченной сделке «ничего не происходило» молча. Теперь при любой блокировке показывается сообщение ПОЧЕМУ (например: «по сделке есть чек/оплата — менять товары нельзя»). Касается добавления, перевыбора, изменения кол-ва/цены, удаления позиций.

**Порядок исправления:** сделка → «Отменить» реализацию → Склад → приход «Отменить/Редактировать» → сделка → «Обновить закупку»/«Обновить цены».

## 07.08.2026 — Приход: редактирование отменённой накладной + дата накладной в движениях + фикс автооткрытия карточки

**Что попросили:** (1) редактировать приход, если он отменён; (2) в «Рух товару» показывать дату накладной, а не создания; (3) при перезагрузке страницы сама открывалась карточка товара.

**Что сделали:**
- **Редактирование прихода.** Если приход в статусе «Черновик» (отменён или ещё не проведён) — появилась кнопка **«Редактировать»**: открывается форма прихода, можно изменить позиции, количество, цену, поставщика, №, дату. Проведённый приход редактировать нельзя — сначала «Отменить приход».
- **Дата накладной в «Рух товару».** В истории движений товара теперь показывается дата накладной (если задана), дата проведения — в подсказке при наведении.
- **Фикс.** Убрали автооткрытие карточки товара при перезагрузке (адрес очищается после открытия).

**Где в системе:** CRM → Склад → «Прибуткові накладні» → открыть черновик → «Редактировать».

## 07.08.2026 — Инвентаризация: общий черновик «Факт» (все видят одно) + кнопка «Обновить»

**Что попросили:** во время совместной инвентаризации Олег и кладовщик видели РАЗНЫЕ данные (введённый «Факт» хранился в браузере у каждого свой). И чтобы во время инвентаризации подтягивались новые продажи.

**Что сделали:**
- **Общий черновик на сервере.** Введённые значения «Факт» теперь хранятся на сервере (не в браузере). Все, кто открыл инвентаризацию, видят ОДНИ и те же цифры; список обновляется каждые 8 секунд — видно, что вводит коллега. Кнопка «Очистить введённое» очищает у всех.
- **Кнопка «🔄 Обновить»** в ведомости — перечитать актуальные остатки и продажи (введённый «Факт» сохранится), если во время пересчёта прошли новые продажи.

**Где в системе:** CRM → Склад → Инвентаризация.

## 07.08.2026 - Права склада + «Обновить цены» из номенклатуры

**Права (доступ):**
- **Аналитика склада** — теперь видит только владелец. Право снято с отдела «Склад/Логістика» (было у Ілони и Інни).
- **Закупочная цена** и **приход** — как и было, видит/делает только владелец. Хочешь дать ответственному — выдай право «product.cost.view» (видеть закупку) и «warehouse.tab.receipts» (приход) в Ролях.
**Розничная цена из номенклатуры:**
- Перевыбор товара в сделке уже тянет свежую розничную цену (и закупку) из номенклатуры — на **неоплаченных** сделках.
- Добавлена кнопка **«🏷️ Обновить цены»** рядом с «Обновить закупку»: одним кликом подтягивает свежие розничные цены во все товары, сумма сделки пересчитывается. Только для **неоплаченных** сделок.
- На **оплаченных** сделках цена заблокирована (защита чека) — менять нельзя, иначе разойдётся с фискальным чеком.

## 07.08.2026 — Кнопка «Обновить закупку» в сделке (подтянуть себестоимость из номенклатуры)

**Что попросили:** менял закупочную цену товара в номенклатуре, а в оплаченной сделке она не подтягивалась (перевыбор товара ничего не менял).

**Почему так было:** после оплаты/чека сделка «заперта» — товары менять нельзя, иначе разойдётся фискальный чек. Это правильно для цены/количества.

**Что сделали:** в сделке (блок «Товари», у кого есть право видеть закупку) появилась кнопка **«🔄 Обновить закупку»**. Она подтягивает **свежую закупочную цену из номенклатуры** во все товары сделки и пересчитывает маржу. **Сумма клиента и фискальный чек НЕ меняются** — только внутренняя себестоимость. Заодно синхронизируется себестоимость в реализации (складской учёт).

**Где в системе:** карточка сделки → «Товари» → кнопка «🔄 Обновить закупку».

## 07.08.2026 — Приходные накладные: можно отменить или заново провести

**Что попросили:** возможность перепроводить приходные накладные или отменять (как у реализаций).

**Что сделали:** в приходной накладной (Склад → Прибуткові накладні → открыть документ) появились кнопки:
- **«Провести приход»** — если накладная в статусе «Черновик» (товар встанет на остаток);
- **«Отменить приход»** — если проведена (товар снимется с остатка, для исправления ошибочного прихода).

Право — у ответственного за склад или бухгалтера (как у реализаций). Проверено: отмена снимает товар с остатка, повторное проведение возвращает.

**Где в системе:** CRM → Склад → «Прибуткові накладні» → клик по накладной → кнопки «Отменить/Провести приход».

## 07.08.2026 — Инвентаризация: недельная проверка, переход в сделки, списание на «ТТН створена»

**Что попросили:** недельная проверка проданного (период сб→пт); переход из товара в сделки, которые уехали с ним; списание товара со склада на стадии «ТТН створена».

**Что сделали:**
- Режим ведомости **«Продано за период»** — показывает только то, что продавалось/отгружалось за неделю (быстрая проверка недостач). Период сб→пт, кнопка «Эта неделя».
- В колонке **«Продано»** число кликабельно (**«−N ↗ сделки»**) → список сделок с этим товаром за период; каждая сделка кликабельна → переход в карточку.
- **Списание перенесено на «ТТН створена»**: когда создана ТТН (посылка уехала со склада) — товар списывается и себестоимость идёт в расход. Защита от двойного списания сохранена (на «Успішній» повторно не спишет). Резерв и логика дозаказа Codex сохранены.
- Плюс: скрытие себест./суммы в истории инвентаризации по правам.

**Где в системе:** CRM → Склад → Инвентаризация → «Продано за период» → клик по числу в «Продано».

## 07.08.2026 - Наложка одной посылки точно делится между дозаказами

**Вопрос:** одна ТТН, у основной и дозаказов свои суммы, наложка общая — как деньги распределятся?
**Как это работает:**
1. При создании ТТН каждая сделка (основная и дозаказ) хранит СВОЮ часть наложки (свой остаток). Физическая ТТН несёт сумму.
2. Клиент оплатил на почте → система для КАЖДОЙ сделки отдельно пишет её часть и выбивает ЕЙ свой чек (основная — свою сумму, дозаказ — свою). Не одной кучей.
3. Деньги от НоваПей приходят одной выплатой → банк-матчер по номеру ТТН узнаёт группу наложек этой посылки и помечает «оплачено» ИМЕННО их, каждой свою сумму (а не жадно по сумме к чужим). external_id платежа = ТТН.
**Итог:** каждый дозаказ получает свою сумму и фискально (чек), и деньгами (оплачено). Ничего не сваливается на одну сделку.

## 07.08.2026 - Склад: дозаказ = подзадача внутри основной (одна задача, одна коробка, одна ТТН)

**Поправка процесса:** ТТН создаёт СКЛАД (не менеджер).
- Дозаказы больше **не висят отдельными карточками** в очереди склада — они показаны **подзадачами внутри основной задачи** (видны их товары и наборы).
- Складовщик **берёт одну задачу** — дозаказы автоматически закрепляются за ним же. Собирает **одну коробку**, создаёт **ОДНУ ТТН** (в задаче, блок «📦 Одна посылка»).
- При «Готово»: списываются товары всех сделок посылки; **упаковка платится один раз** (не задваивается ЗП), вес и тонировка — по каждой.
- Значок «📦 одна посылка» и подсказка видны на карточке и внутри задачи.

## 07.08.2026 - Склад видит «одна посылка» (дозаказ) + где создавать ТТН

- У дозаказа своя задача на отгрузку (товары тонируются/пакуются/списываются отдельно — правильно для учёта).
- Теперь на КАЖДОЙ задаче склада (основной и дозаказа) виден значок **«📦 одна посылка»** и в карточке задачи баннер: с чем паковать вместе и что **ТТН создаёт менеджер на основной сделке** (№ показан).
- Менеджеру: ТТН делать на ОСНОВНОЙ сделке (в блоке «📦 Одна посылка» отметить дозаказы). Складовщик пакует всё в один ящик.

## 07.08.2026 - Дозаказ (допродажа) реально едет ОДНОЙ посылкой

**Зачем:** клиент оплатил сделку и хочет добавить товар. Оплаченную сделку менять нельзя — делаем дозаказ (связанная сделка), и теперь он реально уезжает ОДНОЙ ТТН с основной.
**Как работает:**
- Кнопка **«➕ Дозамовлення»** создаёт связанную сделку того же клиента (было и раньше — но ехало отдельно).
- Теперь при создании ТТН на основной сделке (вкладка «Нова Пошта») снизу блок **«📦 Одна посылка»** — галочками отмечаешь дозаказы, что кладёшь в эту коробку.
- **Одна ТТН** на всё. **Контроль оплати** (наложка) считается сам = остаток по основной + остатки по отмеченным дозаказам.
- **Каждая сделка получает свой чек** за свои товары (фискально раздельно, без задвоения).
**Сценарии:** дозаказ оплачен сразу → наложки нет; дозаказ наложкой → наложка = его остаток; всё наложкой → наложка = сумма, на почте выбиваются 2 чека.
**Не ломает старое:** если дозаказов нет — всё как было. Полная пошаговая инструкция с примерами — в CRM на странице **«Що нового»**.

## 06.08.2026 - Нова Пошта: «Контроль оплати» ставится сам и закреплён за менеджером

**Зачем:** чтобы не терять деньги, если менеджер забыл включить контроль оплаты, и чтобы складовщик случайно его не снял.
- **Автоподстановка.** Если тип оплаты — «Передоплата + післяплата НП» или наложка, «Контроль оплати» в форме НП **включается сам**, а сумма подставляется **остатком** (сумма сделки минус уже оплачено). Менеджер может поправить.
- **Только менеджер.** В складском окне поле «Контроль оплати» **заблокировано** (склад видит сумму, но не меняет и не снимает — рядом значок 🔒 «тільки менеджер»). Адрес, телефон, упаковку и параметры посылки складовщик редактирует как раньше.
- **Двойная защита.** Даже если контроль оплаты не передали, при создании ТТН система сама подставит остаток для наложки/предоплаты-НП.
- **Чек клиенту.** При реальной оплате наложки на почте чек создаётся и уходит клиенту (сообщение с чеком).
**Где:** карточка сделки → «Нова Пошта» (менеджер); Склад → задача → «Нова Пошта» (складовщик, поле контроля закрыто).

## 06.08.2026 - Checkbox: закрыта зависшая сделка 65502

**Что было:** аванс выбит на 3117,50, потом сумму подняли на +230, наложка собрала 2722,50 — аванс не закрывался.
**Что сделали:** аванс закрыт на его остаток 2492,50, а доплата 230 проведена отдельным чеком. Итого фискально 625 + 2492,50 + 230 = 3347,50 (= сумма сделки). Клиенту ничего не отправляли. На будущее сумму после чека менять уже нельзя (см. запись выше про фискальный замок).

## 06.08.2026 - Отгрузка в сделке: блок справа, фото открываются увеличенно

- Блок **«📦 Відвантаження»** перенесён в **правую колонку**, ниже «Історія накладних» и «Поля картки» — там удобнее смотреть фото после отгрузки.
- **Фото теперь открываются** (раньше были битые иконки): грузятся с авторизацией, клик по фото — **увеличение прямо в этом же окне**, клик по фону — закрыть.
**Где:** карточка сделки, правая колонка.

## 06.08.2026 - Склад/Відвантаження: убрали лишнюю вкладку, компактный фильтр отгрузок

- Убрали отдельную вкладку «Історія» (не нагружаем меню).
- В очереди склада — компактный сворачиваемый блок **«🔎 Відвантаження — пошук і період»**: фильтр за **любой период**, по **всем сотрудникам или конкретному**, и **поиск по № сделки / клиенту**. Клик по строке открывает задачу (только просмотр).
**Где:** Склад → очередь (видно руководителю).

## 06.08.2026 - Защита фискального чека: сумму сделки нельзя менять после чека/оплаты

**Проблема (сделка 65502):** авансовый чек в Checkbox выбит на сумму 3117,50, потом товар в сделке отредактировали (+230 → 3347,50), наложка собрала 2722,50 — и Checkbox не дал закрыть аванс («решта 230»).
**Что сделали:** после того как по сделке появился **чек Checkbox или оплата**, сумму товаров менять нельзя — **не только добавить** (было раньше), но и **отредактировать/удалить** позицию. Раньше замок был только на добавление, поэтому сумму можно было раздуть редактированием. Нужно больше — **Дозамовлення** (новая связанная сделка).
- **Наложка**: когда клиент реально оплатил на почте (НП шлёт сигнал) — чек создаётся **и отправляется клиенту** (как и было). Без отправки — только при ручном дозакрытии старых хвостов.
**Где:** карточка сделки → Товары; авто по наложке.

## 02.08.2026 - Отгрузка: блок в сделке (read-only с фото) + история с поиском

- **В карточке сделки** появился блок **«📦 Відвантаження»** — статус задачи склада, кто взял, даты, и **фото, которые прикрепил складовщик**. Только просмотр — ответственный видит историю, но ничего не меняет.
- **В разделе Склад/Відвантаження** новый вид **«🔎 Історія»**: фильтр за период (даты) + поиск по **№ сделки / клиенту / ТТН**. Клик по строке открывает задачу.
**Где:** карточка сделки (левая колонка); Склад → «Історія».

## 02.08.2026 - Защита денег: предупреждение о неоплате, запрет товаров после оплаты, дозаказ

- **Предупреждение** при ручном переносе неоплаченной сделки на «Оплату отримано»: показывает сколько оплачено из суммы, тип оплаты и что компания может потерять деньги — нужно подтвердить. Для предоплаты/наложки не мешает.
- **Запрет добавлять товары** в сделку, где уже есть оплата (чтобы не было пересчётов после оплаты). Для дозаказа — новая сделка.
- **Дозаказ (одна посылка):** кнопка в меню сделки создаёт связанную сделку того же клиента (едет одной посылкой). Меню сделки → «Дозамовлення».
- **Доплата к остатку** теперь сама привязывается к частично оплаченной сделке (банк-платёж по ФИО в любом порядке; тёзок/однофамильцев не путает).

## 02.08.2026 - Ссылка на оплату: фиксированная сумма, старые ссылки блокируются

**Проблема:** клиент мог оплатить старую ссылку с меньшей суммой (когда сумму сделки меняли, старая ссылка оставалась рабочей).
**Что сделали:** теперь работает **только последняя** ссылка на оплату по сделке. Если сумму обновили и ушла новая ссылка — старая показывает «Посилання застаріло, попросіть актуальне». Сумма на самой ссылке LiqPay и так фиксированная (клиент не может её изменить). Итог: клиент оплатит ровно текущую сумму.
**Где:** страница оплаты (ссылка /p/...).

## 02.08.2026 - Чек Checkbox: аванс/финал по ТИПУ оплаты, а не по сумме

**Проблема (сделка 65946):** в CRM полная оплата, а в Checkbox висит «Часткова оплата». Причина: сумму сделки подняли (ошибочная вторая ссылка 1935), клиент оплатил 1575, чек ушёл как «аванс» — потому что система сама решала «аванс/финал» по сравнению сумм.
**Что сделали:** чек теперь ориентируется на **тип оплаты, который выбрал менеджер** (Полная / Предоплата / Наложка), а не на сумму. Полная (или не указано) → сразу финальный чек; Предоплата/Бронь/Наложка → аванс, пока не покрыто.
**Где:** авто, при приёме/поступлении оплаты.

## 02.08.2026 - Поиск по номеру Checkbox в верхнем поиске + фикс окна фильтра

- **Верхний поиск «Пошук по CRM»** теперь находит сделку по номеру заказа Checkbox (и по ТТН) — по всем воронкам. Доска Канбан ищет только внутри своей воронки, поэтому для номера заказа удобнее верхний поиск.
- **Фикс:** окошко фильтра на доске сделок больше не прячется за колонками — выпадает поверх всего.
**Где:** верхний поиск (сверху справа) — вбей номер из Checkbox.

## 02.08.2026 - Поиск сделки по номеру заказа Checkbox

**Что попросили:** находить сделку по номеру заказа, который показан в Checkbox.
**Что сделали:** этот номер (напр. 17855909023095) теперь можно вбить в **поиск сделок** — откроется нужная сделка. Также он показан на карточке сделки в блоке чека («№ заказа Checkbox»), можно скопировать. Искать можно и по номеру ТТН Новой Почты.
**Где:** поиск сделок (сверху) + карточка сделки → строка «№ заказа Checkbox».

## 02.08.2026 - Наложки НП: чек в Checkbox теперь всегда, авто-закрытие

**Проблема:** при получении наложки на НП чек в Checkbox не всегда выбивался — сделка висела «частково оплачена». Причина: сумму наложки читали только из одного поля, а у части сделок она в другом месте (или только в НП).
**Что сделали:**
- Поллер НП теперь берёт сумму наложки **напрямую из Новой Почты** (по факту оплаты) — чек выбивается для любых сделок.
- Добавлен режим «выбить чек без отправки клиенту» (для дозакрытия старых сделок).
- Разово дозакрыли 4 застрявшие сделки (65947, 65535, 65499, 65145) — чек зарегистрирован, клиентам НЕ отправляли. Сделка 65502 — рассинхрон сумм в Checkbox, на ручную проверку.
**Где:** авто, при получении наложки. Финансы → сделка → чек Checkbox.

## 02.08.2026 - ЗП/KPI: клик по «N сделок» → детализация по каким сделкам считались оплаты

**Что попросили:** в ЗП/KPI при клике на сотрудника видеть, по каким сделкам считались его оплаты за выбранный период.
**Что сделали:** в Финансы → «ЗП/KPI» у каждого менеджера цифра **«N сделок» стала кликабельной** (синяя ссылка). Клик открывает окно со списком всех сделок, по которым в выбранном месяце поступили деньги: номер сделки, клиент, сколько поступило, дата. Итог окна совпадает с выручкой в карточке. Клик по сделке — открывает её карточку.
**Где:** Финансы → вкладка «ЗП/KPI» → клик по «N сделок» у сотрудника.

## 02.08.2026 - Новая вкладка «💵 Деньги» (Cash flow, как в Finmap)

**Что попросили:** отчёт «Деньги» как в Finmap — сколько пришло за период, с группировкой и журналом в одном экране.
**Что сделали:** в Финансах новая вкладка **«💵 Деньги»** (группа «Обзор»). В ней:
- Период + рядом переключатели: группировка **Категории / Контрагенты / Проекты**, разрез **Месяцы / Кварталы**, вид **Графики / Список**.
- Сводка: Поступления / Списания / Сальдо за период.
- График движения денег (доход зелёным, расход чёрным) по месяцам или кварталам.
- Разбивка поступлений и списаний по выбранной группировке (полосы).
- **Журнал операций прямо тут** — клик по строке открывает операцию.
- Кнопка **«Список»** — та же разбивка таблицей.
**Где:** Финансы → вкладка «Деньги».

## 06.08.2026 - Задачи: множественный выбор в каждом фильтре

**Что попросили:** чтобы в каждом фильтре можно было выбрать сразу несколько значений.

**Что сделали:** в окне фильтров все списки стали с галочками (можно отметить несколько):
- **Сотрудники** — отметить сразу нескольких + быстрые кнопки «Тільки мої» / «Всі».
- **Статус** — несколько статусов сразу + пресеты «Активні» / «Закриті» / «Всі».
- **Тип** и **Приоритет** — тоже по нескольку.
- **Срок** — как было (диапазон дат).

Каждое выбранное значение показывается отдельным чипом с крестиком — можно убрать по одному. Выбор сохраняется между заходами.

**Где в системе:** раздел **«Задачі»** → кнопка «Фільтри».

## 06.08.2026 - Задачи: одно окно фильтров + чипы + активные/закрытые

**Что попросили:** сделать фильтры одним окошком (нажал — всплыло окно, выбрал фильтры), рядом кратко показывать выбранные фильтры и убирать любой крестиком. Выбор сотрудника убрать из шапки (когда сотрудников много — неудобно). Плюс удобно отделять актуальные и закрытые задачи (готовых со временем накопится много).

**Что сделали:**
- **Одна кнопка «Фільтри»** — по клику всплывает окно со всеми фильтрами: сотрудник, статус, тип, приоритет, срок с/по.
- **Чипы выбранного** рядом с кнопкой — видно, что выбрано, у каждого крестик ✕ чтобы убрать этот фильтр. Есть «Скинути все».
- **Выбор сотрудника переехал** из вкладок в шапке — теперь это выпадающий список внутри окна фильтров (Мои / Все / конкретный сотрудник). Больше не разъезжается когда сотрудников много.
- **Статус с удобными пресетами:** «Активні (не закриті)» — по умолчанию, «Закриті (готово/скасовані)», «Всі», плюс конкретные статусы. Так завершённые задачи не мешают: по умолчанию видно только актуальные, а закрытые — по выбору. На доске при «Активні» колонка «Готово» прячется.

**Где в системе:** раздел **«Задачі»** — кнопка «Фільтри» и чипы под переключателем «Дошка/Список».

## 06.08.2026 - Задачи: убрали автозадачи агента, добавили список и фильтры

**Что попросили:** первая колонка задач была завалена — тысячи задач появлялись сами. Убрать автоматические задачи от ИИ-агента, оставить только складские (которые ставятся когда сделка доходит до «Оплату отримано») и те, что менеджеры завели вручную. Плюс — сделать вид задач списком и добавить фильтры (по дате, по сотруднику и др.).

**Что сделали:**
- **ИИ-агент больше не плодит задачи.** Раньше он на каждый ответ клиента ставил «дожим/менеджеру/тонирование» — за неделю набегало 700+ дожимов, никто их не закрывал, и вся просрочка копилась в колонке «На сьогодні». Теперь агент задачи НЕ создаёт.
- **Складские задачи работают как раньше** — когда сделка переходит на «Оплату отримано», задача «Відвантажити…» ставится автоматически (это отдельный механизм, его не трогали).
- **Разово почистили** 1601 старую агентскую задачу (дожим/менеджер/тонирование). Складские и ручные не тронуты — проверено. Удалённые сохранены в резервный файл на сервере, при надобности вернём.
- **Новый вид «Список»** — вверху страницы переключатель «Дошка / Список». В списке задачи таблицей: задача, тип, исполнитель, срок, статус, кнопка «выполнено». С постраничностью.
- **Панель фильтров** (работает и на доске, и в списке): тип задачи, статус, приоритет, срок с/по (даты). Выбор сотрудника — как и раньше, вкладками сверху.

**Где в системе:** раздел **«Задачі»** — переключатель вида и строка фильтров под вкладками сотрудников.

## 02.08.2026 - Направления: строка «(Без напрямку)» — итог сходится с P&L

**Что было:** в «Напрямках» факт-доход показывал только деньги, привязанные к направлению; платежи без направления (например, сырой входящий из банка) висели в общей выручке P&L, но не в направлениях → цифры не сходились (напр. 13504 vs 19151).
**Что сделали:** добавили строку **«(Без напрямку)»** — собирает доходы/расходы без направления. Теперь итог направлений = выручке P&L. По строке можно кликнуть → откроется журнал таких операций, чтобы раскидать их по направлениям.
**Где:** Финансы → Напрямки (проекти).

## 27.07.2026 - Три точечных фикса финансов (B, C, E)

- **B. Фильтр Дт/Кт по категории** теперь ищет по самой категории (по ID), а не по названию. Раньше две категории с одинаковым названием (напр. одна в доходах, другая в расходах) путались - теперь нет.
- **C. Наложка Новой Почты → платёж:** если под сумму выплаты подходит СРАЗУ несколько наложек (неоднозначно) - система больше не гадает и не отмечает случайную как оплаченную, а оставляет менеджеру. Обычный случай (одна наложка = одна выплата, или пачка = сумма) работает как раньше.
- **E. Массовое редактирование** операций в журнале стало «всё или ничего»: если на середине что-то не так - откатываются ВСЕ изменения, а не половина.

**Где в системе:** Финансы → журнал (массовое редактирование, фильтр Дт/Кт); банк-импорт наложек НП.

## 27.07.2026 - Инструкции «Що нового»: все финансовые собраны в раздел «Финансы»

**Что попросили:** проверить пошаговые инструкции по сегментам, объединить дубли категорий, чтобы все финансы были в финансах.
**Что сделали:** 10 финансовых инструкций (приём оплаты, дебиторка, кредиторка, аванс, банки, чек, фонды, закрытие периода, разноска платежа, деление между направлениями) висели без раздела - теперь все под **«Финансы»**. Инструкцию про поиск дублей отнесли к **«Клиенты»**. Пустых разделов не осталось. Тексты у всех полные, пошаговые - добавлять было нечего.
**Где в системе:** страница **«Що нового»** - записи сгруппированы по разделам.

## 27.07.2026 - Аудит финансов агентами: исправлены безопасные баги

**Что сделали (после проверки 2 агентами):**
- Закрыли дыру в правах: изменить статью финмодели (проценты фондов, ставки) можно было в обход права «редактировать финмодель» - теперь проверка на месте.
- Защита от зацикливания дерева категорий в разрезах (не упадёт с 500, если категория окажется сама себе родителем).
- Фильтр журнала по клиенту не падает на нечисловом значении.
- Сумма выбранных операций в журнале больше не считает переводы между счетами как доход.

**Остальное** (единый вид/названия фондов, потеря впечатанной категории, консистентность отчётов) - в списке на решение владельца.

## 27.07.2026 - Разрез «По категоріях» теперь иерархичный (как в Довідниках)

**Что попросили:** чтобы категории в аналитике выглядели зеркально Довідникам - родитель и дети с отступом.

**Что сделали:** разрез Аналітика -> Напрямки -> По категоріях теперь показывает категории деревом: родительская категория, под ней дети с отступом (↳). Каждая со своим доходом/расходом/прибылью.

## 27.07.2026 - Поднаправления (иерархия направлений) + клик по операции

**Что попросили:** при разворачивании направления показывать поднаправления, которые к нему относятся; заходить в любую транзакцию; чтобы мирроринг с общим журналом.

**Что сделали:**
- **Иерархия направлений:** у направления появилось поле «Родительское направление» (в окне добавления/редактирования). Назначаешь направлению родителя - оно становится поднаправлением.
- **При разворачивании родителя** под ним показываются строки-поднаправления со своими суммами (доход/расход/прибыль), каждое разворачивается в свой журнал операций. Ниже - операции самого родителя. Сумма родителя = его операции + все поднаправления (rollup).
- **Клик по операции** в журнале направления открывает её карточку (редактирование) - как в общем журнале.

**Где в системе:** Аналітика -> Напрямки. Чтобы сделать поднаправление: карандаш у направления -> «Родительское направление» -> выбери родителя.

## 31.07.2026 — Задачи: право «Бачити задачі інших співробітників»

**Что попросили:** сделать так, чтобы кнопки-фильтры «Всі», «Кирил», «Ілона», «Валентин» видел только тот, кому это разрешено. Обычный менеджер должен видеть только свои задачи, руководитель — все.

**Что сделали:**
- **Новое право «task.view.others»** — «Бачити задачі інших співробітників».
- **По умолчанию у всех выключено.** Обычный сотрудник видит только кнопку «Мої» и в канбане — только свои задачи. Даже если через URL или localStorage попробует посмотреть чужие — сервер вернёт пусто.
- **Кому дать право:** налаштування → Співробітники і права → відкрити роль (напр. «Керівник», «РОП», «Адміністратор») → група «Задачі» → включити «Бачити задачі інших співробітників». Или дать индивидуально сотруднику.
- **Кому уже даётся автоматически:** суперадмин (Олег) и все с правом «roles.manage» — они всегда видят задачи всех.
- **В канбане:** если у меня нет права — не показываются кнопки «Всі» и другие сотрудники. Только «Мої».
- **Красный кружок в меню** тоже респектит право — если без права, показывает только свои.

**Где в системе:**
- Право включается: «Співробітники і права» → «Ролі» → нужна роль → блок «Задачі».
- Проверить у себя: «📋 Задачі» → сверху видны кнопки только с теми, кого разрешено видеть.

## 27.07.2026 - Разрезы перенесены ВНУТРЬ вкладки Направления

**Что попросили:** разрезы (категории/контрагенты/месяцы/матрица) сделать не отдельной вкладкой, а внутри Направлений.

**Что сделали:** убрали отдельную вкладку Розрізи. В Аналітика -> Напрямки вверху появились кнопки-переключатели: **Напрямки | По категоріях | По контрагентах | По місяцях | Матриця**. Период общий для всех видов. По умолчанию - Напрямки (как было).

## 27.07.2026 - Поиск контрагентов в справочниках

**Что попросили:** в Довідники -> Контрагенти сделать поиск.

**Что сделали:** добавили поле поиска - фильтрует список контрагентов по названию на лету + показывает «Найдено: N».

**Где в системе:** Фінанси -> Довідники -> Контрагенти.

## 27.07.2026 - Новая вкладка «Розрізи» в аналитике (4 разреза)

**Что попросили:** добавить в аналитику разрезы по категориям и контрагентам; предложить варианты.

**Что сделали:** новая вкладка **Аналітика -> Розрізи** с 4 видами (за выбранный период, переводы между счетами исключены):
- **По категориям** - статья -> доход / расход / прибыль / кол-во операций.
- **По контрагентам** - клиент/поставщик -> получено / оплачено / сальдо / кол-во. Топ по обороту.
- **По месяцам** - доход / расход / прибыль помесячно (тренд).
- **Матрица** - направление × месяц, чистыми (доход - расход) - сводный pivot.

Везде строка ИТОГО. Эндпоинт /api/finance/breakdown/?by=category|counterparty|month|matrix.

## 27.07.2026 - Направления: колонка расходов + журнал не зависает

**Что попросили:** в направлениях не подгружался журнал операций; расходы и доходы видеть отдельно.

**Что сделали:** добавили колонку **«Факт витрати»** (расходы) рядом с доходом и прибылью (+ в строке ИТОГО). Журнал операций направления теперь грузит до 1000 операций и не зависает при ошибке (раньше без обработки ошибки мог остаться на «Журнал...»).

**Где в системе:** Аналітика -> Напрямки (проекти).

## 31.07.2026 — Задачи: кнопки со всеми сотрудниками + запоминание выбора

**Что попросили:** «В задачах у меня должен быть выбор чьи задачи я могу видеть».

**Что сделали:**
- Сверху страницы «Задачі» теперь **ряд кнопок**: [Мої 17] [Всі 245] · [Кирилл 17] [Ілона 3] [Валентин 5] [Інна 5] ... по одной на каждого активного сотрудника.
- Цифра на кнопке — сколько у этого сотрудника задач НА СЕГОДНЯ (невыполненных).
- Клик на «Кирилл» → сразу видишь его канбан (все 4 колонки).
- **Выбор запоминается**: обновил страницу, вышел-зашёл в CRM — тот же вид, что оставил.
- **Красный кружок в меню** «📋 Задачі» тоже показывает выбранного сотрудника: смотришь на Кирилла — кружок 17, переключился на «Мої» — своё число.
- Сотрудники без задач на сегодня — блёкло-серые, чтобы не отвлекать.

**Где в системе:** «📋 Задачі» → верхняя панель с кнопками.

## 27.07.2026 - Сумма выбранных операций в журнале

**Что попросили:** когда выбираешь галочками строки в журнале - показать сумму выбранных.

**Что сделали:** в нижней панели «Вибрано: N» теперь рядом показывается **сумма выбранных** (с копейками, знак: доход +, расход -). Считается по отмеченным строкам на текущей странице.

**Где в системе:** Финансы -> Операции -> Журнал -> галочки -> нижняя панель.

## 31.07.2026 — Задачи: красный кружок в меню со счётчиком

**Что попросили:** возле пункта «Задачі» в меню показать красный кружок с количеством невыполненных задач на сегодня, чтобы сразу было видно сколько ещё висит.

**Что сделали:**
- Возле пункта **«📋 Задачі»** в левом меню теперь появляется **красный кружок с числом** — это количество твоих задач в колонке «На сьогодні» (кроме уже выполненных).
- Если задач на сегодня нет — кружок не показывается.
- Число обновляется автоматически каждую минуту (или сразу при перезагрузке страницы).
- Например: у Кирилла сейчас в меню будет «Задачі 🔴17».

**Где в системе:** левое меню, пункт «Задачі».

## 27.07.2026 - Фильтр журнала по клиенту находит платежи по имени (не только по привязке)

**Что заметил Олег:** выбрал в фильтре журнала клиента (Романенко Інна) - список пустой.

**Что нашли:** фильтр искал только операции, ЖЁСТКО привязанные к контакту (contact_id). А у клиента все платежи привязаны по ИМЕНИ (контрагент), а не жёсткой ссылкой - поэтому фильтр их не видел.

**Что сделали:** фильтр журнала по клиенту теперь находит операции и по жёсткой привязке, И по имени клиента в поле контрагента (независимо от порядка фамилия/имя). Пример: Романенко Інна теперь показывает 31 операцию (было 0).

**Где в системе:** Финансы -> Операции -> Журнал -> Фильтры -> Клиент.

## 31.07.2026 — Задачи: фильтр «Исполнитель» + кнопки создания задачи из сделки и чата

**Что попросили:** «У Кирилла в задачах пусто» — потому что каждый видит СВОИ задачи, а руководитель хочет посмотреть чужой список. Плюс — чтобы задачу можно было поставить прямо из окна чата (в том числе через контекстное меню на сообщении) и из карточки сделки, не переходя на страницу задач.

**Что сделали:**
- **Фильтр «Исполнитель»** сверху страницы «Задачі». Руководитель может выбрать любого сотрудника → видит его канбан. По умолчанию — «Мои».
- **Кнопка «+ Задача»** в шапке карточки сделки (рядом с Checkbox). Одним кликом ставит задачу на клиента этой сделки — тема сделки уже подставлена, вручную выбирать не нужно.
- **Кнопка «+ Задача»** в шапке чата (жёлтая, рядом с «Подзвонити»). И такой же пункт «+ Задача» в меню «⋯» чата. Клик — модалка с уже подставленным клиентом+чатом.
- **Технически:** новый endpoint  (лёгкий список сотрудников для селекторов, доступ у всех вошедших — раньше был , поэтому селектор в модалке был пустой). Общий компонент  — один и тот же в трёх местах.
- Теперь **17 задач Кирилла на IG-проверку виднеются на странице «Задачі» → фильтр «Исполнитель: Кирилл»**.

**Где в системе:**
- Задачі → сверху выбрать «Мої / Всі» + «Виконавець».
- В сделке — панель с табами, кнопка «Задача».
- В чате — шапка (кнопка «Задача» жёлтая) или меню «⋯» → «+ Задача».

**Что ещё в планах:** контекстное меню на конкретном сообщении (правый клик → «Задача по этому сообщению» — с цитатой), drag-and-drop колонок канбана.

## 31.07.2026 — Инвентаризация: себестоимость и сумма в истории — только по правам

**Что попросили:** проводить инвентаризацию могут все с доступом, но столбцы «Себестоимость» и «Сумма» должны видеть только те, у кого право на себестоимость. Илоне — отключить.

**Что сделали:** в «Истории» инвентаризации (детали проведённой) столбцы **«Себест.» и «Сумма» теперь показываются только при праве «Видеть себестоимость» (product.cost.view)**. Раньше они показывались всем — это и была причина, что Илона их видела. Отдельно право менять не пришлось: у Илоны (роль Менеджер) права на себестоимость и так нет — теперь эти столбцы у неё исчезнут.

Сама инвентаризация и её проведение доступны как раньше — скрыты только денежные столбцы.

**Где в системе:** CRM → Склад → Инвентаризация → История → открыть проведённую.

## 31.07.2026 — Инвентаризация: введённый «Факт» больше не теряется при перезагрузке

**Что попросили:** при вводе данных в ведомость и перезагрузке страницы всё сбивалось.

**Что сделали:** введённые вручную значения «Факт» **автоматически сохраняются в браузере** — при перезагрузке/случайном закрытии они восстанавливаются. Над таблицей показывается «Введено значений: N — сохраняются автоматически» и кнопка «Очистить введённое». После проведения инвентаризации проведённые позиции из черновика убираются (остальные страницы сохраняются).

**Про остатки (ответ на вопрос):** да — после проведения остаток каждого товара становится равен внесённому «Факту». В следующей инвентаризации «Начальный» остаток будет уже правильным — тем, что вы внесли, плюс приходы/расходы за период.

**Где в системе:** CRM → Склад → Инвентаризация → ведомость.

## 31.07.2026 — Задачи: новый модуль с канбаном + автозадачи по 17 клиентам

**Что попросили:** сделать раздел «Задачи», куда сотрудники могут ставить задачи себе и друг другу, видеть в канбане что на сегодня, а также сразу поставить Кириллу задачи проверить клиентов, которым не дошло сообщение (только тем, кому реально не дошло — без дубляжа).

**Что сделали:**
- **Новый раздел «Задачи»** в левом меню (иконка галочка, между «Чаты» и «Контакт-центр»). Открывается по .
- **Канбан 4 колонки:** На сьогодні (красная) · На тиждень (оранжевая) · Пізніше (синяя) · Готово (зелёная). Задача попадает в колонку сама по дедлайну.
- **Кнопка «+ Задача»** сверху страницы: заголовок, исполнитель (любой сотрудник), дедлайн (дата+время), приоритет, обязательная привязка (клиент / сделка / лид / чат). Все всем могут ставить.
- **Кнопки быстрого переноса** в каждой карточке: «→ Сьогодні» и «✓» (готово).
- **Фильтр «Мои / Все»** сверху — по умолчанию «Мои» (моя работа на сегодня).
- **Приоритет 🔴 «Высокий»** подсвечивает карточку красной полосой слева.
- **Клик по задаче** — модалка редактирования (можно поменять всё или удалить).
- **Автоматически проверили ChatPlace** по всем 28 повисшим повідомленням: у 13 клиентов сообщения УЖЕ дошли (менеджер написал вручную с мобильного) — их не трогаем, чтобы не дублировать. Остальным **17 клиентам поставили задачу Кириллу** (высокий приоритет, дедлайн сегодня 18:00) с текстом сообщения и инструкцией: если уже написал — ставит ✓ и всё, если нет — копирует текст и шлёт из CRM.

**Где в системе:** левое меню → «📋 Задачі». Кирилл откроет — сразу увидит 17 карточек в колонке «На сьогодні».

**Ещё в планах (следующий шаг):** кнопка «+ Задача» прямо из карточки сделки и из окна чата, чтобы не переключаться на страницу задач.

## 31.07.2026 — Инвентаризация: поиск по товару в ведомости

**Что попросили:** искать товар прямо в ведомости инвентаризации.

**Что сделали:** над ведомостью добавлено поле «Поиск по товару / артикулу». Пишешь название или артикул — список сразу фильтруется (по названию и по артикулу). Работает вместе с выбором папки и периодом. Крестик — очистить.

**Где в системе:** CRM → Склад → Инвентаризация → строка поиска в шапке.

## 31.07.2026 — Instagram: почему сообщения из CRM переставали доходить до клиента

**Что попросили:** Кирилл написал, что при попытке отправить сообщение в Instagram из карточки клиента CRM выдаёт «HTTP Error 404: Not Found» — проверил на 4 клиентах, у всех одинаково.

**Что случилось (для истории):** ChatPlace (сервис, через который CRM ходит в Instagram Direct) сегодня посреди дня внезапно перенёс свою «точку входа» с адреса  на просто . Наш код продолжал стучаться по старому — получал 404. С 09:37 до 12:26 по Киеву **19 сообщений (17 клиентам)** записались в CRM, но до Instagram не дошли. Пока разбирались, ChatPlace ещё раз переключился обратно — то есть «то один адрес живой, то другой», и заранее угадать нельзя.

**Что сделали:**
- **CRM теперь сам находит живой адрес.** Если Instagram возвращает 404 — CRM автоматически пробует альтернативный адрес и запоминает его. Клиент не замечает, менеджер не замечает — сообщение уходит.
- Все следующие сообщения того же дня идут сразу по «живому» адресу без задержки.
- Отправка проверена: 0 фейлов за 5 минут после включения.

**Где в системе:** ничего в интерфейсе не меняется. Просто отправка в Instagram снова работает и не зависит от того, какой адрес у ChatPlace сегодня живой.

**Внимание:** 19 непринятых сообщений всё ещё «зависли» в CRM без пометки «отправлено». Если менеджер уже написал этим клиентам вручную с телефона — можно пропустить. Если нет — их надо будет отправить повторно (список ниже, спросить Олега).

## 31.07.2026 — Импорт инвентаризации: умное сопоставление товаров (стало «не найдено» в разы меньше)

**Проблема:** ИИ распознал больше 150 позиций, а система сопоставила только ~45 — остальные попадали в «не найдено». Причина: сопоставление требовало ТОЧНОГО совпадения названия/артикула, а из-за кавычек, лишних пробелов и мелких неточностей почерка точного совпадения не было.

**Что сделали:** сопоставление стало умным:
- артикул — точно, и с нормализацией (регистр, пробелы, дефисы);
- название — точно, а если нет — **нечёткое сравнение** (находит товар, даже если название записано чуть иначе: кавычки, регистр, лишние пробелы, мелкие опечатки).

Теперь распознанные позиции почти все находятся. Что реально не нашлось — по-прежнему показывается списком, эти строки правишь вручную.

**Совет:** для максимальной точности перепечатай ведомость заново — теперь в ней есть колонка «Артикул», и по нему сопоставление самое точное.

## 31.07.2026 — Фикс: распознавание многих фото падало с ошибкой 500

**Проблема:** при загрузке 14 фото «Распознать» выдавало Internal Server Error — сервер обрывал долгую обработку через 60 секунд.

**Что сделали:** увеличили допустимое время обработки запроса на сервере (gunicorn timeout 60с → 300с). Теперь распознавание пачки фото успевает завершиться.

## 31.07.2026 — Импорт инвентаризации: больше фото (до 24), прокрутка и постраничный просмотр

**Что попросили:** грузилось только 8 фото — сделать больше, с прокруткой в большом списке; и сразу выбор количества отображаемых строк.

**Что сделали:**
- **Больше фото.** За один раз — до 20 фото (миниатюры листов прокручиваются вбок). Можно грузить **пачками**: распознал одну пачку → добавил ещё → распознал — результаты **накапливаются** в общем списке (дубли по товару обновляются). Итого лист за листом — хоть весь склад.
- **Постраничный просмотр.** В таблице распознанных позиций теперь **выбор количества строк** (5/20/50/100/500) и перелистывание страниц — большой список удобно прокрутить и увидеть всё.
- Внутри система обрабатывает фото группами (по 5) — чтобы много листов не упирались в ограничения ИИ.

**Где в системе:** CRM → Склад → Инвентаризация → «Импорт из файла» → «Добавить фото листов».

## 31.07.2026 — Инвентаризация: артикул в печати + загрузка ФОТО листов (ИИ распознаёт факт)

**Что попросили:** печатать сразу с артикулом; и не слать фото вручную, а загружать фото прямо в инвентаризацию — чтобы ИИ распознал рукописный «Факт» и сам заполнил, а ответственный поправил позиции.

**Что сделали:**
- **Артикул в распечатке.** В печатной ведомости добавлена колонка «Артикул» — удобнее считать и потом сверять.
- **Загрузка фото.** В окне «Импорт» теперь можно **добавить фото** распечатанных листов (несколько, прямо с телефона). Нажимаешь «Распознать фото (ИИ)» — искусственный интеллект читает рукописные цифры в колонке «Факт» и заполняет таблицу.
- **Редактируемое превью.** Распознанные строки показываются таблицей, где **можно поправить «Факт»** у любой позиции или **убрать строку** — прежде чем провести. Что не распозналось/не нашлось — показано отдельно.
- Как и раньше, работает файл Excel/CSV. После проверки жмёшь «Провести» — инвентаризация создаётся только по расхождениям.

**Важно:** ИИ может ошибиться в почерке — поэтому обязательно **проверяешь глазами** перед проведением. Ответственный правит позиции прямо в окне.

**Где в системе:** CRM → Склад → Инвентаризация → «Импорт из файла» → «Добавить фото листов».

## 31.07.2026 — Инвентаризация: импорт факта из файла (Excel/CSV) без ручного ввода

**Что попросили:** распечатали товары, посчитали на складе, вписали факт руками — не хочется вбивать эти цифры вручную в CRM. Хотим загрузить файлом.

**Что сделали:** в инвентаризации кнопка **«Импорт из файла»**. Загружаешь Excel (.xlsx) или CSV с колонками **Артикул + Факт** (можно 3-ю «Название» — для своей проверки). Система:
1. показывает **превью**: что нашла, учётный остаток, твой факт, расхождение, и что НЕ нашла (проверяешь перед проведением);
2. по кнопке **«Провести»** создаёт инвентаризацию только по товарам с расхождением — вручную ничего не вносишь.

Матч по артикулу (точно), если пусто — по точному названию. Ненайденные товары показываются списком — их правишь в файле и грузишь заново.

**Где в системе:** CRM → Склад → Инвентаризация → «Импорт из файла».

## 31.07.2026 — Инвентаризация: фильтр «Без категории (корень)»

**Что попросили:** в фильтре инвентаризации видеть не только по папкам, но и отдельно корневые товары.

**Что сделали:** в «По папке» добавлен пункт **«Без категории (корень)»** — показывает товары (40 шт.), которые не разложены ни в одну папку. Раньше их отдельно было не увидеть.

**Где в системе:** CRM → Склад → Инвентаризация → «По папке» → «Без категории (корень)».

## 25.07.2026 — Контроль простоя: свой порог для отдельного сотрудника

**Что попросили:** чтобы в настройках контроля простоя можно было задать порог не только по отделу, но и по каждому менеджеру отдельно.

**Что сделали:** в «Сотрудники → Активность → ⏱ Контроль простоя» под списком отделов появился блок «Персонально по сотруднику». У каждого — своё поле минут:
- **пусто** — берётся значение отдела (как раньше);
- **число** — личный порог, важнее отдела (напр. отделу 15 мин, а конкретному человеку 30);
- **0** — выключить контроль лично для этого сотрудника.

Личная настройка всегда важнее настройки отдела. Работает и для окна «Вы на месте?», и для авто-паузы на сервере.

**Где в системе:** CRM → «Сотрудники» → «Активность» → «⏱ Контроль простоя» → раздел «Персонально по сотруднику».

## 27.07.2026 — Банковский платёж цепляется к клиенту по фамилии (даже без номера сделки)

**Что попросили:** если клиент платит по IBAN и не указывает номер сделки, платёж «висит ничей». Нужно матчить по фамилии, независимо от регистра и порядка слов.

**Что сделали:** при импорте банка входящий платёж теперь пробует сопоставить контрагента (напр. «ДОБРОВОЛЬСЬКА НАТАЛІЯ ОЛЕК») с клиентом по фамилии (точное совпадение) + имени (по первым буквам) — регистр и порядок неважны. Если клиент нашёлся — платёж привязывается к его карточке. А если у клиента ровно одна открытая сделка на близкую сумму (±3%) — платёж сам привязывается к сделке, создаёт оплату и двигает стадию.

**Безопасность:** если совпадений несколько (например несколько однофамильцев-дублей) — матчер НЕ гадает, оставляет для ручной привязки. Так не привяжет к чужому.

**Где в системе:** импорт ПриватБанка (авто, крон) — работает для новых платежей.

## 31.07.2026 — Аналитика склада: инвентаризация (излишки и недостачи) отдельным блоком

**Что попросили:** видеть инвентаризацию в аналитике — чтобы расхождения (недостачи и излишки) были на виду.

**Что сделали:** в разделе Аналитика → Склад появился блок **«Инвентаризация за 90 дней»**:
- **Излишки** (что оприходовали) — в штуках и в деньгах (зелёным);
- **Недостачи** (что списали) — в штуках и в деньгах (красным);
- **Итог** (излишки минус недостачи);
- **Таблица последних инвентаризаций** — дата, что за инвентаризация, сколько позиций, излишки/недостачи по каждой.

Всё по закупочной себестоимости. **Отменённые (сторно) инвентаризации не учитываются** — цифры честные.

**Где в системе:** CRM → Аналитика → Склад → блок «Инвентаризация за 90 дней» (виден тем, кто видит себестоимость).

## 27.07.2026 — В накладной видно «Призначення платежу» + почему это важно

**Что попросили:** клиенты иногда платят по IBAN из накладной, не указывая номер сделки — платёж не привязывается автоматически (пример: сделка #65947, оплата 8900 не села). Нужно, чтобы в накладной было чётко видно, что писать в назначении платежа и почему это важно.

**Что сделали:** в видаткову накладну (кнопка «Документ» в сделке) добавлен заметный блок **«Призначення платежу»** с точным текстом для копирования: «Оплата за декоративні матеріали згідно накладної №<номер>, без ПДВ» + пояснение, почему это важно (без него платёж не находится, заказ задерживается; с ним оплата видна сразу). Такой же текст теперь и в сообщении «Реквізити IBAN», которое шлём в чат.

**Заодно:** починил сделку #65947 — платёж 8900 привязал вручную, сделка перешла на «Оплату отримано».

**Где в системе:** сделка → «Документ» (накладна) — блок призначення; сделка → «Прийняти оплату → Реквізити IBAN» — сообщение клиенту.

## 31.07.2026 — Инвентаризация: отмена (сторно) без потери истории, право у ответственного, дата документа

**Что попросили:** чтобы историю инвентаризаций нельзя было случайно затереть (корректировки не удаляют то, что было); при этом уметь отменить ошибочную инвентаризацию — с правом только у ответственного; и уметь ставить дату (в т.ч. задним числом) для переходного периода и корректировок за другие месяцы.

**Что сделали:**
- **Отмена = сторно, а не удаление.** У проведённой инвентаризации в «Истории» появилась кнопка «Отменить». При отмене остаток возвращается к состоянию до этой инвентаризации, а сама запись **никуда не пропадает** — остаётся в истории с серой пометкой «Отменено». Полностью удалить инвентаризацию нельзя (защита от потери истории) — только отменить.
- **Право у ответственного.** Отменять инвентаризацию может только тот, кому выдано новое право **«Скасування (сторно) інвентаризації»** (Настройки → права, раздел «Склад»). Обычный менеджер кнопки «Отменить» не видит.
- **Статус в истории.** Каждая запись помечена «Проведено» (зелёным) или «Отменено» (серым).
- **Дата документа.** Инвентаризация теперь сохраняет дату (по концу выбранного периода) — в т.ч. можно поставить прошлую дату, чтобы скорректировать остатки за другой месяц. Дата видна в истории.

**Как это в учёте (коротко):** так и положено — задним числом ничего не затирается; ошибку исправляют отменой (сторно) с сохранением следа: кто, когда, что. Недостачи/излишки уже отражаются в аналитике склада («потери склада» в деньгах по себестоимости).

**Где в системе:** CRM → Склад → «Инвентаризация» → «История» → кнопка «Отменить» (видна ответственному). Право — Настройки → права сотрудника → «Склад».

## 31.07.2026 — Контроль простоя: «открыт CRM = на месте» + секунды в отчёте

**Что попросили:** у менеджеров, которые сидят в CRM (читают чат, говорят по телефону), не дожидаясь 15 минут выскакивало окно «Вы на месте?» и ставило на паузу — хотя человек за компьютером. Плюс в отчёте у коротких пауз (меньше минуты) начало и конец показывались одинаково.

**В чём была причина:** система определяла присутствие только по движению мыши и клавиатуры. Кто читает переписку или разговаривает по телефону, глядя в CRM, — мышь не двигает, и система ошибочно считала его «отошедшим» и ставила паузу сама (из браузера).

**Что сделали:**
1. Теперь присутствие считается по-простому: **пока вкладка CRM открыта и видна на экране — менеджер на месте**, паузу никто не ставит. Чтение чатов и разговоры по телефону с открытым CRM больше НЕ считаются простоем.
2. Пауза за простой ставится только когда менеджер реально ушёл с вкладки CRM (свернул браузер или переключился в другое приложение/вкладку) дольше порога своего отдела. Решение теперь принимает сервер — надёжно, без ложных срабатываний.
3. Когда человек возвращается на вкладку — CRM сразу это видит и предлагает снять паузу кнопкой «Продолжить работу».
4. Порог для отдела продаж оставили 10 минут (настраивается в Сотрудники → Контроль простоя).
5. В отчёте по сотруднику паузы теперь показываются **с секундами** (например 09:14:05–09:14:22) и с длительностью, а короткие паузы больше не выглядят одинаково.

**Где в системе:** Сотрудники → выбрать сотрудника → таблица смен, колонка «Паузы». Настройка порога — Сотрудники → кнопка «⏱ Контроль простоя».

## 31.07.2026 — Инвентаризация: история проведённых + понятное подтверждение при проведении

**Что попросили:** уметь смотреть историю инвентаризаций (сделали в этом месяце — в следующем зашёл и посмотрел, что было). И: после нажатия «Провести инвентаризацию» «ничего не происходило».

**Что сделали:**
- **История.** Вверху инвентаризации переключатель **«Ведомость / История»**. В «Истории» — список всех проведённых инвентаризаций: дата, период, сколько позиций, и сколько было недостач (красным) и излишков (зелёным). Клик по строке открывает подробности: по каким товарам было расхождение, на сколько штук и на какую сумму (по себестоимости).
- **Понятное проведение.** Раньше при «Провести» не было видимого ответа. Теперь: сначала спрашиваем подтверждение («скорректировать N позиций?»), потом показываем результат отдельным окном («готово, скорректировано N позиций»). Если ни один фактический остаток не вписан — прямо говорим, что вписать число в колонку «Факт». Если ошибка — показываем её, а не молчим.
- Каждая проводка сразу попадает в «Историю».
- **Без всплывающих окон браузера.** Подтверждение проведения — встроенное окно прямо на странице (со списком: было → стало → разница), результат — цветная плашка. Никаких системных pop-up.

**Как это работает (коротко):** «Провести» создаёт складской документ-инвентаризацию, который двигает остаток на разницу между фактом и учётом (недостача — в минус, излишек — в плюс, в деньгах по себестоимости). Эти документы и показываются в «Истории».

**Где в системе:** CRM → Склад → «Инвентаризация» → переключатель «Ведомость / История» сверху.

## 30.07.2026 — Оплата: если сумма сделки поменялась после оплаты — стадия сама пойдёт вперёд

**Что попросили:** разобраться, почему в сделке Людмилы Шокиной (#65946) клиент оплатил, чек Checkbox пришёл, но сделка так и висела на «Домовились про оплату» вместо «Оплату отримано». Из-за этого склад и тонирование не получили автоматические задачи вовремя.

**Что случилось (для истории):** менеджер сначала выслал ссылку на 1575 грн, потом добавил ещё товар и выслал вторую ссылку на 1935 грн. Клиент оплатил 1575 (по первой ссылке). В момент прихода оплаты система смотрела: «оплачено 1575, а в сделке уже 1935» → «оплата не полная, стадию не двигаем». Потом менеджер убрал лишний товар — сделка снова стала 1575, и оплата уже её покрывала, но код проверял это только в момент прихода денег. При редактировании позиций такой проверки не было.

**Что сделали:**
- **Автоматическая перепроверка после любой правки позиций.** Теперь если менеджер добавил или убрал товар, и после этого оплата уже покрывает новую сумму — сделка автоматически переходит на «Оплату отримано», как будто клиент только что оплатил. С этой стадии дальше уже включаются обычные задачи склада и тонирования.
- **Работает только для «застрявших» сделок.** Автопередвижка срабатывает только если сделка ещё на стадиях до оплаты (Данні / Розрахунок / Домовились). Если сделка уже выиграна, отгружена или отменена — ничего не трогается.
- **Ручной фикс задним числом.** Сделку #65946 вручную перевели на «Оплату отримано» — теперь склад и тонирование Velvet Luna Silver получат задачи как положено.

**Где в системе:** в карточке сделки → добавьте/удалите товар. Если новая сумма уже равна или меньше того, что клиент оплатил — стадия сама шагнёт вперёд. В истории сделки появится запись «recalc: сплачене (X) покриває суму сделки (Y→Z)».

## 30.07.2026 — Инвентаризация: виден весь склад, листание страниц, печать для отметок и выбор папки

**Что попросили:** в инвентаризации показывать весь список товаров с листанием страниц и выбором количества строк; уметь распечатать ведомость для отметок на физическом складе; уметь смотреть либо все товары общим списком, либо отдельной папкой.

**Что сделали:**
- **Виден весь склад.** Раньше в инвентаризацию попадали только те товары, что открыты на текущей странице раздела «Товары». Теперь инвентаризация сама показывает весь склад (все физические товары, которые учитываются по остатку) — независимо от вкладки «Товары».
- **Листание страниц.** Внизу ведомости появились кнопки «← Назад / Вперёд →» и номер страницы, а также выбор количества строк на странице: 5, 20, 50, 100, 500.
- **Печать для склада.** Кнопка «🖨 Печать» формирует ведомость всего списка (по текущему фильтру, а не только видимой страницы) с пустой колонкой «Факт (вписать)» и колонкой «Примечание» — распечатал, посчитал руками на складе, потом внёс. Внизу листа — строки для подписи.
- **Все товары или отдельная папка.** Вверху инвентаризации переключатель «Все товары / По папке». При выборе папки берётся папка и все её подпапки. Рядом видно, сколько всего позиций.
- Введённые вручную «фактические» остатки больше не сбрасываются при переходе между страницами.

**Где в системе:** CRM → Склад → вкладка «Инвентаризация» (переключатель отображения и «Печать» — сверху, листание и выбор строк — снизу).

## 28.07.2026 — Единый вход, клиентский кабинет и проверки этапов ремонта

**Что попросили:** убрать отдельный «вход для сотрудников», сделать регистрацию понятной для клиента и не позволять пользователю самому выбирать права. Обычному клиенту нужен простой путь ремонта, а сотрудникам — работа только с назначенными клиентами и проверка сложных этапов.

**Что сделали:**
- в приложении теперь один вход и обычная регистрация; каждый новый аккаунт создаётся как клиент без служебных прав;
- сотрудником или администратором аккаунт становится только после назначения ответственным администратором в CRM;
- клиентский кабинет получает только собственный контакт, свои проекты замера и свои черновики расчётов — чужие клиенты, сделки и внутренние разделы CRM ему недоступны;
- в приложении появился простой маршрут «Мой ремонт»: анкета, замер, предварительный расчёт, нужные этапы и подсказки по выполнению;
- рискованные этапы клиент отправляет на проверку, а принять результат или вернуть на доработку может только ответственный сотрудник с правом управления ролями;
- в разделе сотрудников добавлена очередь клиентских регистраций и блок «Проверки этапов ремонта» с именем клиента, проектом, этапом и комментарием.

**Где в системе:** приложение Wallcov Замер → «Войти» / «Мой ремонт»; CRM → «Сотрудники» → «Новые регистрации» и «Проверки этапов ремонта».

## 27.07.2026 — IBAN поставщика подтягивается из накладной + поле в карточке контрагента

**Что попросили:** при оплате «з ФОП» ошибка «Немає IBAN отримувача». Куда вписать IBAN? И чтобы он сам подтягивался из Рахунка.

**Что сделали:**
- **Авто из накладной:** парсер теперь достаёт из файла реквизиты поставщика (IBAN, ЄДРПОУ, ІПН, название) — из шапки «Постачальник … Р/с UA…». При проведении прихода эти реквизиты сохраняются в карточку контрагента (если там пусто). Дальше IBAN сам подтягивается в оплату «з ФОП».
- **Вручную:** в карточку контрагента добавлены поля **IBAN / рахунок** и **ЄДРПОУ / ІПН** — можно вписать/поправить руками (клик по полю).

**Где в системе:** карточка контрагента (поля IBAN/ЄДРПОУ) + Финансы → Дт/Кт → «ФОП». Для текущего долга Ковчега проставил IBAN КОРСИКА в контакт — оплата пройдёт.

## 27.07.2026 — Ковчег: названия товаров теперь видны + IBAN Новой Почты подтягивается

**Что заметил Олег:** в накладной Ковчега строки/кол-во/суммы появились, но названия товаров пустые. И IBAN Новой Почты не подтягивается при оплате.

**Что сделали:**
- Названия товаров: универсальный парсер клал имя не в то поле — исправили, теперь имена видны (5 товаров Ковчега на месте).
- IBAN Новой Почты: акт/специфікація НП не содержит реквизитов (они в Рахунке). Теперь для Новой Почты подставляются её постоянные реквизиты (ЄДРПОУ 31316718, IBAN UA5333…), прописали их в карточку контрагента «ТОВ Нова Пошта». Когда проводишь акт НП — кредиторка привязывается к этому контрагенту, и IBAN сам подтягивается для оплаты «з ФОП».

**Где в системе:** Финансы → Вх. накладные (Ковчег — товары); оплата НП «з ФОП» — реквизиты из карточки контрагента.

## 27.07.2026 — Накладные других поставщиков (Ковчег/КОРСИКА) распознают товары

**Что заметил Олег:** накладная Ковчега не показывала товары для выбора — форма поставщика пустая.

**Что нашли:** парсер файлов был заточен только под формат Корженевского. Файл Ковчега (ТД КОРСИКА, старый .xls в кодировке cp1251) не распознавался — 0 позиций.

**Что сделали:** добавили универсальный парсер рахунка-фактуры: находит таблицу «Товар/Кількість/Ціна/Сума», снимает позиции до итога, чинит кодировку старых .xls. Работает и в почте, и при ручной загрузке — как запасной, когда конкретный парсер не сработал. Накладная Ковчега (#591) теперь показывает 5 товаров, итого 4066,40.

## 27.07.2026 — Отменённый акт НП можно провести заново

**Что попросили:** Олег отменил кредиторку по акту НП и хочет провести заново — но система говорила «уже есть».

**Что сделали:** проверка дубля по номеру акта теперь игнорирует отменённые/отклонённые — так отменённый акт можно провести заново. Удалили отменённую кредиторку по акту НП-018556243, вернули акт #585 в черновики (полные данные: 7719,59 · 12 отправлений) — жми «Провести», создастся свежая кредиторка. Дубликаты-пустышки того же акта отклонены.

## 27.07.2026 — Нова Пошта никогда не идёт в «поставщиков» (и при ручной загрузке)

**Что заметил Олег:** документ Новой Почты открывал форму поставщика с выбором товаров и приходом на склад — а НП это доставка, товаров у неё нет.

**Что нашли:** ручная загрузка накладной («Завантажити накладну») всегда ставила тип «поставщик». Поэтому НП-рахунок попадал в поставщиков с 0 позиций и требовал добавить товары. Это дубликаты уже обработанного акта.

**Что сделали:** теперь любой документ Новой Почты (письмо ИЛИ ручная загрузка) кладётся в раздел «НП» — там кредиторка за доставку, без прихода/товаров. Перенесли существующие #568 и #592 в НП. Дубликаты просто отклоняешь.

**Важно:** НП-документ НЕ нужно проводить как поставщика (это создало бы пустой приход + дубль долга). Настоящий акт НП (со специфікацией) создаёт кредиторку сам, без товаров.

## 27.07.2026 — Суммы долгов (Дт/Кт) показываются точно, с копейками

**Что заметил Олег:** в кредиторке по акту Новой Почты стояло 7 720 ₴, а в счёте 7 719,59 — округлялось.

**Что нашли:** данные хранились точно (7 719,59), но список Дт/Кт показывал суммы через округляющий формат (до целых гривен).

**Что сделали:** суммы кредиторки/дебиторки (и остаток) в разделе Дт/Кт теперь показываются точно, с копейками (7 719,59 ₴). Сами деньги не менялись — только отображение стало точным.

**Где в системе:** Финансы → Дт/Кт.

## 27.07.2026 — Карточка НП-документа без специфікації теперь понятна

**Что заметил Олег:** документ Новой Почты в разделе НП показывал пусто — «від», «0 ₴ · 0 відправлень», без номера, непонятно что это.

**Что сделали:** для НП-письма без специфікації теперь достаём № акта и дату из темы/имени файла, и карточка показывает: № акта + дату, отправителя, имя файла и подсказку «Без специфікації — вероятно дубль уже проведённого акта, можно Відхилити». Так сразу видно, что за документ. Существующий #589 → «НП-018556243 від 20.07.2026».

**Где в системе:** Финансы → Вх. накладные → раздел «Нова Пошта».

## 27.07.2026 — Письма от Новой Почты больше не попадают в «поставщиков»

**Что заметил Олег:** во «Входящих накладных» документ от Новой Почты оказался в поставщиках, а не в разделе НП, и было непонятно как его провести/оплатить.

**Что нашли:** классификатор писем считал документ «актом НП» только если во вложении есть файл «специфікація». Нова Пошта прислала к уже обработанному акту №НП-018556243 ещё два отдельных письма («Акт» и «Рахунок») БЕЗ специфікації — они и упали в «поставщиков». Это дубликаты уже проведённого акта (он проведён отдельно, кредиторка создана). Письма пришли 22.07 — задолго до сегодняшних правок, это не они.

**Что сделали:** теперь ЛЮБОЕ письмо от Новой Почты (отправитель novaposhta.ua) кладётся в раздел «НП», а не в «поставщиков» — даже без специфікації. Такой документ можно спокойно отклонить, если это дубль. Существующий документ #589 перенесли в раздел НП.

**Важно:** возможность провести/оплатить не менялась — просто мис-классифицированный НП-документ показывал форму поставщика (с товарами склада). Настоящие акты НП (со специфікацией) обрабатываются как и раньше.

## 27.07.2026 — В карточке клиента: выбор числа строк (от 5) + модалка «Создать сделку»

**Что попросили:** в блоках карточки клиента листать много строк неудобно. Нужен выбор числа отображаемых строк в КАЖДОМ блоке отдельно (начиная с 5), со скроллом и страницами. И чтобы «Создать сделку» открывалось окном, а не появлялось между строк.

**Что сделали:** в блоке «Угоди клієнта» добавили свой выбор «Показувати» (5/10/25/50, по умолчанию 5) + прокрутку + страницы. В блоке финансовых операций выбор строк теперь тоже начинается с 5 (5/10/25/50/100/200), по умолчанию 5. Кнопка «Створити угоду» теперь открывает отдельное окно (модалку) по центру — больше не вклинивается между строками.

**Где в системе:** карточка любого клиента → блоки «Фінанси клієнта» и «Угоди клієнта».

## 27.07.2026 — Приход помнит накладную-источник, её можно открыть

**Что попросили:** чтобы в каждом приходе сохранялась исходная накладная-источник и её можно было открыть.

**Что сделали:** когда приход создаётся из входящей накладной (Финансы → Вх. накладные → «Провести приход + кредиторку»), в приходе теперь сохраняется ссылка на эту накладную. В карточке прихода (Склад → Приходные накладные) появилась строка с поставщиком и № накладной + кнопка «Відкрити накладну-джерело» — открывает оригинал (файл/письмо) в новой вкладке.

**Где в системе:** Склад → Приходные накладные → открыть приход → кнопка «Відкрити накладну-джерело». Работает для приходов, созданных из входящих накладных; у ручных приходов источника нет — кнопка не показывается.

## 27.07.2026 — Товарный кредит: дебиторка создаётся при входе в стадию (даже мышкой)

**Что попросили:** Олег: сделка «грунт» стоит в стадии «Выдано в товарный кредит», но её нет ни в расходе, ни в дебиторке. Если товар отдали в кредит — клиент должен нам, это должно быть в дебиторке.

**Что нашли:** дебиторку создавала только кнопка «Товарний кредит (відвантаження в борг)» в окне оплаты. Если сделку просто перетащили в стадию кредита мышкой (в канбане) — долг не заводился. Поэтому у грунта дебиторки не было.

**Что сделали:** теперь при переходе сделки в стадию товарного кредита ЛЮБЫМ способом (перетащил/вручную) автоматически создаётся дебиторка на остаток суммы, если её ещё нет. Плюс починили конкретную сделку грунт (#65552) — завели дебиторку 900 грн на Сергія Селіфонова. Когда клиент оплатит — в Финансы → Дт/Кт жмёшь «Оплачено», и появится доход.

**Почему грунт не в расходе:** товарный кредит — это незавершённая сделка (деньги ещё не пришли). Себестоимость и прибыль считаются по завершённым (оплаченным) сделкам, поэтому пока грунт в расход не идёт — он висит как долг клиента (дебиторка). Расход/прибыль появятся, когда оплатит.

## 27.07.2026 — Колонка «Дата» в сделках клиента + период считает по дате сделки

**Что попросили:** Олег: в таблице «Угоди клієнта» нужна дата сделки, чтобы видеть, с какого числа начинать период. У клиента первая новая сделка — грунт 07.07, всё остальное раньше.

**Что сделали:** добавили колонку «Дата» в список сделок клиента (дата создания сделки). Период для прибыли/себестоимости теперь считается по этой же дате (created_at) — это реальная дата сделки, видна в списке. Раньше внутри считалось по дате закрытия, а она у старых сделок из Битрикса — оптовая дата импорта (мешала).

**Где в системе:** карточка клиента → «Угоди клієнта» (новая колонка «Дата») + фильтр «Период». Теперь дата в списке и период совпадают: задал период с даты первой новой сделки — и прибыль честная.

## 27.07.2026 — Прибыль по клиенту теперь считается за выбранный период

**Что попросили:** Олег: у клиента с историей из Битрикса прибыль показывала ложный минус (себестоимость по ВСЕМ сделкам за всё время, а выручка — только по недавним оплатам в CRM). Нужно видеть финансы «с момента, как начали работать», а старые сделки чтобы не мешали.

**Что сделали:** в карточке клиента плитки Выручка / Себестоимость склада / Прибыль / Расход теперь считаются за период из фильтра «Период» (доход и расход — по дате операции, сделки — по дате закрытия). Пусто = за всё время. Добавили кнопки «Цей рік» и «Цей місяць» — один клик.

**Где в системе:** карточка клиента → шапка «Фінанси клієнта» + фильтр «Период» ниже. Пример: у Сергія Селіфонова за всё время «Прибуток −9 447» (ложь), а с 01.07.2026 — честные +81 860 (июльская декор-работа без старых материалов из Битрикса).

## 27.07.2026 — Окно «Оплата з авансу» теперь понятно объясняет нехватку

**Что попросили:** Олег: аванса у клиентки хватает только на часть сделки, а окно оплаты «висело» и не проводило — было непонятно почему.

**Что сделали:** в окне «Прийняти оплату → З авансу клієнта» теперь сразу видно:
- если свободного аванса **0** — красная подсказка «у клиента нет свободного аванса, выбери реальный способ оплаты»;
- если аванса **меньше суммы сделки** — жёлтая подсказка «аванс покрывает N ₴ из M ₴, остаток проведи отдельно другим способом» + кнопка «Списать весь аванс (N ₴)» (ставит сумму = доступный аванс в один клик);
- если ввёл сумму **больше аванса** — красная строка под полём «оплата не пройдёт, уменьши сумму».

**Где в системе:** карточка сделки → «Прийняти оплату» → «З авансу клієнта». Логика: аванс закрывает столько, сколько реально свободно; остаток по сделке проводится отдельной оплатой (наличные/карта/LiqPay).

## 27.07.2026 — «Аванс клиента» теперь показывает реально свободные деньги

**Что попросили:** Олег заметил две проблемы. (1) При оплате сделки «из аванса» цифра аванса в карточке и проверка при оплате считались по-разному — плитка могла уйти в минус. (2) У клиентки, которая оплатила свои сделки наличными, в окне «З авансу клієнта» подтягивалась ВСЯ сумма, что она когда-либо платила (напр. 22 000), хотя эти деньги уже потрачены на закрытые сделки — свободных денег у неё нет.

**Что нашли:** старая формула аванса вычитала только оплаты, сделанные кнопкой «з авансу», но НЕ вычитала обычные оплаты сделок (наличные, LiqPay, терминал). Поэтому любой клиент, заплативший за сделку налом, показывал весь свой платёж как «свободный аванс».

**Что сделали:** и плитку «Аванс (вільні гроші клієнта)» в карточке, и проверку при кнопке «Провести оплату → З авансу» теперь считаем одинаково: **аванс = сколько клиент внёс − расходы − ВСЕ оплаты его сделок (любым способом)**. Обычная оплата создаёт и доход, и платёж — в авансе они гасят друг друга. Оплата «из аванса» дохода не создаёт — только уменьшает аванс.

**Где в системе:** карточка клиента → плитка «Аванс»; окно «Прийняти оплату → З авансу клієнта». Теперь свободный аванс = только реально не потраченные деньги клиента, и он честно переходит на будущие сделки. Пример: у клиентки, оплатившей все свои сделки налом, «Доступний аванс» = 0 (платить новую сделку надо налом/LiqPay, а не из несуществующего аванса).

## 27.07.2026 — Доход показывается в карточке клиента, даже если оплату провели ДО добавления клиента

**Что попросили:** менеджер сначала провела оплату, а потом добавила клиента к сделке — и в карточке клиента доход не отобразился.

**Что нашли:** доход привязывался к клиенту только по имени/прямой ссылке в момент оплаты. Если клиента ещё не было (оплату провели раньше) — доход оставался «ничей» и в карточке клиента не показывался.

**Что сделали:**
1. Теперь при проведении оплаты доход и расход **сразу привязываются к клиенту сделки** (не только по имени, а прямой связью).
2. Карточка клиента считает доходом и все операции, привязанные к **сделкам этого клиента** — поэтому, даже если оплату провели до добавления клиента, после привязки клиента к сделке доход появляется автоматически.
3. Дозаполнили **15 уже существующих** операций, которые «висели без клиента», — теперь они привязаны и видны в карточках.

**Где в системе:** карточка клиента → блок «Фінанси клієнта» → плитка «Дохід» и список операций.

## 26.07.2026 — Замер, смета и настройки калькулятора привязаны к клиенту и ответственному

**Что попросили:** после LiDAR-замера передавать весь объект по комнатам в смету конкретного клиента, не смешивать данные разных клиентов и сотрудников, убрать ручной переключатель роли и дать администратору единые условия калькулятора.

**Что сделали:** CRM теперь принимает полный проект с комнатами, стенами, окнами, дверями, откосами, колоннами и балками, создаёт нативную смету только в сделке выбранного клиента и сохраняет ответственного сотрудника. На общем iPhone проекты разделены по учётным записям; чужой клиент, проект или сделка блокируются. В карточке сделки появилась собственная вкладка «Смета» с площадью стен, пола, потолка, откосами и позициями. Добавлены отдельные права на просмотр, редактирование и административные настройки калькулятора; ставки, запас, минимальная сумма, скидка и округление задаются в CRM и применяются в приложении.

**Где в системе:** CRM → «Продажи» → сделка клиента → вкладка «Смета»; условия — CRM → «Настройки» → «Калькулятор»; права — CRM → «Сотрудники и права».

## 21.07.2026 — Замеры и смета сохраняются только выбранному клиенту

**Что попросили:** при отправке сметы из приложения Wallcov Замер сохранять весь объект по комнатам конкретному новому или существующему клиенту и не показывать ему данные других клиентов.

**Что сделали:** приложение отправляет в CRM сразу весь проект: комнаты, стены, проёмы, площадь стен, пола и потолка, погонные метры и квадратуру откосов, а также строки и итог сметы. CRM проверяет выбранного клиента, привязывает замер только к его сделке или создаёт для него новую сделку. Если передана сделка другого клиента, сохранение блокируется. В карточке клиента появился отдельный блок «Замеры из приложения» с разбивкой по комнатам; данные других клиентов туда не попадают.

**Где в системе:** приложение Wallcov Замер → проект → «Калькулятор» → «Отправить смету и замеры в CRM»; результат — CRM → «Клиенты» → карточка выбранного клиента → «Замеры из приложения».

## 20.07.2026 — Чат лендинга и квиз передают клиента в CRM

**Что попросили:** дать посетителю возможность сразу поговорить на сайте с обученной Юлей или менеджером, не потерять контакт после закрытия страницы и передавать ответы квиза в отдельную воронку лендинга.

**Что сделали:** добавили один общий канал **«Web Chat · Wallcov»** в Контакт-центр. В нём Юля отвечает на базе тех же знаний, что используются для Facebook и Telegram, а менеджер может подхватить диалог в CRM. Ошибка соединения `Failed to fetch` устранена: чат теперь подключается к CRM через защищённый маршрут самого лендинга. В виджете клиент сначала выбирает большую понятную кнопку: онлайн-чат с Юлей, Telegram или Viber; Instagram, TikTok и Telegram-канал показаны отдельно как социальные страницы. Источник, UTM, выбранный материал, комната и точная площадь сохраняются в сделке. Квиз считает «Сирену», «Мерми» и обычную «Луну»; если расчёт меньше 220 грн, применяется минимальная сумма тест-набора. Для Вельвета менеджер видит номер `V001–V192`, группу цвета и цифровой тон из новой текстурной палитры. Имя, телефон и удобный канал связи сохраняются до перехода клиента в Telegram или Viber.

**Где в системе:** CRM → «Контакт-центр» → канал «Web Chat · Wallcov»; заявка — CRM → «Продажи» → «Сделки» → воронка «Лендинг · wallcovdliastin.com.ua».

## 20.07.2026 — Отдельная воронка для лендинга wallcovdliastin.com.ua

**Что попросили:** создать отдельную воронку и назвать её так, чтобы сотрудники сразу понимали, с какого лендинга пришёл клиент.

**Что сделали:** добавили воронку **«Лендинг · wallcovdliastin.com.ua»** с понятными этапами: новая заявка → первый контакт → фото и параметры → подбор трёх вариантов → образец → расчёт → ожидание оплаты → заказ оплачен → успешная сделка или «не реализовано». Старые воронки и сделки не менялись.

**Где в системе:** CRM → «Продажи» → «Сделки» → список воронок → «Лендинг · wallcovdliastin.com.ua».

## 20.07.2026 — Поиск дублей: найти по номеру или фамилии и объединять пачками

**Что попросили:** чтобы во вкладке «Поиск дублей» можно было найти конкретный номер телефона или фамилию и объединить именно их. И чтобы было видно, насколько совпадение надёжное — если у контактов совпадает всё (и номер, и имя, и почта), такие можно объединять массово, а не по одному.

**Что сделали:**
- Появилась **строка поиска**: вводишь номер телефона (можно последние цифры), фамилию, email или ник — и остаются только те группы дублей, где такой человек есть.
- Появился фильтр **«По какому совпадению»**: все / только телефон / только email / только мессенджер / только имя-фамилия.
- Появился фильтр **«Точность»**: показать только те группы, где совпало 2, 3 или все 4 поля. Чем больше совпало — тем надёжнее, что это правда один и тот же человек.
- В шапке каждой группы теперь видно **зелёные метки** — по каким именно полям контакты совпали, и значок «совпало 3». Самые надёжные группы система ставит **первыми**.
- Одна и та же пара контактов больше **не показывается по 4 раза** (раньше она попадала в список отдельно за телефон, отдельно за имя и т.д.) — теперь это одна группа со всеми метками сразу.
- **Массовое объединение:** слева у каждой группы галочка, есть кнопка «Отметить всё на странице». Внизу появляется панель «Отмечено групп: N» → «Объединить отмеченные». Перед объединением система **сначала показывает, что именно будет сделано**: кого оставит, кого удалит и сколько всего контактов исчезнет. Пока не нажмёшь «Да, объединить» — ничего не удаляется.
- Система сама подсказывает, **кого оставить**: контакт, у которого заполнено больше данных. Можно переключить вручную зелёным кружком.
- Защита: если один и тот же контакт попал сразу в две отмеченные группы — вторая группа пропускается (о ней напишут в окне подтверждения), чтобы ничего не потерялось. Такие объединяй по отдельности.

**Где в системе:** CRM → «Поиск дублей» → вкладка «Клиенты».

## 19.07.2026 — Карточка сотрудника с фото и зелёная отметка «на работе сейчас»

**Что попросили:** видеть, у кого прямо сейчас идёт рабочий день (зелёный значок), и завести карточку сотрудника, где можно заполнить данные о человеке, его интересы и поставить фото или логотип.

**Что сделали:**
- Рядом с именем сотрудника появился **зелёный значок «На работе»**, если у него прямо сейчас идёт рабочий день, и **жёлтый «Пауза»**, если он на обеде/паузе. Значок виден в списке сотрудников, во вкладке «Активность» и на карте отделов (зелёная точка).
- Появилась **карточка сотрудника** — клик по имени или фото. В ней: фото, должность, день рождения, телефон, Telegram, блок «О сотруднике» и **«Интересы, хобби»**. Видно также email и с какого числа человек в компании.
- **Фото или логотип** ставится кнопкой 📷 на аватарке: выбираешь файл — картинка сама обрезается по центру и уменьшается, поэтому загружается быстро и не засоряет систему. Фото можно убрать — тогда снова будут инициалы цветным кружком.
- Кто может редактировать: **свою карточку — каждый сотрудник сам**, чужие — только руководитель (право «Управление сотрудниками»). Чужую карточку обычный сотрудник открыть на редактирование не сможет.

**Где в системе:** CRM → «Сотрудники» → «Список» (клик по имени) или «Активность» (клик по фото/имени).

## 19.07.2026 — Контроль простоя: авто-пауза, если сотрудник отошёл и «забыл» отметиться

**Что попросили:** ограничить безделье и уберечь компанию от обмана «числюсь на смене, а сам занимаюсь своими делами». Если сотрудник не поставил паузу сам — система должна спросить «ты на месте?», и если не ответил — поставить паузу; когда вернулся — обязательно снять паузу. При этом склад и тонировку не наказывать: там работают руками и могут не подходить к компьютеру.

**Что сделали:**
- Пока идёт смена, система тихо следит за активностью (мышь/клавиатура). Если нет активности заданное время — всплывает окно **«Вы на месте?»** с обратным отсчётом 60 сек. Ответил «Да, работаю» — смена идёт дальше. Не ответил — **авто-пауза** (красная пометка «простой»), время не идёт в рабочее.
- Если сотрудник закрыл вкладку CRM и ушёл — сервер это замечает (пропал «пульс») и тоже ставит паузу. Просто закрыть программу и числиться на смене — не выйдет.
- Когда человек вернулся и коснулся мыши — выскакивает **обязательное** окно «С возвращением! Снять паузу и продолжить работу» — пока не нажмёт, время простоя продолжается.
- Если сотрудник **ушёл и не завершил день** (или забыл смену со вчера) — система сама закрывает смену на момент последней активности, чтобы часы не раздувались.
- **Время простоя настраивается по каждому отделу** (Сотрудники → Активность → «Контроль простоя»). Продажи/офис — 15 минут. **Склад/Логистика и Производство/Мастера — выключено (0)**, потому что работают руками и отходят от ПК; их контролируем по факту отгрузок/задач и по входу-выходу.
- Все авто-паузы и авто-завершения попадают в ленту активности сотрудника (видно, когда и почему отлучался).

**Где в системе:** таймер смены вверху CRM (▶ Начать день / ☕ Обед / ⏹ Завершить). Настройка порогов — CRM → «Сотрудники» → «Активность» → кнопка «⏱ Контроль простоя».

## 19.07.2026 — В окне активности сотрудника: таблица смен с паузами (обед)

**Что попросили:** в выпадающем окне по сотруднику показать смены таблицей с понятными столбцами и видеть, когда человек ставил паузу и когда снимал (например, обед).

**Что сделали:** блок «Смены» стал таблицей со столбцами: **Дата · Приход · Уход · Паузы (обед/отлучки) · Пауза всего · Отработано**. Каждая пауза показывается интервалом времени (например 13:00–13:30); ручные паузы — жёлтым, авто-паузы за простой (на будущее) — красным. Теперь система запоминает каждую паузу отдельно, а не только общую сумму. Старые смены (до этого обновления) показывают только часы — детализация появляется с новых смен.

**Где в системе:** CRM → «Сотрудники» → «Активность» → клик по сотруднику → таблица «Смены и паузы».

## 19.07.2026 — Аналитика по сотрудникам: посещаемость, активность и статусы «уволен / неактивный»

**Что попросили:** видеть активность каждого сотрудника — что и когда он делал, сколько отработал по времени; отмечать уволенных и неактивных; смотреть отчёты (в т.ч. ЗП/KPI) отдельно по работающим и по ушедшим; видеть картину по отделам.

**Что сделали:**
- У каждого сотрудника теперь есть **статус**: «Активный», «Неактивный» (временно — отпуск/пауза, доступа нет, но человек не уволен) и «Уволен» (с датой увольнения). В разделе «Сотрудники → Список» появились цветные метки статуса, дата приёма/увольнения и кнопки: пометить неактивным, вернуть в активные, уволить. Сверху — фильтр «Активные / Неактивные / Уволенные / Все».
- Новая вкладка **«Активность»** (Сотрудники → Активность). За выбранный месяц по каждому сотруднику видно: сколько **часов** отработал (из таймера смен), сколько **рабочих дней**, средний день, сколько **действий** сделал в системе, больничные/отпуска/прогулы и когда был последний вход. Есть сводка по всей компании и **по отделам**. Можно фильтровать по статусу и по отделу.
- **Клик по сотруднику** открывает его личную ленту: список смен (когда пришёл/ушёл, сколько отработал) и все его действия по времени — сменил стадию, создал сделку, провёл оплату, оформил ТТН и т.д.
- В отчёте **ЗП/KPI** добавлен фильтр статуса — можно смотреть зарплату отдельно по активным и по уволенным за период.
- Доступ к аналитике по сотрудникам — только у руководителя (права «Управление сотрудниками» или «ЗП/KPI»); обычный менеджер чужую активность не видит.

**Где в системе:** CRM → «Сотрудники» → вкладки «Список» (статусы и фильтры) и «Активность» (посещаемость + лента). Отчёт ЗП: Финансы → ЗП/KPI.

## 18.07.2026 — Визуальный редактор сайта и понятная карточка проверки товара

**Что попросили:** длинная форма главной страницы не показывает, где текст или фотография находятся на сайте, а карточка проверки товара выглядит как техническая таблица и непонятна сотруднику.

**Что сделали:** редактор главной страницы теперь показывает структуру сайта, настройки только выбранного блока и живой макет страницы. Блок можно выбрать слева или нажать прямо на него в макете; есть отдельный вид для компьютера и телефона. Карточку проверки товара разделили на понятные вкладки «Основное», «Цена и расход», «Для покупателя» и «Служебные данные». Ошибка расхода объясняется рядом с полями, а перепутанные минимальное и максимальное значения можно поменять местами одной кнопкой.

**Где в системе:** CRM → «Интернет-магазин» → «Главная и дизайн»; для товара — «Товары и цены» → проверка таблицы → «Открыть понятную карточку».

## 18.07.2026 — Все страницы интернет-магазина снова прокручиваются

**Что попросили:** в админ-панели интернет-магазина длинные страницы не прокручивались, поэтому нижние блоки, товары и кнопки сохранения нельзя было увидеть.

**Что сделали:** добавили отдельную вертикальную прокрутку во все три раздела интернет-магазина: «Товары и цены», «Главная и дизайн» и «Аналитика сайта». Прокрутка работает мышью, трекпадом и пальцем на телефоне; боковое меню и верхняя панель CRM остаются на месте.

**Где в системе:** CRM → «Интернет-магазин» → любая из трёх вкладок.

## 18.07.2026 — «Закрепить чат» теперь забирает и «остывшего» клиента

**Что попросили:** бывало, менеджер закрепляет чат за собой, а клиент и история сделок к нему НЕ переходят — потому что клиент числится за другим менеджером. Просили: раз менеджеров немного, разрешить перехват.

**Что сделали:** когда менеджер жмёт «Закрепить за собой», система смотрит, работал ли прежний ответственный с этим клиентом. Если он **не писал клиенту 30 дней и больше**, а клиент снова обратился — клиент и его открытые сделки/лиды переходят к тому, кто взял живой чат. Прежнему ответственному приходит уведомление «вашего клиента перехватил такой-то». Если же прежний менеджер активно ведёт клиента (писал в последние 30 дней) — перехват НЕ происходит, чат просто закрепляется. Выигранные и проигранные сделки по-прежнему не трогаем (чтобы не ломать зарплату и историю).

**Где в системе:** Входящие → любой чат → кнопка «Закрепить за собой». Порог 30 дней задаётся одной строкой в коде (_TAKEOVER_STALE_DAYS).

## 17.07.2026 — Проверка цен из таблицы и понятные подсказки в карточке сайта

**Что попросили:** непонятно, как заполнять интернет-магазин, что означает «Группа товара» и почему цены из таблицы нельзя сначала проверить перед сохранением.

**Что сделали:** в разделе «Интернет-магазин» появился безопасный экран проверки Excel. Он сравнивает каждую строку с номенклатурой CRM, показывает текущую и новую цену, отмечает пропуски и ошибочные расчёты, позволяет открыть все столбцы и исправить их. Загрузка таблицы ничего не меняет; карточки сохраняются только после выбора строк и отдельного подтверждения. В самой карточке товара добавлены короткие пояснения и четыре шага заполнения. Для обычного товара служебные «Группа товара» и URL создаются автоматически — вручную их придумывать больше не нужно.

**Где в системе:** CRM → «Интернет-магазин» → блок «Проверить цены и описания из таблицы»; затем «Открыть карточку» для описаний и фотографий.

## 15.07.2026 — Финансы на компьютере: чище шапка, вкладки в ряд, фильтр в окне

**Что попросили:** строка «Розділ бачать finance.view» занимает место; вкладки в два ряда — некрасиво; фильтр журнала раздроблён на много ячеек — собрать в одно окно, оставить широкий поиск как в Google.

**Что сделали:**
- Убрали постоянную строку-напоминание про права (не мешает больше).
- **Вкладки** Финансов теперь в **один ряд** (листаются вбок, если не влезают) вместо двух рядов.
- **Журнал:** сверху осталась одна **широкая строка поиска** — ищет по всему (сумма, контрагент, категория, комментарий, счёт). Все детальные фильтры (клиент, даты, сумма от/до, категория, фонд, направление, канал…) собраны в **отдельное окно** — открывается кнопкой «⚙ Фільтри». На кнопке показывается, сколько фильтров сейчас активно.

**Где в системе:** Финансы → Журнал: строка поиска + кнопка «Фільтри».

## 15.07.2026 — Финансы удобно с телефона (компьютерная версия не изменилась)

**Что попросили:** адаптировать страницу «Финансы» под телефон, не сломав вид на компьютере. И окно быстрого платежа сделать в стиле CRM.

**Что сделали:** на телефоне «Финансы» теперь удобные:
- **Дт/Кт (долги)** — вместо широкой таблицы на телефоне показываются **карточки в одну колонку**: контрагент, сумма, срок, остаток и кнопки «Оплачено» / «💳 ФОП» под палец.
- **Вкладки** сверху листаются вбок пальцем (не налезают друг на друга).
- Отступы и панели компактнее — всё влезает в экран.
- **Окно быстрого платежа ⚡** получило фирменный вид: терракотовая шапка в стиле CRM, короткие кнопки-фонды, крупные поля.

**Важно:** на компьютере всё осталось как было — телефонная версия работает отдельно (по ширине экрана), десктоп не тронут.

**Где в системе:** открой «Финансы» с телефона — увидишь карточки и удобные вкладки. Кнопка ⚡ внизу справа.

## 15.07.2026 — ⚡ Быстрый платёж: зафиксировал за 10 секунд, банк подтвердил сам

**Что попросили:** оплатил (например, забрал материалы с Новой Почты) — хочу сразу зафиксировать в CRM с фондом «за что», пока помню, минимумом движений. Выписку сразу втащить не могу, а потом забуду, откуда платёж. И удобно с телефона.

**Что сделали:** внизу справа на «Финансах» появилась круглая кнопка **⚡**. Нажимаешь (удобно с телефона) → открывается быстрая форма: **сумма** (крупно), **за что** (кнопки-фонды: Упаковка/Доставка/Материалы… или выбор из всех), **кому** (по умолчанию «Нова Пошта»), **карта или наличка**, коммент. «Зафиксувати» — расход в CRM со всеми полями за 10 секунд. Дальше выписка Привата, когда подтянется, **сама склеит** этот платёж с твоей заметкой (по сумме + дате + получателю) — банковские детали допишутся, **дубля не будет**. Наличку выписка не видит — она остаётся как твоя заметка.

**Где в системе:** любой экран «Финансов» → круглая кнопка ⚡ внизу справа.

## 16.07.2026 — Все рабочие инструкции сохраняются по блокам в «Що нового»

**Что попросили:** сделать постоянное правило, чтобы инструкции по каждому разделу не терялись в переписках, а находились прямо в CRM.

**Что сделали:** теперь после каждой заметной доработки, которая меняет работу сотрудника, в CRM обязательно создаётся новая или обновляется существующая инструкция. Записи распределяются по понятным блокам: «Интернет-магазин», «Финансы», «Склад», «Продажи», «Клиенты», «Телефония» и «Общее». Если порядок работы изменился, старая инструкция обновляется, чтобы рядом не оставалось устаревшей версии. Внутри каждого пункта простым языком указано, где найти функцию, что нажимать по шагам, что работает автоматически, как проверить результат и чего нельзя делать.

**Где в системе:** левое меню CRM → «Инструкции и что нового» → выбери нужный блок сверху.

## 15.07.2026 — Сортировка и фильтры в Дт/Кт + виден счёт оплаты

**Что попросили:** сортировку в кредиторке (в оплаченных, запланированных, отменённых) по каждому столбцу; чтобы последний оплаченный был вверху; фильтры по всем полям (направление, сделка, категория); а в карточке операции показывать, с какого счёта прошла оплата с ФОП.

**Что сделали:** в разделе «Дт/Кт» теперь **клик по названию столбца сортирует** таблицу по нему (срок, сумма, контрагент по алфавиту, категория, направление, канал, сделка, комментарий) — повторный клик меняет порядок. По умолчанию: в «Оплаченных» сверху **последний оплаченный** (кого оплатил только что — вверху), в «Запланированных» — по сроку. Добавлены **фильтры**: направление и категория (выпадашки) + контрагент. В оплаченных строках теперь видно **дату оплаты и счёт**, с которого ушли деньги. В карточке операции поле «Рахунок оплати» теперь заполняется реальным счётом, с которого прошла оплата с ФОП.

**Где в системе:** Финансы → «Дт/Кт». Заголовки столбцов кликабельны (сортировка), сверху фильтры направление/категория/контрагент.

## 15.07.2026 — Автопогашение долга после оплаты (сам закрывается по выписке)

**Что попросили:** чтобы после того как оплатил долг с ФОП, он в CRM закрывался сам, без ручного «Оплачено».

**Что сделали:** CRM и так читает выписку твоего ФОП по Привату каждые 5 минут. Теперь, когда в выписке появляется исходящий платёж, CRM сама находит подходящий долг (по ЄДРПОУ или счёту получателя + сумме) и **уменьшает остаток**. Если оплачено полностью — долг закрывается сам. Частичные оплаты накапливаются (заплатил половину — остаток уменьшился на половину). Один платёж гасит один долг (не задваивает). То есть: подписал платёж в Приват24 → через пару минут долг в CRM сам погасился.

**Важно:** чтобы совпадение было точным, у получателя долга должны быть заполнены реквизиты (ЄДРПОУ/счёт) — из накладной они подтягиваются автоматически. Если два долга одному поставщику на одинаковую сумму — надёжнее платить через кнопку «💳 ФОП».

**Где в системе:** ничего нажимать не нужно — работает само в фоне. Результат виден в «Дт/Кт» (остаток уменьшается / долг «оплачено»).

## 16.07.2026 — Полный чат в карточке клиента

**Что попросили:** дать в карточке клиента такой же доступ к диалогу, как в карточке сделки: открывать полный чат справа, сворачивать, когда он не нужен, выбирать канал и начинать переписку, если открытого диалога ещё нет.

**Что сделали:** в шапке карточки клиента появилась кнопка «Открыть чат». Она раскрывает справа полноразмерную панель с тем же живым диалогом, действиями, полем ответа и выбором Viber/Telegram, что и в карточке сделки. Панель закрывается кнопкой «Свернуть» или кликом вне неё. Если у контакта есть только завершённые чаты или переписки ещё не было, показывается форма первого сообщения: CRM берёт номер из карточки, отправляет через выбранную линию и создаёт новый открытый чат без нового контакта и лида.

**Где в системе:** Клиенты → карточка клиента → «Открыть чат» в верхней строке.

## 16.07.2026 — Выбор Viber/Telegram сверху и первое сообщение без готового чата

**Что попросили:** убрать выбор канала и номера из блока чата и перенести его выше — в строку с кнопками «Стрічка / Чат», «Товари», «Нова Пошта» и «Кошторис», расположив справа. Если с клиентом ещё нет открытого диалога, дать возможность выбрать Viber или Telegram, написать первое сообщение на номер из карточки клиента и создать чат отправкой.

**Что сделали:** живой переключатель «Відповідати через» перенесён в правую часть верхней панели сделки. Через него выбирается Viber или Telegram и нужный номер; выбранный канал сразу применяется к тому же чату. Если переписки ещё нет, CRM показывает окно первого сообщения, берёт телефон из карточки клиента и после нажатия «Надіслати» одновременно отправляет текст и создаёт чат. Для существующего контакта новый контакт и лид не создаются. Секретные API-ключи линий в интерфейс не передаются.

**Где в системе:** карточка сделки → верхняя строка вкладок и действий → справа «Відповідати через»; блок «Чат з клієнтом» → первое сообщение.

## 15.07.2026 — Отдельная админ-панель интернет-магазина внутри CRM

**Что попросили:** управлять сайтом из CRM, а не создавать вторую отдельную админку на домене магазина.

**Что сделали:** в левом меню CRM появился раздел **«Интернет-магазин»**. На одном экране видно все группы товаров сайта, количество опубликованных карточек и черновиков, четыре комплектации, наличие фотографий, ошибки заполнения и состояние фоновой передачи. Есть поиск по названию и SKU, фильтры, кнопка редактирования номенклатуры и ссылка на опубликованную страницу. Данные обновляются автоматически каждые 30 секунд; отдельная база и дубли цен не создаются.

**Где в системе:** левое меню → «Интернет-магазин». Редактирование открывает ту же карточку номенклатуры CRM с вкладками «На сайте», «Фото» и «Проверка».

## 15.07.2026 — Частичное погашение долга + остаток по накладной + открыть накладную из долга

**Что попросили:** большие суммы гасятся частями — погашение должно быть на основании конкретной кредиторки (накладной), и чтобы было видно остаток: сколько ещё должны по отдельной накладной (плюс общая сумма как сейчас). И чтобы из кредиторки можно было кликнуть на основание и открыть саму накладную.

**Что сделали:** теперь долг можно гасить **частями**. Нажимаешь «Оплачено» — CRM спрашивает сумму погашения (по умолчанию весь остаток, но можно вписать часть, например половину). В строке долга сразу видно: **общая сумма** и **остаток** (сколько ещё осталось погасить по этой накладной). Когда остаток становится 0 — долг закрывается сам. Кнопка «💳 ФОП» тоже платит остаток. Плюс: если долг создан из накладной поставщика, его **основание кликабельное (📄)** — нажимаешь и открывается сама накладная на просмотр.

**Где в системе:** Финансы → «Дт/Кт». У каждого долга видно сумму и остаток, кнопка «Оплачено» (частично), кликабельное основание для открытия накладной.

## 15.07.2026 — Оплата долга поставщику с ФОП прямо из CRM (кнопка «ФОП»)

**Что попросили:** чтобы возле платежа можно было нажать кнопку и оплатить с ФОП через Приват — и Новой Почте, и поставщикам, любые платежи.

**Что сделали:** в разделе «Дт/Кт» возле каждой кредиторки (долга) появилась кнопка **«💳 ФОП»**. Нажимаешь — CRM сама подставляет реквизиты получателя (счёт, ЄДРПОУ/ІПН, название) из карточки контрагента или из накладной, сумму и назначение, показывает тебе на проверку. Подтверждаешь — **в твоём Приват24 Бизнес создаётся черновик платежа** (уже заполненный). Дальше ты заходишь в Приват24 и **подписываешь его своей подписью (СмартАйДи/КЕП)** — только тогда деньги уходят. То есть вручную реквизиты вбивать не нужно, а окончательное решение и подпись — всегда за тобой.

**Важно:** чтобы кнопка отправляла платёж, у твоего Автоклиента в Приват24 Бизнес должно быть включено право «создание платежей». Первый раз проверь на маленькой сумме (1 грн на свой счёт).

**Где в системе:** Финансы → «Дт/Кт» → у долга кнопка «💳 ФОП». Подпись платежа — в приложении Приват24 Бизнес.

## 15.07.2026 — Ответ Telegram возвращается в тот же чат и контакт

**Что попросили:** после сообщения из CRM ответ клиента в Telegram создавал отдельный чат, новый контакт и новый лид. Нужно узнавать клиента по номеру и Telegram, продолжать тот же диалог и добавлять Telegram как дополнительный канал существующего контакта.

**Что сделали:** CRM теперь распознаёт один номер во всех обычных форматах (`097…`, `38097…`, `+38097…`) и ищет Telegram не только в основной ссылке, но и среди дополнительных мессенджеров контакта. Первый ответ Telegram связывает технический ID пользователя с уже открытым диалогом по номеру, поэтому следующие сообщения приходят туда же. Telegram добавляется в каналы существующей карточки без удаления Instagram или других связей; новый контакт и лид для такого ответа больше не создаются.

**Где в системе:** карточка сделки или существующего контакта → чат → Telegram E-chat; карточка контакта → дополнительные каналы и мессенджеры.

## 15.07.2026 — Открытие письма и накладной прямо из CRM (до проведения)

**Что попросили:** до того как провести накладную — хочу открыть её из CRM и посмотреть, а само письмо чтобы открывалось при клике на строку. И чтобы читались накладные в формате .xlsx тоже.

**Что сделали:** в разделе «Вх. накладные» **клик по строке открывает письмо** — видно текст письма, от кого, номер и сумму. Прямо из окна **кнопка «Открыть накладную»** — скачивает/открывает оригинальный файл (.xls или .xlsx), чтобы проверить перед проведением. Оттуда же можно сразу «Провести» или «Отклонить». Теперь читаются и .xls, и .xlsx накладные. Заодно поправили распознавание дат в старых накладных (раньше некоторые читались не полностью).

**Где в системе:** Финансы → «Вх. накладные» → клик по строке → окно письма с кнопкой «Открыть накладную».

## 15.07.2026 — В магазине опубликованы пробники Galateya и Mermi Silk

**Что попросили:** продолжить наполнение магазина товарами из номенклатуры и решить, где должна находиться админ-панель сайта.

**Что сделали:** CRM оставили единой админ-панелью магазина, чтобы цены, SKU, фото и публикация не дублировались в двух местах. После предварительной проверки и теста одного товара опубликованы две следующие карточки — Galateya и Mermi Silk. В каждой доступны четыре комплектации пробника со своими номенклатурными артикулами, ценами и фотографиями. Все изменения переданы на сайт без ошибок; выбор конкретной комплектации и добавление в корзину проверены.

**Где в системе:** Склад → Номенклатура → папка «Тестові набори та викраски» → карточка товара → вкладки «На сайте», «Фото», «Проверка». Публичные карточки: `/new/product/galateya-probnik` и `/new/product/mermi-silk-probnik`.

## 15.07.2026 — Накладные поставщиков (Корженевский) → приход на склад одним окном

**Что попросили:** накладные от Корженевского приходят на почту (файлы .xls). Не хочу вносить каждую строку вручную. Хочу окно, где под каждый его товар выберу наш со склада — и это запомнится как правило (его название = наш товар). Дальше приход делается быстро, после моей проверки. И кредиторка, если беру в долг.

**Что сделали:** накладные Корженевского теперь сами падают в раздел «Вх. накладные» и разбираются (номер, дата, товары, количество, цены). Жмёшь **«Провести»** у его накладной — открывается **окно сопоставления**: слева его товар, справа выбираешь наш товар со склада (поиск по названию, 1345 позиций), можно поправить количество и цену. Выбор **запоминается как правило** — в следующей его накладной те же товары подставятся сами. Нажимаешь «Провести приход + кредиторку» → товар заходит на склад (приход) + создаётся долг поставщику (кредиторка на сумму накладной). Поставщик заводится автоматически по его реквизитам (ІПН, IBAN из накладной). Повторную накладную с тем же номером дважды не проведёт.

**Где в системе:** Финансы → «Вх. накладные» → у накладной поставщика кнопка «Провести» → окно сопоставления. Приход появляется на Складе, кредиторка — в Дт/Кт.

## 15.07.2026 — Накладные и акты приходят с почты автоматически (раздел «Вхідні накладні»)

**Что попросили:** не хочу вручную загружать файлы. Накладные и акты приходят мне на почту (Новая Почта — акты за доставку, Корженевский и другие поставщики — накладные на товар). Хочу, чтобы CRM сама забирала их с почты, показывала списком, а я только проверял и подтверждал. Видно должно быть только мне.

**Что сделали:** подключили почтовый ящик к CRM (Настройки → Интеграции → «Пошта накладних»: сервер, e-mail, пароль приложения, список отправителей). Теперь CRM каждые 10 минут проверяет почту и забирает письма **только от указанных отправителей** (Новая Почта, Корженевский). Появился раздел **Финансы → «Вх. накладные»** (виден только владельцу): туда попадают **черновики** — акты Новой Почты (сумма, дата, количество отправлений уже разобраны) и накладные поставщиков (файл, отправитель). По акту НП жмёшь **«Провести»** — создаётся кредиторка на Нову Пошту и доставка разносится по сделкам. Ненужное — «Отклонить» или удалить. Есть кнопка **«Проверить почту»** — забрать прямо сейчас. Ничего не создаётся, пока не подтвердишь.

**Где в системе:** Финансы → вкладка «Вх. накладные». Настройка подключения — Настройки → Интеграции → «Пошта накладних».

## 15.07.2026 — Telegram E-chat на том же номере и исправление приёма ответов

**Что попросили:** добавить Telegram, подключённый в E-chat на том же рабочем номере, в список «Відповідати через». После отправки из CRM клиент получал сообщение, но его ответ обратно в CRM не появлялся.

**Что сделали:** CRM теперь поддерживает отдельную Telegram-линию E-chat рядом с Viber: у каждой свой API-ключ, номер, webhook и пункт выбора в чате. Добавили правильные форматы отправки и приёма Telegram E-chat. Контакт-центр явно показывает webhook каждой линии и предупреждает, что без добавления этого адреса в кабинет E-chat входящие ответы не поступают. Диагностика подтвердила причину пропажи ответа: E-chat ни разу не обратился к Viber-webhook CRM.

**Где в системе:** Контакт-центр → «E-chat · Viber + Telegram»; карточка сделки → чат → «Відповідати через».

## 15.07.2026 — Объединение дублей: несколько мессенджеров у контакта (Instagram + Telegram + Viber + номер)

**Что попросили:** при объединении дублей — не выбирать один мессенджер, а оставить несколько (у одного дубля Telegram, у другого Instagram, будет ещё Viber и телеграм-номер), и чтобы источники были понятно подписаны по типу (Instagram/Telegram/Viber/телефон).

**Что сделали:** в окне объединения внизу появился блок **«Мессенджеры / контакты (можно несколько)»** — собраны все ссылки/номера из всех дублей, каждая с подписью типа (Instagram, Telegram, Viber, Facebook, TikTok, Телефон/Viber). Галочками отмечаешь, какие оставить — по умолчанию все. Объединённый контакт хранит теперь **несколько мессенджеров**, а не один. На карточке клиента они показываются списком со ссылками.

**Где в системе:** «Дублі» → «Об'єднати» → внизу окна блок «Мессенджеры / контакты»; на карточке клиента — под «Ссылка на аккаунт».

## 15.07.2026 — Кредиторка по Новой Почте из акта (загрузка счёта одной кнопкой)

**Что попросили:** Новая Почта каждый период присылает акт со счётом за доставку — сколько мы, как бизнес, должны им заплатить. Нужно, чтобы это считалось в CRM: видеть кредиторку по Новой Почте, а стоимость доставки раскидывалась по сделкам.

**Что сделали:** в разделе «Финансы» → вкладка «Дт/Кт» появилась кнопка **«⬆ Акт НП»**. Нажимаешь, выбираешь два файла из кабинета Новой Почты (Специфікація + Рахунок, формат .xlsx) — система сама читает сумму, номер акта, договор и список всех отправлений. Показывает окно: сумма (с НДС), сколько отправлений привязалось к сделкам, сколько без сделки. Подтверждаешь — создаётся одна **кредиторка на «Нова Пошта»** (в блоке «Кредиторка — ми винні») на всю сумму акта, а по каждому отправлению стоимость доставки записывается в его сделку — теперь видно, во сколько обошлась доставка каждого заказа. Повторная загрузка того же акта дубль не создаёт. Важно: деньги в прибыли не задваиваются — расход учтётся один раз, когда оплатишь эту кредиторку.

**Где в системе:** Финансы → Дт/Кт → кнопка «⬆ Акт НП» (рядом с «Додати»). Результат — в списке «Кредиторка — ми винні».

## 15.07.2026 — Объединение дублей: выбор что оставить и какие данные перенести

**Что попросили:** при объединении дублей нужно выбирать, какой контакт останется, а из остальных — галочками отметить, какие данные (имя, телефон, почта, ссылка на мессенджер, адрес и т.д.) перенести в него. Открывать контакты рядом в одном окне и там выбирать. Кнопка «Объединить» — это по сути сохранение.

**Что сделали:** кнопка «Объединить» теперь открывает **окно сравнения**. В нём все дубли показаны рядом (колонками), сверху точкой выбираешь, **кто останется** (получит все сделки, лиды и чаты). Ниже — таблица по каждому полю (Имя, Фамилия, Ник, Телефон, Email, Ссылка IG/TG/FB, Адрес, Дата рождения, ЄДРПОУ, IBAN, Комментарий): по каждому полю галочкой выбираешь, чьё значение взять. Мессенджер-каналы объединяются автоматически (из всех). Нажимаешь «Объединить» — выбранные данные сохраняются в оставшийся контакт, дубли удаляются.

**Где в системе:** «Дублі» → вкладка «Клієнти» → у группы дублей кнопка «Обʼєднати» → окно выбора.

## 15.07.2026 — CRM стала админ-панелью интернет-магазина

**Что попросили:** чтобы товары для сайта создавались только из номенклатуры CRM, пробники объединялись по четыре комплектации, у каждой были свои фото, а магазин не терял скорость.

**Что сделали:** в карточке товара появились разделы «На сайте», «Фото» и «Проверка». CRM хранит понятное название, описание, группу покрытия, конкретную комплектацию, цену, фото и разрешение публикации. Новое фото сначала нужно утвердить. Товар без SKU, цены, фото или полной группы из четырёх вариантов остаётся черновиком и не виден покупателю. Изменения передаются в магазин в фоне с повторными попытками; сам магазин хранит собственную копию товаров и продолжает работать, даже если CRM временно недоступна. Выключенный или вынесенный из папки товар скрывается без удаления истории.

**Где в системе:** Склад → Номенклатура → товар из папки «Тестові набори та викраски» → вкладки «На сайте», «Фото», «Проверка».

## 15.07.2026 — В чате можно выбрать, с какого канала и номера отвечать клиенту

**Что попросили:** в чате карточки сделки над кнопками «Закрепить / Переадресовать / Менеджер / Завершить» дать менеджеру выбор, от имени какого канала и номера писать клиенту. Также подключить номера Viber через E-chat в контакт-центре.

**Что сделали:** добавили строку «Відповідати через» со списком доступных каналов и конкретных номеров. Если у клиента есть телефон, CRM может начать отдельный Viber-диалог через выбранную E-chat-линию; повторный выбор использует тот же диалог и не создаёт дублей. Контакт-центр теперь хранит несколько E-chat-номеров отдельно: добавление нового номера не перезаписывает уже подключённый. Исправили блокировку запросов E-chat защитой Cloudflare и успешно включили API для уже сохранённой линии. API-ключи остаются скрытыми и менеджерам не показываются.

**Где в системе:** карточка сделки → чат с клиентом → выбор «Відповідати через» над кнопками; Контакт-центр → блок «Viber через e-chat.tech».

## 15.07.2026 — Кнопка «Приход» — только для ответственных за склад, не для отдела продаж

**Что попросили:** кнопка «Приход» была видна и у менеджеров (в карточке сделки и в карточке товара). Оприходовать товар должны только те, у кого есть на это право, а не отдел продаж.

**Что сделали:** кнопка «Приход» теперь показывается только сотрудникам с правом **«Приходные накладные» (warehouse.tab.receipts)** — и в карточке сделки, и в карточке товара. У менеджеров отдела продаж этого права нет, поэтому кнопки они больше не видят. Дополнительно закрыли это и на сервере: даже если попытаться создать приход в обход кнопки — система вернёт «Нет прав» (проверено: менеджер получает отказ, у кого право есть — проходит).

**Где в системе:** право выдаётся в «Співробітники і права» → группа «Склад» → «Приходные накладные». Кому нужно оприходовать — выдай это право.

## 15.07.2026 — Заказы сайта используют номенклатуру, менеджер сам забирает новую заявку

**Что попросили:** не назначать менеджера автоматически при создании заказа. Ответственным должен стать тот, кто первым перевёл новую заявку с первого статуса в работу. Тестовые наборы из сайта не должны попадать в сделку как выдуманный товар вне номенклатуры.

**Что сделали:** новые заявки интернет-магазина остаются без ответственного и видны менеджерам как общая очередь на первом статусе. Когда менеджер впервые двигает карточку дальше, CRM назначает ответственным именно его. Тестовый набор теперь приходит с точным SKU, находится в номенклатуре CRM и добавляется в сделку обычным складским товаром. Неизвестный SKU отклоняется до создания сделки.

**Где в системе:** Сделки → «23 Інтернет-магазин» → первый статус; товары заказа — в карточке сделки.

## 15.07.2026 — Телефония: надёжно слышно клиента на входящих + весь код телефонии в одном месте

**Что попросили:** снова не было слышно клиента на входящем звонке. «Почему каждый раз ломается телефония, мы её не трогали». Собрать весь код телефонии в один блок и задать правила, чтобы не ломалось.

**Что нашли:** живой звук звонка живёт только в одном файле (веб-телефон). «Лишних» строк в других местах нет — там аудио только для голосовых сообщений и записей разговоров, они звонок не трогают. Настоящая причина — гонка: программа начинала слушать «пришёл звук клиента» чуть позже, чем этот звук фактически приходил, и на входящих иногда его пропускала. Поэтому симптом плавающий — то слышно, то нет.

**Что сделали:** сделали привязку звука надёжной — теперь система забирает звук клиента сразу и несколькими путями (по событию, сразу при ответе и страховочной перепроверкой первые ~4 секунды разговора). Входящий звук больше не теряется. Плюс файл телефонии помечен как единственный «дом» звонков, чтобы будущие правки её не задевали.

**Где в системе:** веб-телефон (звонки прямо из браузера) — работает как раньше, просто входящий звук теперь стабильный.

## 15.07.2026 — Смена воронки: сделка сохраняет свою стадию, автоматизации не рвутся

**Что попросили:** при смене воронки сделка падала на первую стадию новой воронки. Нужно, чтобы сделку можно было перенести в другую воронку, а она осталась на той же стадии (на какой была) и все дальнейшие автоматизации продолжали работать.

**Что сделали:** теперь при смене воронки система ищет в новой воронке стадию с ТАКИМ ЖЕ названием и переносит сделку прямо на неё. Была «Оплату отримано» — останется «Оплату отримано» (воронки «21 Основний продукт» и «22 Тестовий набір» имеют одинаковые стадии, поэтому позиция полностью сохраняется). Склад и реализация повторно НЕ срабатывают (они уже отработали раньше), а будущие автоматизации — Новая Почта, оплаты, приоритеты ИИ-РОП — продолжают работать уже в новой воронке. Если в новой воронке нет такой же стадии (например переносишь в воронку с другими стадиями) — сделка встанет на первую стадию, и предупреждение об этом честно покажется перед переносом.

**Где в системе:** карточка сделки → выпадающий список воронки вверху → выбери другую воронку → подтверди перенос.

## 15.07.2026 — У интернет-магазина появилась отдельная воронка

**Что попросили:** не смешивать заказы сайта с рабочими воронками «21 Основний продукт» и «22 Тестовий набір».

**Что сделали:** создали отдельную воронку **«23 Інтернет-магазин»**. Теперь и пробники, и основные комплекты, оформленные на сайте, попадают только туда. Воронки 21 и 22, их сделки и клиенты не переносили и не изменяли. В новой воронке сохранены привычные этапы подтверждения, оплаты, комплектации и доставки.

**Проверка вживую:** защищённая связь сайта с CRM включена. Тестовый заказ `WV-260715-SBEOZ` создал ровно одну сделку №65732 с товаром Galateya, суммой 340 грн и данными доставки. Две повторные отправки вернули ту же сделку — дублей нет. После проверки заказ отменён, а сделка помечена «ТЕСТ — НЕ ОБРОБЛЯТИ» и перенесена в «НЕ АКТУАЛЬНО».

**Имя клиента:** если номер уже был в CRM с техническим именем «Гость/Гість/Guest», магазин заменяет только это служебное имя на имя из оформления заказа. Настоящее имя существующего клиента не перезаписывается.

**Где в системе:** CRM → Сделки → выбор воронки «23 Інтернет-магазин».

## 15.07.2026 — Товары «под заказ» (дроп): закупка сама догоняет продажу

**Что попросили:** есть товары, которые мы продаём под заказ и закупаем у поставщика УЖЕ ПОСЛЕ продажи (например «Побутовий вентилятор Вентс 100 Квайт»). Когда делаешь приход по реальной цене — эта закупочная цена должна автоматически подставиться и в карточку товара, и в сделку, и в движение товара (расход). Раньше расход показывал старую цену.

**Что сделали:**
1. В карточке товара (Склад → товар → «Змінити») появилась галочка **«Дроп (докуповуємо під замовлення)»**. Включи её для товаров, которые закупаешь после продажи.
2. Теперь, когда делаешь **Приход прямо из карточки сделки** (кнопка «Приход» в блоке товаров сделки) для дроп-товара — новая закупочная цена автоматически подставляется: в номенклатуру товара, в позицию именно этой сделки (даже если сделка уже закрыта) и в её движение расхода на складе. Маржа и прибыль пересчитываются на честную цену.
3. **Защита истории:** приход из карточки ТОВАРА (общий, не привязанный к сделке) НЕ переписывает старые закрытые сделки — там сохраняется цена на момент продажи. Обновляются только открытые сделки. Так старые продажи того же товара не искажаются.
4. Вентилятору Вентс 100 Квайт включили «Дроп» и поправили расход в сделке 65536 на реальную закупку 2151,75 ₴.

**Где в системе:** Склад → карточка товара → «Змінити» → галочка «Дроп»; кнопка «Приход» в блоке товаров карточки сделки.

## 15.07.2026 — Заказы собственного интернет-магазина передаются в CRM без дублей

**Что попросили:** закончить интернет-магазин на собственном Laravel-ядре и не потерять ни один заказ при передаче менеджерам.

**Что сделали:** добавили отдельный защищённый вход для сайта. Новый заказ создаёт клиента, сделку и товарные позиции; с 15.07.2026 все такие заказы направляются в отдельную воронку «23 Інтернет-магазин». Повторная отправка того же заказа не создаёт дубль. Если CRM временно недоступна, заказ остаётся в магазине и автоматически отправляется повторно.

**Где в системе:** CRM → Сделки; в карточке видны номер заказа сайта, доставка, товары, комментарий и источник рекламы.

## 14.07.2026 — Объединены воронки Алмазное+Вентиляция + новые категории

**Что попросили:** объединить воронки «Алмазное» (4) и «Вентиляция» (5); в доходах добавить Вентиляторы в товары и разбить услугу на Сверление/Монтаж/Резка.

**Что сделали:** воронки слиты в «4.С/Алмазне + Вентиляція» (323 сделки, стадии вентиляции добавлены, ничего не потеряно, воронка 5 удалена, бэкап /tmp/funnel_merge_backup.json). Категории: Товары +Вентиляторы; услуга «Сверление/Монтаж/резка» → три отдельные (Сверление/Монтаж/Резка), 209 старых транзакций остались на «Сверление».

## 14.07.2026 — Финансы клиента приведены в порядок (сводка)

За день доработали и выверили всю денежную логику по клиенту/сделке:
- **Минимальная цена за позицию** (напр. сверление 17/см, минимум 1500) + **% себестоимости** (мастеру 40/80/100) — задаются в карточке товара.
- **Скидка** на позицию — суммой ₴ или процентом (переключатель), и в чеке.
- **Доставка/транспорт** — отдельной позицией, 0 маржи.
- **Транзит-материалы** (другой магазин) — галочка в Витрате: кредиторка магазину + дебиторка клиента.
- **Прибыль** — без двойного счёта: услуги/работа считаются из журнала (реальная выплата мастеру), склад — из себестоимости.
- **Витрата (в карточке клиента)** = журнал + склад → Доход − Витрата = Прибыль.
- **Аванс** = Доход − сделки − материалы других магазинов (реальный остаток клиентских денег; минус = недоплата).
- **⚠️ Переплата мастеру** — плашка, если выплатили мастерам больше плана (план по услугам vs факт по журналу без материалов).
- **Долги по клиенту** — списки дебиторки/кредиторки под сделками (оплачено зелёным/перечёркнуто, не оплачено оранжевым).
- **В сделке** «Оплачено (за сделку)» — только в размере сделки, переплата отдельно как аванс.
- Убрана пустая сделка-дубль замера; замер учитывается в основной сделке.

**Где в системе:** Карточка клиента → блок «Финансы клиента» + «Долги по клиенту»; Карточка сделки → позиции и оплата; Карточка товара → минималка и % себестоимости.

## 14.07.2026 — Удаляешь платёж из журнала — он уходит и из сделки (исправлен привид)

**Что заметил Олег:** нажал «Принять оплату» на уже оплаченной сделке, случайно создал лишний платёж. Удалил его из журнала — а в сделке платёж с бейджем остался, «Оплачено» было раздуто.

**Почему было:** платёж в сделке и запись в журнале хранились раздельно — удаление журнала не убирало платёж сделки.

**Что сделали:** теперь это одно и то же. Удаляешь операцию в журнале — связанный платёж сделки удаляется автоматически, «Оплачено» пересчитывается, бейдж пропадает. Лишний платёж 6521,2 грн по сделке 65536 убрали.

**Где в системе:** Финансы → Журнал (или карточка клиента → операции) → удалить лишнюю операцию.

## 14.07.2026 — Удаляешь расход/кредиторку — транзит-дебиторка уходит сама (исправлен баг)

**Что заметил Олег:** удалил платёж (расход/кредиторку по материалам из другого магазина), а дебиторка клиента на 1000 грн осталась висеть.

**Почему было:** дебиторка клиента создавалась отдельно и не была «привязана» к своему расходу — поэтому удаление расхода её не трогало.

**Что сделали:** теперь транзит-дебиторка клиента жёстко привязана к источнику — к самому расходу (если оплатил сразу) или к кредиторке магазина (если «оплатим позже»). Удаляешь источник — дебиторка удаляется автоматически. Старые повисшие тестовые дебиторки (2 шт по 1000 грн, Богдан/Будмаркет) убрали.

**Где в системе:** Финансы → Дт/Кт. Работает и из журнала, и из карточки клиента.

## 14.07.2026 — Карточки платежа одинаковые везде + галочка «транзит-материалы»

**Что просили:** убрать отдельную кнопку «Оплата/долг»; сделать карточку платежа одинаковой в журнале и в карточке клиента; чтобы дебиторка по клиенту создавалась ТОЛЬКО за материалы из чужого магазина (а не когда платим мастеру за услуги из сделки).

**Что сделали:**
- Убрали кнопку «💳 Оплата/долг». Теперь всё в одной карточке платежа: доход/расход, «оплата сейчас / кредиторка», «нам должны / продаж-позика», перевод, валюта + курс НБУ. Карточка одинаковая и в журнале, и при оплате из карточки клиента.
- В расходе добавили галочку «🛒 Материалы клиента (транзит — другой магазин)». Ставишь её (указав контрагента-магазин и клиента-объект) → система сама создаёт дебиторку на этого клиента. Есть поле «Цена клиенту» (пусто = по закупке, впишешь больше = с наценкой).
- Галочку ставим ТОЛЬКО для материалов из магазина. Для оплаты мастеру / логистики — не ставим, чтобы долг клиента не раздувался.

**Где в системе:** Финансы → Журнал → «+ Доход/Расход»; и Карточка клиента → «+ Доход/Расход». Дебиторка видна в Дт/Кт и в карточке клиента. Закрытие — кнопка «Принять оплату» в сделке (галочки на долги).

**Дополнение (позже в тот же день):**
- Журнал теперь использует БУКВАЛЬНО ту же карточку, что и карточка клиента (один код, а не две похожие). Поэтому галочка «транзит-материалы», продаж/позика, привязка сделки, валюта — появились и в журнале тоже.
- Вернули удобный список категорий: главная категория жирным, подкатегории под ней с отступом, можно искать. При выборе категории сам подставляется фонд и направление. (Временно после объединения карточек список был плоским — исправили.)

## 14.07.2026 — Оплата, что закрывает и сделку, и транзит-материалы (другой магазин)

**Что попросили:** клиент одним платежом платит и за сделку, и за материалы, купленные для него в другом магазине (БудМаркет), которые на наш склад не заходят. Нужно распределять платёж и видеть, что клиент должен за эти материалы.

**Что сделали:** то, что клиент должен за транзит-материалы — это дебиторка (как и неоплаченная сделка). При «Принять оплату» появился блок «Клиент должен (закрыть этой оплатой)» — отмечаешь долги, часть платежа идёт на сделку, остаток закрывает эти долги (материалы становятся оплаченными, доход появляется в журнале). По складу не проводим — закупка видна из расхода магазина.

**Где в системе:** карточка сделки → «Принять оплату» → блок дебиторок клиента.

## 14.07.2026 — «Що нового» теперь показывает картинки (скриншоты в обучении)

**Что попросили:** чтобы обучающие карточки были со скриншотами из приложения — визуальное сопровождение к каждому шагу.

**Что сделали:** научили страницу «Що нового» показывать картинки. Теперь в карточку можно вставить скриншот (строка вида ![подпись](/whatsnew/файл.png)) — покажется изображение, клик по нему открывает в полный размер. Текстовые карточки не изменились. Скриншоты снимаем на тестовой сделке (без реальных клиентов) и складываем так, что они попадают и в GitHub.

**Где в системе:** страница «Що нового».

## 14.07.2026 — «Що нового» стал обучением: пошаговые инструкции по Финансам

**Что попросили:** чтобы блок «Що нового» был не коротким списком изменений, а обучением для новичка — по каждой сущности, пошагово, с каждой кнопкой и результатом, термины в скобках.

**Что сделали:** добавили 6 обучающих карточек по блоку **Финансы**, каждая — пошагово простым языком: как принять оплату по сделке (полная/предоплата/бронь), как завести дебиторку (клиент нам должен), как завести кредиторку (мы должны поставщику), как разносится платёж со сделки, как разделить платёж между направлениями, что такое аванс. Дальше так же распишем остальные блоки (Сделки, Склад, Чаты, Телефония, Права).

**Где в системе:** страница «Що нового» в CRM (открывается из меню).

## 13.07.2026 — ИИ-агент больше не ставит «Оплату отримано» без реальной оплаты

**Проблема:** несколько сделок перескакивали на стадию «Оплату отримано» после того, как менеджер отправил ссылку на оплату, — хотя клиент ещё не платил. Это делал ИИ-агент по переписке.

**Что сделали:** ИИ-агент теперь доводит сделку максимум до «Домовились про оплату». Стадию «Оплату отримано» (и дальше) ставит ТОЛЬКО реальная оплата — LiqPay-callback, приём оплаты в карточке или вручную. Добавлена жёсткая защита по названию стадии. Пострадавшие сделки (не оплаченные) вернули на «Домовились про оплату».

**Где в системе:** ИИ-агент / автоматизации, движение по воронке.

## 13.07.2026 — Себестоимость: алмазное 40%, тестовые наборы, закупка+маржа в карточке клиента

**1. Алмазное сверление/резка.** Мастерам за работу — 40%, значит закупка = 40% от цены. Проставили 40% на 28 алмазных работ (у которых её не было). Материалы (плёнка, крепёж) и демонтаж не трогали.

**2. Тестовые наборы.** Себестоимость набора теперь считается из его состава: количество из описания × себестоимость товара. Проставили расчётную закупку на 104 набора (маржа ~76-88%).

**3. Карточка клиента — закупка и маржа (только для ответственных).** В блоке «Сделки клиента» добавили столбец «Закупка» (себестоимость сделки), а под «Прибуток» — маржу (%). Всё это видят только те, у кого есть право на себестоимость (бухгалтерия/админ/назначенный ответственный). У обычных менеджеров этих цифр нет.

**Где в системе:** номенклатура (закупка алмазного/наборов) + карточка клиента → блок «Сделки клиента».

## 13.07.2026 — Подсказка сделок клиента при привязке платежа (и в журнале, и в карточке клиента)

**Что попросили:** чтобы при создании платежа не искать номер сделки вручную — если выбран клиент/контрагент, показывать список его сделок прямо в поле выбора. И сделать это и в журнале.

**Что сделали:** поле «Сделка» (в карточке платежа и в журнале) теперь: как только выбран клиент (объект) — по клику показывает список сделок именно этого клиента; можно выбрать одним нажатием. Если клиента нет под рукой — по-прежнему ищет по номеру или названию. Поле «Сделка» добавлено и в форму журнала.

**Где в системе:** карточка дохода/расхода и журнал → поле «Сделка» → выбрать клиента → всплывут его сделки.

## 13.07.2026 — В карточке платежа можно привязать сделку (поиск по номеру или названию)

**Что попросили:** если платёж (доход или расход) не привязан к сделке — дать возможность найти и привязать сделку прямо в карточке платежа, по названию или по номеру.

**Что сделали:** в окне операции появилось поле «Сделка (№ или название)» с поиском: вводишь номер (напр. 65536) или часть названия — выбираешь из списка. После сохранения платёж привязывается к сделке, и «Оплачено» сделки учитывает его (доход не задваивается — используется существующий механизм). Привязку можно снять крестиком.

**Где в системе:** карточка дохода/расхода → поле «Сделка (№ или название)».

## 13.07.2026 — Оплата сделки «из аванса клиента»

**Что попросили:** возможность оплатить сделку из уже полученных денег клиента (аванса) — например, замер оплатили раньше, а теперь эти деньги идут в счёт полной сделки.

**Что сделали:** в приёме оплаты сделки появился способ **«🏦 Из аванса клиента»**. При выборе:
— показывается доступный аванс клиента;
— «Оплачено» по сделке растёт, стадия двигается как обычно;
— НО новый доход и фискальный чек НЕ создаются (деньги уже получены раньше — не задваиваем);
— аванс клиента уменьшается на эту сумму.
Если аванса не хватает — система не даст провести и покажет, сколько доступно.

**Где в системе:** карточка сделки → «Принять оплату» → способ «Из аванса клиента».

## 13.07.2026 — Контрагент и Клиент (объект) в карточке платежа — оба из базы CRM

**Что попросили:** и «Контрагент», и «Клиент (объект)» в карточке платежа должны выбираться из единой базы CRM (как в журнале), а не вводиться свободным текстом.

**Что сделали:** в окне операции «Контрагент» теперь ищет по базе CRM (имя/телефон, можно создать нового клиента) — тот же компонент, что в журнале. «Клиент (объект)» тоже из базы. Больше нет расхождений вида «Богдан» / «Богдан +телефон».

**Проверено от имени обычного менеджера (Илоны):** счета, категории, направления, фонды, контрагенты, клиенты — всё грузится (по правам finance.view / finance.tx.edit). Функционал у сотрудников с доступом работает.

## 13.07.2026 — В карточке платежа появился выбор «Клиент (объект)»

**Что попросили:** в окне операции (доход/расход) из карточки клиента нельзя было выбрать «Клиент (объект)», как в журнале — поэтому платёж нельзя было привязать к нужному клиенту.

**Что сделали:** добавили поле «Клиент (объект)» с поиском по имени/телефону (тот же компонент, что в журнале). Теперь любую операцию можно привязать к клиенту прямо из карточки платежа.

## 13.07.2026 — Удаление сделки прямо из карточки клиента

**Что попросили:** в карточке клиента в блоке «Сделки клиента» — возможность удалить сделку прямо в строке, с проверкой права (если есть право удалять сделки — доступно и тут, как везде).

**Что сделали:** в каждой строке сделки появилась корзинка 🗑 — удаляет сделку (с подтверждением). Кнопка видна только тем, у кого есть право «удаление сделки» (deal.delete) — то же право, что в карточке сделки и при массовом удалении. Без права кнопки нет, и сервер тоже не даст удалить.

**Где в системе:** карточка клиента → блок «Сделки клиента» → 🗑 в строке сделки.

## 13.07.2026 — Фонды теперь подтягиваются у менеджеров (не только у управляющих)

**Проблема:** у менеджера (например, Илоны) в карточке дохода/расхода не подтягивался список «Фонд (статья)». Причина — список фондов требовал право «управление финансами», которого у менеджера нет.

**Что сделали:** читать список фондов теперь может любой, кто работает с финансами или создаёт доход/расход (как уже было с «Направлениями»). Редактирование самих фондов осталось только у управляющих. Фонды теперь видны у всех менеджеров в выпадающем списке.

**Где в системе:** карточка дохода/расхода → поле «Фонд (статья)».

## 13.07.2026 — В карточке дохода/расхода подтягиваются контрагенты (автоподбор)

**Что попросили:** в окне «+ Доход»/«+ Расход» из карточки клиента поле «Контрагент» не подсказывало уже существующих контрагентов (в отличие от журнала).

**Что сделали:** добавили автоподбор — при вводе в поле «Контрагент» выпадают уже заведённые контрагенты/клиенты (тот же список, что в журнале). Меньше опечаток и дублей.

## 13.07.2026 — Карточка дохода/расхода в карточке клиента = зеркало журнала (появился выбор «Кредиторка/Дебиторка»)

**Что попросили:** в карточке клиента при «+ Доход»/«+ Расход» не было выбора «Кредиторка» (мы должны) / «Дебиторка» (нам должны), как в журнале. Нужно, чтобы окно было точь-в-точь как журнальное — и для дохода, и для расхода.

**Что сделали:** добавили в это окно переключатель:
— для расхода: «💸 Оплата сейчас» (сразу в журнал) / «🕐 Кредиторка (оплатим позже)» — попадёт в Дт/Кт как долг, а в журнал — после кнопки «Оплачено»;
— для дохода: «💰 Деньги пришли» / «🕐 Нам должны (дебиторка)».
Клиент подставляется автоматически. Работает так же, как в журнале.

**Где в системе:** Карточка клиента → «+ Доход» / «+ Расход» → переключатель под полем «Направление».

## 11.07.2026 — Починили звук на входящих звонках (менеджер не слышал клиента)

**Проблема:** на входящих звонках менеджер не слышал клиента (клиент менеджера слышал). Исходящие и приём звонка работали.

**Причина:** накануне веб-телефон переделали так, что во время звонка окошко «переезжает» в отдельный слой поверх страницы. Но вместе с ним переезжал и невидимый аудио-элемент, через который идёт звук. На входящем звонке звук успевал прицепиться ДО переезда — и в момент переезда терялся. На исходящем окошко переезжало заранее, поэтому там звук был.

**Что сделали:** вынесли аудио-элемент из «переезжающего» окошка — теперь он всегда на месте и звук не теряется, независимо от того, входящий звонок или исходящий.

**Где в системе:** веб-телефон в CRM (правый нижний угол), входящие звонки.

## 11.07.2026 — В карточке клиента можно сразу создать перевод между счетами

**Что попросили:** в карточке дохода/расхода (которая открывается прямо в карточке клиента) не было возможности сделать «перевод между счетами».

**Что сделали:** вверху окна операции добавили переключатель **💰 Доход / 💸 Расход / ⇄ Перевод**. Если выбрать «Перевод» — показываются два счёта (откуда → куда) и поле «зачислено получателю» (на случай разной валюты). Перевод не считается ни в доход, ни в расход — просто движение денег между кассами. Для уже созданной операции по-прежнему работает кнопка «Переделать в перевод».

**Где в системе:** Карточка клиента → «+ Доход» / «+ Расход» → переключатель вверху окна.

## 11.07.2026 — Карточка дохода/расхода открывается прямо в карточке клиента

**Что попросили:** когда в карточке клиента жмёшь «+ Доход» или «+ Расход» — не перебрасывать в Журнал, а открывать точно такое же полное окно операции прямо тут, с уже подставленным клиентом.

**Что сделали:** кнопки «+ Доход» / «+ Расход» теперь открывают полную карточку операции поверх карточки клиента (не уводят в журнал). В ней всё как в журнале: сумма, дата, категория, контрагент, счёт, фонд, направление, канал, комментарий, чек/документы, «переделать в перевод», а также распределение суммы между подразделениями (кто заплатил → остальным долг). Клиент подставляется автоматически. Кнопка «💳 Оплата/долг» осталась отдельно — для быстрой дебиторки и оплаты по сделке.

**Где в системе:** Карточка клиента → блок «Финансы» → «+ Доход» / «+ Расход».

# История изменений Wallcov-CRM

> **Что это.** Живой журнал всех доработок CRM простым языком — без технических терминов.
> Каждый раз, когда Олег просит что-то добавить или изменить, сюда дописывается запись:
> **дата → что попросили → что сделали → где это в системе**.
>
> **Как пополняется.** Автоматически при каждой доработке (правило в рабочем протоколе Claude).
> Документ лежит в репозитории `mgvipt/-` (папка `crm-platform/docs/`) и после каждого коммита
> сам заливается на GitHub — то есть история всегда сохранена и её не потерять.
>
> Новые записи добавляются **сверху** (последнее — первым).

---

## 2026-07-11

**Приложение «Wallcov Замер»: проекты больше не пропадают + кнопка «На главную».**
- Раньше замеры («проекты») хранились только на телефоне и стирались, если приложение переустановить. Теперь каждый проект дублируется на сервер и привязывается к устройству — после переустановки или обновления приложения проекты сами восстанавливаются.
- Добавлена кнопка «На главную» на экране замера — быстро вернуться к списку.


**Приложение «Wallcov Замер»: правильный расчёт материалов по эффектам.**
- Раньше в приложении был упрощённый расчёт (одно покрытие). Теперь приложение берёт настоящий прайс из твоей Google-таблицы (расход кг/м² и цена) — правишь таблицу, приложение обновляется само (раз в сутки).
- Появились «эффекты»: выбираешь эффект — приложение само подставляет весь состав (грунт глубокого проникновения + база/грунт + декор) и считает, сколько нужно и сколько стоит на твою площадь. Клиенту так проще; мастер может собрать и вручную.
- Инструменты (валики, шпатели, кельмы) — отдельным списком из каталога CRM, добавляются кликом.
- Технически: новый адрес `/api/pricelist/` отдаёт эффекты, материалы и инструменты.


**Приложение «Wallcov Замер»: клиенты теперь становятся лидами.**
- В приложении для замера стен добавили выбор на входе: «Я делаю ремонт у себя» (клиент) или «Я мастер/сотрудник».
- Когда клиент оставляет имя и телефон и отправляет свой замер — в CRM автоматически создаётся **лид в воронке «Лиды»** с его контактом и замером (сколько м² покрытия нужно, размеры стен, длина по полу, площадь пола).
- Учётные записи не создаются — клиент просто попадает в CRM как лид, менеджер видит замер прямо в карточке. Повторная отправка в течение 10 минут не плодит дубли.
- Мастера как и раньше отправляют замер в сделку воронки «Покрытия для стен».


**Прибыль по клиенту в карточке.**
- В карточке клиента строка «Прибуток по клієнту»: Выручка − Себестоимость склада − Закупки/услуги = Прибыль.
- Складские товары считаются из сделок (себестоимость авто при отгрузке), товары под заказ и услуги мастеров — из журнала. Без двойного счёта.
- Правило: складские товары — в сделку; товары под заказ (закупаем в другом магазине) — только в журнал (доход от клиента + расход/кредиторка магазину).

**Оплата клиента через журнал → попадает в «Оплачено» сделки.**
- В карточке клиента у «+ Дохід» появился выбор сделки. Привязал доход к сделке — платёж попадёт в «Оплачено» сделки и стадия двинется вперёд (как при обычном приёме оплаты). Работает и из журнала.
- Показывается остаток по сделке; если сумма не совпадает — предупреждение: меньше = частичная (внесёшь остаток позже), больше = переплата.
- Защита от двойного счёта: платёж, уже принятый через сделку, журнальный доход не задваивает. Расходы и импорты не затрагиваются.

---

## 2026-07-10 (вечер)

**Фильтры операций + «Купив у нашому магазині».**
- В блоке операций рядом с «Показувати» добавлены фильтры: по «Кому/від кого», по «Клиент (объект)» и по периоду (дата от — до). Пересчитывает список и плитки Доход/Расход.
- Число вверху карточки переименовано в «Купив у нашому магазині (угоди)» — чтобы не путать с расходами по объекту.

**Столбец «Клиент (объект)» + все платежи контрагенту.**
- В операциях виден клиент-объект, к которому относится платёж (может отличаться от карточки, где находишься), кликабельно.
- Карточка поставщика (напр. Будмаркет) теперь показывает ВСЕ платежи ему, и напротив каждого — объект/мастер, кому реально пошли деньги.

**Столбцы «Кому/від кого» и «Сделка» в операциях.**
- В блоке операций видно, кому оплачено (расход) или кто оплатил (доход), и к какой сделке привязана операция.

**Удаление операций + шапка таблицы в карточке клиента.**
- В блоке операций теперь есть шапка с названиями столбцов (Дата / Сумма / Категория / Сделка), названия ровно над данными.
- Операцию можно удалить прямо в строке (корзина) или массово галочками («Удалить выбранные»), с подтверждением. Доступно тем, у кого право на финансы.

**Карточка операции открывается на месте.**
- Теперь клик по доходу/расходу в карточке клиента открывает карточку операции **прямо тут (модалкой)**, без перехода в журнал. Все поля видно и можно редактировать/удалить, перейти в сделку.

**Страница «Что нового» прямо в CRM.**
- В меню слева добавлен пункт «Що нового» — заходишь и читаешь всю историю доработок простым языком, не выходя из CRM. Копия по-прежнему на GitHub.

**Доходы и расходы в карточке клиента стали кликабельными.**
- Клик по любой операции открывает ту же карточку, что и в Журнале (посмотреть/изменить). Видно, к какой сделке привязана операция — по номеру переход в сделку.

**Выравнивание заголовков таблиц.**
- По всему CRM поправил шапки списков, где заголовки «уезжали» от значений (сумма теперь над суммой, стадия над стадией) — карточка клиента, аналитика, финансы.

---

## 2026-07-10

**Карточка клиента — история операций стала полноценной.**
- Внизу карточки теперь можно выбрать, сколько строк показывать (15 / 50 / 100 / 200 / 500), список прокручивается, а если операций больше — переключаются страницы (у Будмаркета, например, 595 операций — 12 страниц).
- В каждой строке расхода видно, **к какой сделке** он относится (если это объект). По номеру сделки можно кликнуть и сразу перейти в её карточку.
- Где: карточка любого клиента → блок «Фінанси клієнта».

**Дебиторка «нам винні» (нам должны).**
- В карточке клиента добавлена плитка «Дебіторка (нам винні)» — видно, сколько клиент нам должен.
- Завести долг можно прямо из карточки: у кнопки «+ Дохід» выбор «💰 Гроші прийшли / 🕐 Нам винні (дебіторка)».
- При дебиторке выбирается характер: **🛒 Продажа** (клиент должен за товар — это будущая прибыль) или **🤝 Заём** (я дал свои деньги в долг — возврат НЕ прибыль, прибыль только проценты; такой человек — заёмщик/должник). Система считает их раздельно, чтобы займы не раздували прибыль.

**Контрагент стал кликабельным + фильтр.**
- В Журнале и во вкладке «Дебет/Кредит» имя контрагента — ссылка: клик открывает карточку клиента.
- В «Дебет/Кредите» появился фильтр по контрагенту (поиск сверху).

**Телефония доведена под ключ** (несколько дней работы).
- Оба входящих номера (Алмаз и Салон) звонят менеджерам в CRM, окно звонка по центру экрана с данными клиента (кто звонит, сколько потратил, последняя сделка).
- Голос слышно быстро и чётко, гудок прекращается при поднятии трубки.
- Звонок приходит только на одну активную вкладку (не дублируется), исходящие тоже показываются по центру.
- В правах телефонии сделаны отдельные доступы: баланс линий, очередь, входящие/исходящие, «видеть все звонки или только свои».

**Мелочи этого периода:** убрано мигание блоков при обновлении списков; в аналитике скрываются воронки, к которым у сотрудника нет доступа; починена привязка чата к карточке канбана; адресная доставка Новой Почты (список улиц) заработала.

---

## Ранее (крупные блоки, лето 2026)

- **Единый денежный контур** (начало июля): аналитика считается по дате операции, средневзвешенная себестоимость, наборы товаров (разузлование), наложка Новой Почты автоматически создаёт доход, полная синхронизация товаров и разделов из Битрикса.
- **Банки** (начало июля): развязка дублей LiqPay (отдельный счёт), защита от повторной заливки выписок, подключение банков через настройку (Приват готов, Моно ждёт токен).
- **Полный перенос из ФинМапа** (2021–2026): весь реестр операций и балансов заведён в CRM с подкатегориями и дедупликацией.
- **Финансовая структура:** связка категория → фонд → направление (автоподстановка), чистые названия категорий, канон контрагентов, гранулярные права на вкладки и объекты финансов.
- **Дубли:** страница «Дублі» — объединение контактов и сделок по телефону/почте/соцсети/полному имени.
- **Склад:** учёт отгрузок/приходов, инвентаризация в деньгах, защита от двойного списания, вкладка аналитики прибыли.
- **Продажи и чаты:** AI-аналитик продаж (коуч), открытые линии с категориями и приоритетами, мультиканальность (Instagram/Telegram/Facebook), звуковые уведомления.
- **KPI менеджеров:** оклад + % оборота + плавающий % маржи, планы, синхронизация с точкой безубыточности.
- **Оплаты без Битрикса:** нативная оплата сделок (LiqPay, чеки Checkbox, реальная ТТН Новой Почты, авто-стадии).

*(Полная детальная история по каждому блоку — в памяти Claude и в `docs/WORKLOG.md`.)*
## 14.07.2026 — Один клиент = один лид, даже если написал в Direct или комментарии

**Что попросили:** комментарий или сообщение из любой открытой линии должно попадать в CRM, но существующему клиенту нельзя создавать ещё один лид.

**Что сделали:** новый контакт из ChatPlace/Instagram, Telegram, Viber и других открытых линий получает один лид. Если CRM уже знает контакт или у него уже есть лид, новое сообщение только открывает/обновляет его диалог — без дубля. Комментарий, который ChatPlace переводит в Direct, идёт по той же проверке.

**Где в системе:** CRM → Чаты / Лиды. Существующая карточка клиента сохраняет историю обращений из всех каналов.
## 15.07.2026 — Телефонию зафиксировали как отдельную стабильную систему

**Что попросили:** прекратить повторные поломки звонков после других изменений и сделать понятный безопасный откат.

**Что сделали:** телефония теперь выпускается отдельно от обычных обновлений CRM. Закрепили точную рабочую версию; внутренний адрес для звука подставляется сам после перезапуска; проверка смотрит не только «запущено», но и связь с телефонным сервером, входящий/исходящий маршрут и звуковой адрес. Перед любым выпуском делается закрытая копия и сохраняется предыдущая рабочая версия. Если идёт разговор, обновление само останавливается и ждёт.

**Где в системе:** веб-телефон в CRM. После выпуска связь с линиями активна, рабочие телефоны 701–704 подключены, CRM работает.
## 15.07.2026 — Инструкция интернет-магазина и контроль синхронизации в Пульте

**Что попросили:** зафиксировать в CRM понятную инструкцию, как номенклатура управляет интернет-магазином, и вывести общую картину в Командный пункт.

**Что сделали:** в разделе «Инструкции и что нового» добавлена пошаговая инструкция для менеджера: где заполнять карточку сайта, как выбирать одну из четырёх комплектаций пробника, утверждать фото, проходить проверку и публиковать. В `pult.wallcov.com.ua` появился живой блок «CRM ↔ интернет-магазин»: количество товаров и групп, черновики, варианты, очередь передачи, ошибки, незаполненные фото/цены/комплектации и состояние CRM, магазина, MySQL и Redis. Показатели читаются автоматически и не требуют ручного обновления справочника.

**Где в системе:** CRM → «Инструкции и что нового» → блок «Интернет-магазин»; Командный пункт → «Обзор» → блок «CRM ↔ интернет-магазин».
## 15.07.2026 — Первый товар полностью подготовлен и опубликован из CRM

**Что попросили:** продолжить запуск интернет-магазина: заполнить первую настоящую группу пробников, добавить четыре комплектации с фотографиями и проверить публикацию.

**Что сделали:** первой подготовлена группа **Velvet Luna**. В CRM заполнены понятное название, описание, преимущества, помещения, SEO и состав каждой комплектации. Для четырёх SKU добавлены и утверждены отдельные изображения: без дощечки/без тонирования, без дощечки/с тонированием, с дощечкой/без тонирования, с дощечкой/с тонированием. После проверки все четыре варианта включены для сайта и группа опубликована. Дополнительно исправлено отображение статуса: теперь «Опубликован» или «Черновик» сразу показывается у всех четырёх SKU группы, а старый технический адрес карточки заменяется понятным адресом из CRM. При первой live-проверке обнаружили несовпадение названий полей комплектаций между синхронизацией и страницей товара; карточка возвращала ошибку. Поля приведены к единому формату, добавлен тест публичной страницы — карточка и корзина работают.

**Где в системе:** Склад → Номенклатура → товары Velvet Luna → вкладки «На сайте», «Фото», «Проверка»; магазин → карточка пробника Velvet Luna; Пульт → «Обзор» → «CRM ↔ интернет-магазин».
## 16.07.2026 — Одна карточка товара для номенклатуры и интернет-магазина

**Что попросили:** не разделять товары CRM и товары сайта. В обычной карточке номенклатуры должны быть все настройки магазина, а раздел «Интернет-магазин» должен показывать только выбранные позиции и открывать ту же карточку на месте.

**Что сделали:** вкладки «На сайте», «Фото» и «Проверка» теперь доступны у каждого товара. В карточке можно включить показ на сайте, выбрать путь категории любой глубины, заполнить название, описание, тип товара и комплектацию. Во вкладке «Интернет-магазин» остаются только товары с включённым показом; карточка открывается справа без перехода в складской каталог и редактирует ту же запись номенклатуры. Обычный товар публикуется одной позицией, а четыре комплектации обязательны только для пробника.

**Где в системе:** «Складской учёт» → любой товар → «На сайте»; либо «Интернет-магазин» → «Открыть карточку».
## 18.07.2026 — Главную страницу магазина теперь можно менять из CRM

**Что попросили:** дать понятный доступ к каждому блоку главной страницы, текстам, кнопкам, фотографиям и подвалу; добавить аналитику сайта и убрать неприятный коричневый цвет.

**Что сделали:** в «Интернет-магазине» появились отдельные вкладки «Главная и дизайн» и «Аналитика сайта». В редакторе каждый блок открывается отдельно: можно изменить заголовки, тексты, ссылки кнопок и фотографии, скрыть блок, сохранить черновик без изменений на сайте и затем отдельно опубликовать. Перед подвалом теперь светлый шалфейно-молочный блок с тёмно-зелёными кнопками. Аналитика показывает посещения, просмотры товаров, корзины, оформление, заказы, выручку и источники переходов. Магазин хранит опубликованную копию у себя, поэтому скорость сайта не зависит от CRM.

**Где в системе:** CRM → «Интернет-магазин» → «Главная и дизайн» или «Аналитика сайта».
# 2026-07-19 — Управление сайтом перенесено из CRM на сам сайт

**Что попросили:** убрать из CRM админку и аналитику сайта, не затронув продажи и остальную работу CRM.

**Что сделано:** скрыт отдельный раздел «Интернет-магазин». Технический обмен товарами и заявками сохранён. Управление сайтом теперь находится в защищённом кабинете `wallcov.com.ua/admin`.

**Где в системе:** CRM больше не показывает отдельный пункт магазина; заказы с сайта продолжают поступать в CRM обычным способом.
