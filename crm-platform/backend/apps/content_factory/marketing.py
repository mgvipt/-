"""ШІ-SMM-стратег і маркетолог контент-заводу (25.09.2026).

Одна «голова» для текстів: знає модель продажів Wallcov, цільову аудиторію, шлях від маркетингового фундаменту до росту,
офіційні правила Instagram (platform_rules) і перевірені практикою неофіційні прийоми (позначені). Мета — більше грошей
через безкоштовну або найдешевшу релевантну аудиторію. Факти про продукт — лише з бази знань блогу / CRM.
Тексти пише Claude Sonnet з коротким виходом (≈$0.005–0.015 за текст).
"""
from . import blogs
from .reels import _clean_caption, clean_text

SOURCE = "content_factory.write"

WALLCOV_FUNNEL = """МОДЕЛЬ ПРОДАЖІВ WALLCOV (з CRM, факти):
- Шлях клієнта: рилс/карусель/реклама в Instagram і TikTok → коментар з кодовим словом або Direct → CRM сама пише в Direct
  (матеріал, ціна «від», сторінка кольорів + реальні фото) → ШІ-консультант веде діалог → ТЕСТ-НАБІР (дощечка 40×40,
  можна з тонуванням) → основне замовлення на кімнату/обʼєкт → оплата LiqPay або накладений платіж → доставка Новою поштою або салон.
- Головний вхід у продаж — тест-набір: людина пробує фактуру й колір у себе вдома. Контент має вести до кодового слова/Direct
  і знімати страхи до тест-набору (шви, витрата, чи впорається сама, чи підійде основа, колір при різному світлі).
- Швидкість відповіді вирішує: відповідь до 15 хв дає ≈17,5% оплат проти ≈4–5% після години — заклик у Direct має обіцяти швидку відповідь.
- Аудиторія: переважно жінки 28–50, роблять ремонт у квартирі/будинку й самі обирають матеріал; окремо — майстри-оздоблювачі
  й дизайнери інтерʼєру (партнери, повторні замовлення).
- Сильні аргументи: український виробник, реальні обʼєкти, фактура «як на фото», підбір кольору, розрахунок витрати під площу."""

GROWTH = """ШЛЯХ РОСТУ (від фундаменту до масштабу):
1) Фундамент: одна ЦА, одна головна обіцянка блогу, 3–5 контент-рубрик, заклик, що веде до грошей (кодове слово → Direct → тест-набір).
2) Охоплення нових людей безкоштовно: рилси з сильним гачком у 3 с і досмотром; контент, який ЗБЕРІГАЮТЬ і ПЕРЕСИЛАЮТЬ
   (чеклисти, порівняння, «помилки», «як обрати»); ключові слова в першому рядку підпису (пошук Instagram).
3) Довіра: реальні обʼєкти, до/після, відгуки клієнтів (UGC), відповіді на часті питання, обличчя власника/майстра.
4) Конверсія: одна дія на пост; кодове слово в коментар (його ловить автоматика) або Direct; нагадування про тест-набір.
5) Масштаб: те, що вистрілило органічно, — у рекламу (найдешевша релевантна аудиторія), повтор формату серією.
ПЕРЕВІРЕНІ НЕОФІЦІЙНІ ПРИЙОМИ (неофіційно — перевірено практикою, не правило платформи):
- Серії й рубрики з однаковим оформленням — людина повертається по продовження.
- Колаборації (Collab) з дизайнерами й майстрами — чужа аудиторія безкоштовно.
- Пробні рилси (Trial Reels) для перевірки нових гачків без ризику для основної стрічки.
- Відповідати на коментарі в перші години й перетворювати часте питання на окремий ролик.
- Той самий ролик — у TikTok і Shorts з адаптованим текстом (не дубль з водяним знаком)."""

WRITER_TASK = """Ти — SMM-стратег і маркетолог цього блогу. Напиши текст за ідеєю власника для вказаного формату й платформи.
Мета тексту — наблизити до грошей за моделлю продажів: безкоштовне охоплення релевантних людей → довіра → одна дія.
Правила: перший рядок — гачок із ключовими словами теми; далі конкретика короткими абзацами; одна дія в кінці з причиною;
для Instagram — 3–5 доречних хештегів у кінці (не більше 5); для Telegram — без хештегів, можна посилання з заклику блогу;
для TikTok — коротко (до 300 символів) + 3–4 хештеги; для YouTube Shorts — назва до 60 символів з ключовим словом + 2–3 речення опису.
Факти й цифри — лише з бази знань; немає — не пиши цифру. Пиши мовою блогу. Якщо дано поточний текст — покращуй його, зберігаючи зміст і намір.
Відповідай ЛИШЕ JSON: {"text":"...","hooks":["ще 2 варіанти першого рядка"],"why":"одним реченням: як текст працює на мету"}"""

FORMATS = {"caption": "підпис до каруселі/поста Instagram", "reel": "підпис до рилса", "telegram": "пост у Telegram-канал",
           "tiktok": "опис до відео TikTok", "youtube": "назва й опис YouTube Shorts"}


def foundation(blog):
    """Маркетинговий фундамент для промптів: для брендів з базою CRM — модель продажів Wallcov + шлях росту."""
    return (WALLCOV_FUNNEL + "\n\n" if blog.use_crm_kb else "") + GROWTH


def write(blog, idea, fmt="caption", current="", call=None):
    blogs.require_ready(blog)
    idea = (idea or "").strip()
    if len(idea) < 5 and not current.strip():
        raise ValueError("Опишіть ідею хоча б кількома словами.")
    facts_text, titles = blogs.facts_block(blog, idea or current)
    system = blogs.system_for(blog, WRITER_TASK + "\n\n" + foundation(blog))
    prompt = (f"Формат: {FORMATS.get(fmt, FORMATS['caption'])}\nІдея: {idea or '(покращити поточний текст)'}\n"
              + (f"Поточний текст:\n{current[:2500]}\n" if current.strip() else "")
              + f"\nБаза знань:\n{facts_text or '(немає)'}\n\n{blogs.memory_block(blog)}")
    if call is None:
        from apps.crm.ai import claude_json
        call = lambda p: claude_json(p, model="claude-sonnet-4-6", max_tokens=1100, system=system, source=SOURCE)
    try:
        r = call(prompt) or {}
    except TimeoutError:
        r = call(prompt) or {}
    text = _clean_caption(str(r.get("text") or ""))
    if not text:
        raise ValueError("ШІ не повернув текст — спробуйте ще раз.")
    return {"text": text, "hooks": [clean_text(str(h))[:200] for h in (r.get("hooks") or [])][:3],
            "why": clean_text(str(r.get("why") or ""))[:300], "facts": titles[:8]}
