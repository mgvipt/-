{
  "name": "cycle-vdvoem-server",
  "version": "1.0.0",
  "description": "Backend proxy for Цикл вдвоём — AI usage metering, limits, subscriptions and credits (margin over LLM cost).",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^4.19.2"
  }
}
