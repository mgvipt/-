{
  "name": "cycle-vdvoem-mobile",
  "private": true,
  "version": "1.0.0",
  "description": "Capacitor iOS wrapper for the Цикл вдвоём PWA",
  "scripts": {
    "sync": "cap sync",
    "open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
