# AGENTS.md — обязательные правила для ЛЮБОГО агента (Codex, Claude) в этом репо

Прод Wallcov-CRM живёт ЗДЕСЬ: Netcup `159.195.136.213`, `/root/gmideas`, ветка `codex/webchat-landing-crm`.
Деплой: **ТОЛЬКО `cd /root/gmideas/crm-platform && ./deploy.sh`** (варианты: `./deploy.sh web` — бэкенд, `./deploy.sh caddy` — фронтенд). ⚠️ Аргумент читается ОДИН: `./deploy.sh web caddy` = только бэкенд.
Руками `build && up -d` НЕ деплоить: deploy.sh прогоняет смоук 33 ключевых функций (backend/smoke_all.py) НА НОВОМ ОБРАЗЕ до переключения — если хоть одна красная, прод не трогается и продолжает работать старая версия. Бэкенд запечён в образ, `restart` НЕ хватает.

## 🧪 Смоук-набор — правило пополнения

Файл `crm-platform/backend/smoke_all.py` — список проверок всех видимых функций (владелец + менеджер Илона id=2).
**Добавил новую видимую функцию / новый экран / новый эндпоинт → добавь строку в CHECKS в том же коммите.**
Проверки только читающие (GET). Смоук уже ловил реальную поломку: 29.08 нашёл NameError в /api/finance/overview/
(сломан коммитом 1dd5c7c от 15.08 — массовая замена таймзоны задела 3 места, включая учёт рабочего дня).

## 🔴 ГЛАВНОЕ ПРАВИЛО: не затирать чужую работу

**Инцидент 26.08.2026 (реальный, не гипотеза):** 25.08 вечером был сделан редизайн вкладки «Маркетинг»
(коммиты `458231c` UI v2 + `ce45b79` UI v3 «таблица показателей»). Утром 26.08 другая сессия добавляла
тултипы (`daec3a3`) и переписала `crm-platform/frontend/src/pages/MetaMarketing.tsx` из СВОЕЙ УСТАРЕВШЕЙ
копии (состояние до v2/v3): −95 строк, весь вчерашний дизайн снесён. Git это принял как обычный коммит —
никто не заметил, пока Олег не открыл вкладку. Механизм: долгая сессия держит копию файла в контексте,
параллельная сессия коммитит своё, первая пишет файл целиком поверх → «тихий откат».

## ✅ Чек-лист ПЕРЕД правкой любого файла (по порядку, без пропусков)

1. `git log --oneline -10 -- <файл>` — кто и когда трогал ЭТОТ файл. Коммиты за последние 48 часов =
   файл «горячий», в нём свежая работа другой сессии. Прочитай эти коммиты (`git show <sha> --stat`).
2. `git log --oneline --since='2 days ago'` — что вообще делалось в репо параллельно.
3. **Перечитай файл С ДИСКА непосредственно перед правкой.** Копия из контекста сессии устаревает.
   Никогда не восстанавливай файл «по памяти сессии».
4. Только ТОЧЕЧНЫЕ правки (edit конкретных строк). Полная перезапись файла (write целиком, scp, `cat >`)
   ЗАПРЕЩЕНА на проде.
5. **Перед коммитом: `git diff --stat`.** Если удаляется больше строк, чем требует твоя задача
   (делал «добавить тултипы», а diff показывает −95) — СТОП: ты сносишь чужую работу.
   Влей своё изменение В свежую версию файла, а не свою версию поверх.
6. Фронт делает PATCH модели → проверь, что ВСЕ поля есть в serializer (иначе PATCH молча затирает
   значения; кейс: привязка contact в карточке Дт/Кт, фикс `6ea9dce`).
7. Данные в БД: массовые операции только DRY_RUN → тест на 1-2 записях → live. Денежные записи
   (contact_id/deal_id платежей) НЕ перевешивать ради отображения. Бэкапы: `/var/backups/crm/daily/`.
8. В сообщение коммита добавляй метку агента: `[codex]` или `[claude:<сессия>]` — git-автор у всех
   один («Wallcov CRM»), без метки не видно, кто сделал коммит.
9. Запись в `crm-platform/docs/ИСТОРИЯ-ИЗМЕНЕНИЙ.md`: дата = СЕГОДНЯШНЯЯ (проверь `date`!), не дата
   из головы сессии. 26.08 записи ошибочно подписывались «19.08» — это тоже след устаревшего контекста.

## Проверка «ничего не потеряли» после серии правок

- `git log --stat --since='2 days ago' -- <ключевые файлы>` — искать большие удаления в «маленьких» коммитах;
- `git diff <последний-одобренный-Олегом-коммит> HEAD -- <файл>`;
- дата образов `docker images | grep crm-platform` должна соответствовать времени HEAD.

## Ручные изменения Олега всегда приоритетны. Конфликт не решать перезаписью.


## 🔴 ИНВАРИАНТ: дедуп исходящих IG/Meta (НЕ ломать — задвоение уже возвращалось 22.08 и 29.08)
Одно логическое исходящее = ОДНА запись Message. Instagram/Meta присылает «echo» нашего же отправленного сообщения обратно (webhook message_echoes) — его НЕЛЬЗЯ записывать как новое.
- send_message пишет `external_id = ext_id`, где ext_id от ChatPlace ≠ Meta-mid; echo всегда несёт Meta-mid. Поэтому сверка ТОЛЬКО по id (external_id/meta_external_id == mid) не ловит ChatPlace-отправку и window_risk-отправку без id → дубль (наше «director/Олег» + echo «ai_assistant/Юля (AI)»).
- В `apps/inbox/meta.py` (echo-ветка) ОБЯЗАТЕЛЬНА текстовая сверка ПЕРЕД созданием сообщения: если is_echo и есть mid — найти наше исходящее по тексту (direction=out, тот же text, meta_external_id пустой, за 10 мин) и доклеить mid (+снять window_risk→sent), затем continue. НЕ удалять эту проверку при рефакторингах echo/дедупа.
- Один IG-аккаунт валится в 2 канала (ChatPlace #1 = ИИ Юля, Meta #12 = зеркало) — не выключать, но следить чтобы echo зеркала не задваивал ответ ИИ.

## 🔴 ИНВАРИАНТ: склад и инвентаризация (правки 02.09.2026 — НЕ откатывать)

**Ведомость инвентаризации** (`InventorySheetView` + `Warehouse.tsx`):
- `opening` = движения ДО `from` **плюс** переучёт, датированный САМИМ `from`. Так «Початковий» = утверждённый факт последней инвентаризации. Не убирать `opening_inv` из агрегации.
- Строка отдаёт `calc` (Початковий+Надходж−Продано), `controls` [{date, fact, delta}], `recount`, `opening_recount`. Колонки на экране: Початковий · Надходж. · Продано · Кінцевий (розрахунок) · Контрольні переобліки · **В системі** · Факт · Розбіжність.
- ⚠️ Колонку «В системі» (`book`) НЕ заменять расчётной: от неё считается дельта в «Провести інвентаризацію» (`Warehouse.tsx`, фильтр `Number(facts[r.id]) !== Number(r.book)` и `delta: q - Number(r.book)`). Подставишь `calc` — проведение допишет лишнее.
- В `backend/smoke_all.py` есть строка «Склад: інвентаризаційна відомість» — не удалять.

**Приходная накладная:** поиск товара в `ReceiptModal.tsx` обязан идти с `is_active=true`. Без него в выпадашке появляются скрытые карточки-двойники (кейс: два SLATE(SL 1112), один — скрытая карточка без артикула в «шт» вместо «кг»), и приход садится не на ту карточку.

**Приход «в долг»:** кредиторка создаётся и в ветке `editDoc` (не только при создании). Ошибку POST `/api/planned-payments/` не гасить пустым `.catch(() => {})` — показывать. Фильтр `?source_stock=` в `PlannedPaymentViewSet.filterset_fields` нужен модалке, чтобы не плодить второй долг.

**Тестовые наборы (bundle):** реализация пишет состав набора НА МОМЕНТ отгрузки. Изменение `ProductComponent` задним числом старые накладные НЕ переписывает — товар продолжает «тянуться» в продажи старого компонента (кейс 02.09: набор 1686 списал Velvet Luna Bianco, хотя в комплектации уже Silver). Меняешь состав набора — проверь отгрузки после предыдущего изменения.

**Скрытые карточки:** `ProductCategoryViewSet.destroy` прячет товары с движениями (`is_active=False, category=None`). Поэтому остаток может «исчезнуть» из ведомости вместе с удалённой папкой. Перед выводами о недостаче проверять `is_active=false AND сумма движений <> 0`.

**Двойное списание:** если реализацию проводят позже фактической отгрузки, а между этим попадает переучёт — товар списывается дважды (кейс MGF 17: выдано 19.08, накладная 22.08, переучёт 21.08 → минус 85 кг). Корректировать переучёт, а не создавать новое списание.

Детали и список уже исправленных данных — memory `project_crm_inventory_audit_2026_09_02` и `crm-platform/docs/ИСТОРИЯ-ИЗМЕНЕНИЙ.md` (запись 02.09).

## 🔴 ПРАВИЛО КОММИТОВ: никаких `git add -A` в /root/gmideas

В репозитории одновременно работают несколько сессий (Claude + Codex) и правки лежат в рабочем дереве незакоммиченными часами. `git add -A` забирает ЧУЖИЕ файлы в свой коммит: 02.09 утренний коммит Meta-синка (709d6d7a) втянул чужие правки склада — код не потерялся, но лёг под неверным заголовком и в истории его не найти.

**Коммитить только свои файлы поимённо:** `git add crm-platform/backend/apps/<app>/<file>.py …`. Перед коммитом — `git status --short` и убедиться, что в списке нет файлов, которых ты не трогал. Если такие есть — оставить их в покое, не подмешивать.

Дополнение к инвентаризации (02.09, поздний вечер):
- `InventorySheetView`: `repack` вынесен из `received`/`sold_neg` в отдельные `repack_in`/`repack_out`; `book` = opening + received + repack_in − sold − repack_out + inv_net. Не возвращать репак обратно в «Продано» — розлив это не продажа.
- `realize_deal`: если `_expand_deal_items` пустой — документ НЕ создаётся (был кейс «накладна проведена, всередині 0 рядків»).
- `DealDetailSerializer.get_realization` отдаёт `missing` — позиции сделки, которых нет в проведённой накладной; карточка показывает красный блок и кнопку reship. Не удалять: это единственный сигнал о товаре, который уехал без списания.
- `InventoryDraftView` GET принимает `?rows=1&to=` и отдаёт полный список расхождений по ВСЕМ страницам — от него работает «Провести інвентаризацію». Не возвращать сбор строк на клиентскую пагинацию.

## 📌 16.09.2026 — где живые данные и как не перезаписать (обязательно прочитать перед работой)

**Живые документы (единственные актуальные):**
1. **Статус внедрения** — artifact `https://claude.ai/code/artifact/24cc74cf-6548-4c4c-8500-ab723211db01` (ведёт сессия plan-v9). Здесь ответы Олега, что сделано и что ждёт решения. Публикуется версиями: перед правкой — прочитать текущую версию (Artifact read) и строить правку от неё, не от своей копии.
2. **План внедрения v15** — artifact `afea7520-897f-4cc6-9c52-b7973f3249ec` (сессия wh15) — АРХИВ. Не перезаписывать: всё новое идёт в статус-документ.
3. **`crm-platform/docs/ИСТОРИЯ-ИЗМЕНЕНИЙ.md`** — человеческая история изменений (верхняя запись = самая свежая) + ChangeLogEntry в CRM («Що нового»).
4. **Волт Obsidian** — `Wallcov-Brain/Daily/<дата>.md` (что делала каждая сессия) и `Projects/…` (состояние систем).

**Перед первой правкой в сессии:**
- `dig +short crm.wallcovdec.com.ua` → 159.195.136.213; `cd /root/gmideas && git status --short && git log --oneline -5` — HEAD свежий, чужих незакоммиченных правок нет;
- прочитать верх `docs/ИСТОРИЯ-ИЗМЕНЕНИЙ.md` и Daily за сегодня — там, что уже сделано другими сессиями сегодня;
- взять lock на КАЖДЫЙ файл: `Wallcov-Brain/Infra/agent-lock.sh acquire file/crm-platform/<путь> <owner> <task> <files> <ttl> "<причина>"`; просроченный чужой lock забирать только после проверки `git status` файла и записи takeover в Daily.

**Данные (деньги, ставки, планы) — правила, чтобы старое не затирало новое:**
- **Ставки сотрудников** меняются ТОЛЬКО новой версией с 1-го числа (`POST /api/payroll/schemes/<id>/save/` с `valid_from`) — прошлые месяцы не пересчитываются. Никогда не редактировать чужую свежую версию «из головы»: сначала `GET`, потом правка.
- **Фонды Фінмоделі, ставки склада, планы менеджеров** — перед изменением бэкап JSON (`/root/<что>_backup_<дата>.json` или лог), после — запись в ИСТОРИЮ и ChangeLogEntry.
- **Массовые операции** (>5 записей): пробный прогон без записи → 2 записи → всё, с сохранением «как было» (BACKUP-строки в лог `/root/*.log`).
- **Утверждённые месяцы (PayrollRun) и прошлые периоды не пересчитывать.**
- **Записи журнала (деньги) не удалять** — правки только точечные и по «да» Олега; удаление операции делает сам Олег в CRM.

**Пакеты из песочниц** (`/root/claude-sandbox/*-pkg/apply.py`): применять только после `--check` на СВЕЖЕМ HEAD; если фрагмент найден не ровно 1 раз — не писать ничего и сливать вручную.

**Кто что вёл 15–16.09.2026:** правило веса/упаковки склада, регламент v2, экономика сделки, поиск, комментарии FB/IG, возвраты, «Розвиток v2», планы и стандарт склада — сессия plan-v9 (эта); ставки, фонды и план v15 — сессия wh15 (закрыта). Итоговое состояние — в статус-документе и в ИСТОРИИ.

## 📌 17.09.2026 — инварианты (НЕ откатывать, проверять перед правкой)

**Деплой.** `deploy.sh` читает ТОЛЬКО первый аргумент: `./deploy.sh web caddy` выкатит лишь бэкенд. Бэкенд + фронт — `./deploy.sh` без аргументов. После фронта проверить строку из правки в `docker compose exec caddy grep -l '<строка>' /srv/assets/*.js`.

**Воронки сайтов = стадии рабочих онлайн-воронок.** Вся автоматика ищет стадии ПО НАЗВАНИЮ: LiqPay/приём оплаты → «Оплату отримано» (+ задача складу), склад взял задачу → «Відвантаження», ТТН → «НП_ТТН створена», статусы НП → «НП_В дорозі» / «НП_Прибув на відділення» / «Отримано», ссылка на оплату → «Домовились про оплату». В воронках «Лендинг · …» и «23 Інтернет-магазин» стадии приведены к «21 Основний продукт» командой `python manage.py mirror_site_funnels` (DRY по умолчанию, `--apply`, `--limit N`; повтор ничего не делает). Нельзя:
- переименовывать/удалять стадии в «21 Основний продукт» / «22 Тестовий набір» без повторного `mirror_site_funnels --apply` и проверки всех 4 воронок;
- создавать новую воронку сайта со своими названиями стадий — только копией (`ensure_landing_funnels` берёт зразок из «Лендинг · wallcovdliastin.com.ua», он уже приведён).
Кейс: #66638 оплачена 15.09, уехала на «Подбор 3 вариантов», складу задача не пришла 2 дня.

**Нова Пошта: «Контроль оплати» при полной оплате.** `NPDelivery.tsx`: если остаток (сделка + выбранные дозаказы) = 0, наложка в ТТН НЕ отправляется даже при сохранённом `np_data.backward.on=true` (галочку в этом состоянии не видно). Бэкенд `create_ttn` оставляет запрет «наложку не можна ставити» — не убирать, это защита денег. Кейс #66748.

**Открытые линии: права.** `conversation.view.all` = командная очередь («Всі», «Не призначені», поиск) — больше НЕ даёт выбирать коллег и забирать чужие чаты. Список «Всі співробітники» и `?manager=` — только `conversation.staff_filter` (или `conversation.supervise`). «Закріпити»/«Переадресувати» чата, уже закреплённого за другим, — только `conversation.takeover` / `roles.manage` / суперюзер (`can_view_staff_chats`, `can_take_over_chats` в `inbox/views.py`). Отделу продаж эти два права не выдавать без решения Олега.

**«Взяв у роботу» в аналитике** — только через `apps/crm/take_stats.py` (тот же клиент тем же сотрудником за 30 дней = 1). Не считать `ActivityLog «Взяв чат»` напрямую в новых отчётах.

**Увольнение.** `UserViewSet.dismiss` передаёт коллегам клиентов, ОТКРЫТЫЕ лиды/сделки и незакрытые чаты; выигранные/проигранные остаются за сотрудником (история продаж и ЗП). Что кому передано — `accounts.StaffTransfer`; возврат в «Активні» до 7 дней (`set_status`) возвращает то, что коллеги не меняли, и открывает ставку. Кейс: Лаптев 17.09 09:53.

**Продажи с сайтов = онлайн в ЗП (17.09.2026).** `payroll/engine.policy()` добавляет воронки сайтов (`site_funnel_ids()`: «Лендинг · …», «Інтернет-магазин») в `funnels.online`; явные списки ставок `[21, 22]` расширяет `online_funnels()`. «Тест» / «основное» для сделок сайта — только через `kind_q()` / `deal_kind()` (тест = в сделке только тест-наборы). Новые места в ЗП/KPI/Розвитке, где фильтруете по воронкам, — через эти функции, НЕ через `pol["funnels"]["test"|"main"]` напрямую.

**Тонировка тест-наборов и выкрасок (17.09.2026).** Варианты и цены — только `apps/crm/kit_tint.py` (`options()`, `prices()`): набор — каталог / `ind` (+KIT_TINT_PRICE_IND=150) / `rich` (от KIT_TINT_PRICE_RICH=200); выкраска — каталог (цена карточки 150) / `s_ind` (+SAMPLE_TINT_PRICE_IND=100). `sync_lines` НЕ перезаписывает цену существующей строки-доплаты (менеджер поднимает «насыщенный от 200»). Выкраски — карточки «… викраска …» по названиям тест-наборов + рецепты `SampleRecipe` (тонкослойные 100 г/лист, фактурные 150 г/лист, 4 шт с листа); это НЕ комплектация — материал списывается при изготовлении («Викраски з А3»), не при продаже.

**Юля (ChatPlace) и база знаний CRM — цены тест-наборов, тонировки, выкрасок (17.09.2026).** Источник правды: CRM (каталог + Налаштування → Ставки). В ChatPlace IG `647e28e9` и TikTok `4aab5db8` обновлены KB (121), добавлено 7 записей, globalRules «📋 ПОВНИЙ ПРАЙС ТЕСТ-НАБОРІВ» (21 набор + блок тонировки/выкрасок), topic «Тестові набори». Бэкап живого состояния: Netcup `/root/claude-sandbox/cp_live_20260917.json`. При смене цен наборов/тонировки/выкрасок — менять И каталог CRM, И globalRules/KB Юли (обе базы), не только одно место. В CRM — 5 утверждённых KnowledgeItem (темы test_sets/tinting, аудитория без yulia_* — Юля обновляется напрямую, иначе публикация создаст дубли). Запрещённые старые цены: выкраска 50/100 грн, «+100 за свой цвет», варианты «Старт/Креатив/Максимум», Velvet Luna Lure / Luna Lux / Velvet Lux БАЗА.

**Меню «Відвантаження» = право `warehouse.work`** (отдельно от `warehouse.view` «Складський облік»). Менеджерам продаж не выдавать; Оксаненко и Лаптеву запрещено через «Заборонені права».

**Швидкі відповіді з номенклатури (17.09.2026).** `apps/inbox/product_replies.py` + `QuickReplyFolder` (папка номенклатури → розділ). Відповідь = сімʼя товару (`shop_group_key` або `p<id>`), `QuickReply.products`, `{ціни}` у тексті рендериться при показі (`_replies_payload`) і відправці (`send_library`) — НЕ вписувати ціни цифрами в ці відповіді. Синхронізація ліниво при відкритті вікна (раз на 2 хв/процес), `manage.py sync_product_replies [--apply]`; текст/назву/«коли» синхронізація не переписує. Привʼязки: папка 59 «Тестові набори та викраски» → «Тест-набори з цінами» (test_set), папка 112 «Викраски» (дочірня 59) → «Викраски з цінами» (sample). **Викраски живуть у підпапці 112** — перевірки «це тест-набір» ідуть через `apps.crm.kit_tint.in_test_folder()` (папка або батьківська з «тестов»), не через точну назву категорії; `shop_sync.SAMPLE_CATEGORY_ID=59` навмисно не включає 112 (викраски не публікуються в магазин). Вікно `QuickRepliesScript.tsx`: розділи «Хід розмови» (кроки) / «Ситуації» / «Товари з цінами» / обрані й нещодавні (localStorage); «/» у порожньому полі чату (Inbox.tsx) відкриває вікно.
